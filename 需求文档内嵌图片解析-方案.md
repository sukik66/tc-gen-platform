# 需求文档内嵌图片 / 扫描件 — 技术方案（待拍板）

## 背景与现状

| 入口 | 当前行为 |
|------|------------|
| 单独上传 `.png` / `.jpg` 等 | `extractImageOcrText`：浏览器端 **Tesseract.js**，输出 OCR 文本 |
| `.docx` | **mammoth.extractRawText**：仅正文/表格等可转文本，**嵌入图、截图不进模型** |
| `.pdf` | **pdf.js getTextContent**：仅文字层；**扫描件/纯图页**通常几乎无字 |

目标：需求里**插在 Word/PDF 里的示意图、流程图、表格截图**等，能变成可参与生成的文字（或结构化占位），而不是静默丢失。

---

## 方案 A（推荐先做）：仅增强 .docx 内嵌图

**思路**：在解析 `.docx` 时除纯文本外，遍历文档内图片资源 → OCR → 按阅读顺序拼成「图片附录」块，与 `extractRawText` 结果合并。

**实现要点**：

1. 使用 **mammoth** 的 `convertToHtml` + `convertImage: mammoth.images.dataUri`（或 `inline`）拿到每图 base64/ArrayBuffer。
2. 或 **JSZip** 直接打开 docx，读取 `word/media/*`，用 `document.xml` 中关系与锚定顺序还原「图在文档中的大致位置」（工作量中等）。
3. 每张图走现有 **Tesseract** 管线（与 `extractImageOcrText` 复用 worker；**串行**避免并发炸内存）。
4. 合并策略（二选一或组合）：
   - **A1**：在最终 `text` 末尾追加 `\n\n--- 文档内图片 OCR（按顺序）---\n` + 各段 `[图N]\n{ocr}\n`；
   - **A2**：在 mammoth 抽出的纯文本中插入占位 `[图1:见附录]`，附录集中写 OCR（便于模型对照顺序）。

**限制与护栏**：

- 最多处理 **N 张图**（如 30），单图最大边 **降采样**（如 2048px），超限跳过并 `parseNote` 说明。
- 总 OCR 时间可能较长：侧栏解析状态显示 **「正在识别文档内图片 (x/y)…」**。

**依赖**：无新增服务端依赖；仍全在浏览器完成（与现架构一致）。

---

## 方案 B：扫描 / 弱文字 PDF 整页 OCR

**思路**：对每一页：若 `getTextContent` 得到字符数 **低于阈值**（如 &lt; 50 字/页），则将该页 **渲染为位图**（pdf.js `render` 到 canvas）→ 整页 OCR → 与文字层合并。

**代价**：页数多时 **非常慢**、内存高；需分页进度与 **可取消**。

**可选折中**：仅对「用户勾选的前 K 页」或「仅首页+目录页」做 OCR，其余提示「疑似扫描件，请拆成图片单独上传」。

---

## 方案 C（后期）：服务端多模态

将 docx/pdf 或导出图上传至支持视觉的 API，由模型直接读图。**成本、合规、密钥与网络**与当前「本地 dev + 自建 API」假设冲突较大，建议不作为第一阶段。

---

## 建议落地顺序

1. **Phase 1**：方案 A（docx 内嵌图 + 附录 OCR），可观测、与现有栈一致。  
2. **Phase 2**：方案 B 的「弱文字页触发整页 OCR」+ 强限制（页数、超时、取消）。  
3. **Phase 3**：视产品需要再评估方案 C。

---

## 待你拍板的问题

1. **合并形态**：更倾向全文后附录（A1），还是文中占位 + 附录（A2）？  
2. **docx 顺序**：是否必须严格按文档流插入 OCR（实现选 JSZip+关系，工期更长），还是「先全文后按 media 文件名排序 OCR」可接受 MVP？  
3. **PDF**：是否 Phase 1 必须带 PDF，还是先做 docx 再迭代？  
4. **语言**：OCR 维持 `chi_sim+eng` 是否足够，是否要加繁体/竖排等？

---

*文档版本：草案，未改代码；拍板后再进开发任务。*
