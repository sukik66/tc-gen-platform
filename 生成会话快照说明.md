# 测试用例生成 · 本次会话快照说明

本文说明「测试用例生成」页面中 **「本次会话快照」** 功能的行为与限制，便于使用与维护。

## 功能位置

路径：**测试用例生成** 主内容区，在「生成的测试用例」标题与视图切换（脑图/卡片/表格）工具条下方，区块标题为 **「本次会话快照」**。

包含：

- **下拉框**：列出本会话内已自动保存的快照，选择后可将该快照恢复到右侧用例区域。
- **「清除快照」按钮**：仅删除已保存的快照记录，**不修改**当前右侧正在显示的用例列表。

## 存什么、何时存

- 每次 **API 流式生成成功结束**（`onDone`），且 **本轮合并进列表的新用例条数大于 0** 时，自动追加 **一条快照**。
- 快照内容为 **当时右侧用例列表的完整副本**（包含列表中已有用例 + 本轮新增），便于整体恢复或对比调试。
- 仅通过 **「API 生成用例」** 成功触发生成时追加；手动编辑、Cursor 辅助导入等 **不会** 自动新增快照。

## 如何使用

1. **恢复某次生成结果**：在下拉框中选择对应条目 → 按提示确认 → 右侧列表被该快照 **整体替换**（进行中的内联编辑会先关闭，未确认的编辑会丢失）。
2. **放弃所有备份记录**：点击「清除快照」并确认 → 仅清空 `sessionStorage` 中的快照数据，**当前列表不变**。

下拉列表按时间 **新 → 旧** 排列；每条展示标签含 **时间、总条数、末条用例摘要前缀**（便于辨认）。

## 数据保存在哪里、保留多久

- 使用浏览器 **`sessionStorage`**，键名：`ai-test-platform:generation-session-snapshots:v1`。
- **同一标签页内刷新页面**，快照仍在。
- **关闭该标签页** 后，`sessionStorage` 被浏览器清空，快照 **不再保留**（与「关电脑」无必然关系，以是否关闭该标签为准）。

若需跨标签、跨天保留，需另行设计（例如 `localStorage` 或导出文件），当前版本不包含。

## 容量与上限

- 最多保留 **15 条** 快照；超出时丢弃更早的记录。
- 若单页用例很多导致写入超出浏览器配额，实现上会 **减半重试** 写入，直至成功或仅剩一条仍失败则放弃本次持久化（不影响生成结果本身）。

## 与其它逻辑的关系

- 恢复快照后，会同步更新 **修订记录基线**（`revisionBaselineRef`），避免把「恢复」误判成大量人工编辑差异。
- 恢复快照前会 **关闭** 用例编辑弹层/内联编辑（`clearDraftUi`），避免状态不一致。

## 相关源码（维护用）

| 说明           | 路径 |
|----------------|------|
| 读写与追加逻辑 | `src/lib/generationSessionCache.ts` |
| UI 与生成挂钩  | `src/pages/TestCaseGenerationPage.tsx`（`sessionSnapshots`、`appendGenerationSessionSnapshot`、`handleRestoreSessionSnapshot` 等） |

---

*文档与当前实现对齐；若产品逻辑变更，请同步更新本说明。*
