# AI 测试平台 · 本地启动说明

本目录为 **AI 原生测试平台**（Web + 本地 API），与仓库根目录的 SuperAI Python 控制台 **相互独立**，需 **单独终端** 启动，避免与其它任务的子进程一并被结束。

**一键释放端口并重启（本机调试）**：在仓库根目录执行 `scripts\restart-local-services.cmd`（或 `powershell -File scripts\restart-local-services.ps1`）。默认会结束 **5173 / API_PORT / SuperAI Web 端口 / toolbox 端口** 上的监听进程，并各开一个新窗口启动 **SuperAI Web** 与 **`npm run dev`**。参数见脚本内注释（`-SkipWeb`、`-SkipAiTestPlatform`、`-NoStart` 等）。

---

## 环境要求

- **Node.js**：建议 LTS 版本（与 `package.json` 中依赖兼容）。
- **操作系统**：Windows / macOS / Linux 均可。

---

## 第一次使用

在 **`ai-test-platform`** 目录下执行：

```bash
npm install
```

复制环境变量模板并编辑（Windows PowerShell 可用 `Copy-Item .env.example .env`）：

```bash
cp .env.example .env
```

在 `.env` 中至少配置：

1. **`LLM_PROVIDER`**：与你要用的厂商一致（见 `.env.example` 顶部说明）。
2. **当前厂商对应的一组变量**（API Key、Base URL、模型名等）；其余厂商可留空。

详细变量说明见 **`.env.example`** 与 **`README.md`** 中的「大模型配置」章节。

---

## 日常启动（推荐）

```bash
cd ai-test-platform
npm run dev
```

该命令会 **并行** 启动：

| 进程 | 默认地址 | 说明 |
|------|----------|------|
| 本地 API | `http://127.0.0.1:8787` | `server/index.js`；终端出现 `[ai-test-platform API]` 即表示已监听 |
| 前端（Vite） | 多为 `http://localhost:5173` | 若 5173 被占用，终端会显示实际端口；`vite --open` 会尝试自动打开浏览器 |

前端通过开发代理访问 **`/api/*`**，请求会转发到上述本地 API，**无需**再单独配置跨域。

**常用入口：**

- 功能目录：`/`  
- 用例生成：`/generation`  
- 用例库：`/case-library`  
- 知识库：`/knowledge`  

---

## 其它脚本（可选）

| 命令 | 用途 |
|------|------|
| `npm run dev:api` | 仅启动本地 API（一般不单独用） |
| `npm run dev:web` | 仅启动 Vite（无后端时 `/api` 不可用） |
| `npm run dev:vite` | 同「仅前端」，不调大模型场景不推荐 |
| `npm run build` | 生产构建（`tsc` + `vite build`） |
| `npm run preview` | 预览构建产物 |
| `npm run start:api` | 无 watch 的生产方式启动 API（部署或排错用） |

---

## 常见问题

1. **页面能开但生成失败**  
   检查 `.env` 是否保存、对应厂商 Key 是否有效；终端是否有 API 报错。

2. **端口被占用**  
   修改 Vite 端口或释放占用进程；API 端口见 `server/index.js` 中的 `PORT`（可用环境变量覆盖，以代码为准）。

3. **与 SuperAI 主项目同时运行**  
   请在本目录 **独立终端** 运行 `npm run dev`，不要依赖其它脚本顺带拉起，以免停止父任务时误杀子进程。

---

## 更多文档

- **`README.md`**：功能说明、厂商列表、排错与项目关系。  
- **`CONTEXT.md`**：产品约定与决策摘要。  
- **`TASKS.md`**：功能清单与实现状态。
