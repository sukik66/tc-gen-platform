# 质量契约走查报告

- 日期：2026-05-07 14:56
- 需求文档：新手引导-一测.docx
- 走查引擎：Cursor Agent（重新搜索模式 refreshSearch=true）

## 总览

| 指标 | 数量 |
|------|------|
| 契约总数 | 17 |
| 代码走查 (code_review) | 15 |
| 通过 (pass) | 6 |
| 违规 (fail) | 0 |
| 存疑 (uncertain) | 9 |
| 跳过 (非 code_review) | 2 |
| 调度模式 | 并行（1组，15条） |
| ast-grep | 降级（sg 不可用） |
| Evidence 验证 | total=43, verified=43, failed=0, downgraded=0 |
| 映射缓存(v2) | 本次重新搜索，缓存脚本读取旧格式节点，更新 +0（需格式统一后重跑） |

---

## 走查详情

### 1. 新手引导 > 关卡选择 — uncertain (45%)

**规则**：新手引导 Step 0 触发时，关卡选择界面默认选中第一关且第一关为唯一可点击关卡，其余关卡按钮处于锁定不可交互状态

**边界提示**：锁定由 PreRequisiteLevel + mIsLocked 字段控制，使按钮 interactable=false；引导期间即使数据满足解锁条件，非当前引导关卡仍强制锁定

**判定**：uncertain | 置信度 45%

**推理**：StarMapPoint.Init() 通过 PreRequisiteLevel 实现关卡锁定（PreRequisiteLevel==0 则解锁，否则依赖前关完成）。SelectLevel() 调用 mBattleButtonCanvasGroup.SetCanvasGroupActive(!isLocked) 屏蔽战斗按钮。但契约要求「引导期间即使数据满足解锁条件，非当前引导关卡仍强制锁定」的额外保护逻辑在代码中未找到——现有机制完全由配置数据中的 PreRequisiteLevel 驱动，不存在教程阶段专用的强制覆盖锁定。MainUITutorialPanel.StartMatchTutorial 只高亮战斗按钮，不修改关卡锁定状态。

**证据**：
- `client/Assets/Scripts/ExternalClient/StarMap/StarMapPoint.cs` L51-58 `Init` — PreRequisiteLevel==0时 mIsLocked=false，否则依赖 CheckBattleStageFinish
- `client/Assets/Scripts/ExternalClient/UI/SubUIPanel/MainUI/ChapterPanel_MainPanel.cs` L508-512 `SelectLevel` — SelectLevel → mBattleButtonCanvasGroup.SetCanvasGroupActive(!isLocked)
- `client/Assets/Scripts/ExternalClient/UI/SubUIPanel/TutorialUI/MainUITutorialPanel.cs` L74-123 `StartMatchTutorial` — Step 0 仅高亮 BattleButton，无关卡强制锁定逻辑

**缺口**：
已验证：针对「引导期间强制锁定非当前关卡」做了定向搜索（Grep NoviceGuide|tutorial.*lock|TutorialStep.*lock），未找到任何代码实现此逻辑；结论：该保护依赖配置数据而非代码层面强制。

---

### 2. 新手引导 > 引导蒙版射线拦截 — pass (72%)

**规则**：强引导进行中，非当前步骤高亮区域的 UI 被半透明蒙版遮盖且射线检测被拦截（IsRaycastLocationValid 返回 false），仅高亮镂空区域内的 UI 可响应点击

**边界提示**：步骤切换时蒙版镂空区域与高亮目标同步更新，不得出现全屏遮挡的中间帧；高亮区域由 GuideCutoutImage 的 targetArea 定义

**判定**：pass | 置信度 72%

**推理**：两套射线拦截实现均存在：(1) GuideCutoutMaskRaycastFilter.IsRaycastLocationValid() 始终对洞外返回 !inside（拦截），洞内穿透；(2) GuideCutoutMaskController.ICanvasRaycastFilter.IsRaycastLocationValid() 在 mPassThroughHoleRaycasts=true 时同样对洞内穿透。遮罩镂空参数由 Shader(_HoleCenter/_HoleExtents/_ShapeMode)与 IsInsideHole 几何判断共用同一套参数，保证视觉与射线一致。步骤切换时 SetHoleAtPosition/SetHoleFromTarget 会重新计算洞位置。

**证据**：
- `client/Assets/Scripts/ExternalClient/UI/Common/GuideCutoutMaskSupport.cs` L174-191 `IsRaycastLocationValid` — GuideCutoutMaskRaycastFilter: return !inside —— 洞外拦截，洞内穿透
- `client/Assets/Scripts/ExternalClient/UI/Common/GuideCutoutMaskController.cs` L251-258 `IsRaycastLocationValid` — mPassThroughHoleRaycasts=true时：return !IsScreenPointInsideHole(sp, eventCamera)
- `client/Assets/Scripts/ExternalClient/Utils/TutorialMask.cs` L44-73 `SetHoleAtPosition` — TutorialMask 通过 HoleMaskImage.holeRect 设置镂空区域

**缺口**：已验证：Grep mPassThroughHoleRaycasts 未找到运行时赋值代码，依赖 Prefab 序列化值——需 ui_test 确认 mPassThroughHoleRaycasts=true 在目标 Prefab 中配置正确。

---

### 3. 新手引导 > 组件tips层级 — uncertain (38%)

**规则**：Step 4/Step 11 点击教程组件（230068、230057）后弹出的 tips 面板层级高于引导蒙版，tips 内容完整可见不被蒙版遮挡

**边界提示**：该规则对 ID=230068 和 ID=230057 两个教学组件均适用；tips 关闭时机由下一步引导触发

**判定**：uncertain | 置信度 38%

**推理**：PlaneUITutorialPanel 被实例化后立即调用 SetAsFirstSibling()，在 TutorialRoot 下排为第一子节点（渲染最底层）。契约要求 tips 面板层级高于引导蒙版，但代码实际做的是把教程蒙版放到最低层。PlanePartTipsPanel_Portrait 没有看到显式的 sortingOrder 设置。真实运行时层级依赖 Prefab 中 Canvas 配置和 TutorialRoot 与 TipsPanel 父子关系，仅从代码无法确认 tips > mask 的层级关系。

**证据**：
- `client/Assets/Scripts/ExternalClient/UI/UIPanel/PlaneEditUI_Portrait.Tutorial.cs` L50 `OpenPlaneUITutorial` — mUITutorialPanel.transform.SetAsFirstSibling() —— 教程面板在 TutorialRoot 下排最前（底层）
- `client/Assets/Scripts/ExternalClient/UI/SubUIPanel/PlaneEditUI/PlanePartTipsPanel_Portrait.cs` L13-80 `SetTips` — SetTips 激活 tips 面板，无 sortingOrder 显式设置

**缺口**：已验证：搜索 sortingOrder.*Tutorial|TipsPanel.*sortingOrder 无结果；TutorialRoot 与 TipsPanel 的 Canvas 层级关系需读取 Prefab 或运行时测试才能确认，此为必须的 ui_test 验证项。

**附带发现**（隐式审查）：
- [待确认] PlaneUITutorialPanel SetAsFirstSibling 可能是故意设计为底层蒙版，Tips 通过单独 Canvas 层在其上显示——但代码证据不足以确认

---

### 4. 新手引导 > 拖拽安装 — uncertain (50%)

**规则**：Step 5/Step 6 拖拽教学：(A) 玩家将组件拖至飞船组装区域黄色框内有效位置释放则安装成功，手势提示消失，文字提示保留，组件 tips 关闭，底版区域高亮持续至组件落位；(B) 仅点击未拖拽或拖至区域外释放，组件回退到背包原位且手势引导重新出现

**边界提示**：回退后组件在背包中的排列顺序与操作前一致；黄色框位置由 tutorialValidArea 定义；手势引导重新出现时重置到拖拽初始状态

**判定**：uncertain | 置信度 50%

**推理**：成功路径已找到：PlaneEditUI_Portrait 在组件从背包拖入编辑区后调用 OnEnterTutorialArea()，后者设置 mIsOverTutorialArea=true 并派发 TUTORIAL_IS_COMPLETED 事件触发安装完成；OnDrag 过程中若 mIsOverTutorialArea=true 则把组件位置 Clamp 在 tutorialValidArea 内。失败路径（区域外释放回退背包 + 手势重显）未在代码中直接找到。CheckInValidArea 中检测进入教程区域的分支被注释掉（由外部 PlaneEditUI_Portrait.OnDragCloneEnterArea 显式调用替代）。

**证据**：
- `client/Assets/Scripts/ExternalClient/UI/UIPanel/PlaneEditUI_Portrait.cs` L392 `OnDragCloneEnterArea` — 拖拽入编辑区时 PlaneEditorManager.Instance.OnEnterTutorialArea() 被调用
- `client/Assets/Scripts/ExternalClient/Managers/PlaneEditorManager/PlaneEditorManager.Operate.cs` L193-207 `OnEnterTutorialArea` — mIsOverTutorialArea=true; Dispatch(TUTORIAL_IS_COMPLETED, PlaneTutorialStep)
- `client/Assets/Scripts/ExternalClient/Managers/PlaneEditorManager/PlaneEditorManager.Operate.cs` L379-428 `OnDrag` — 拖拽时若 mIsOverTutorialArea=true 则 ClampScreenPointToRect 限制在 tutorialValidArea 内

**缺口**：已验证：搜索 ReturnToBag|SendBackToBag|BackToInventory|重置.*位置 未找到释放失败后回退背包的显式逻辑；搜索 Tutorial_text_5.*reset|手势.*重新 同样无结果；结论：「区域外释放回退」和「手势引导重显」逻辑缺失或在事件驱动流程中未被找到。

---

### 5. 新手引导 > 重叠提示 — uncertain (58%)

**规则**：玩家首次将组件拖至与已安装组件重叠位置时，旋转和翻转按钮变为高亮状态并显示 Tutorial_text_6，提示持续至组件被正确安装到非重叠有效位置

**边界提示**：同一教程周期内该提示仅显示一次（首次触发后置 mIsShowedTips=true 不再触发）；组件初次即放置到非重叠位置则此提示不出现

**判定**：uncertain | 置信度 58%

**推理**：Tutorial_text_6 在 StartCompletePartAssemblyTutorial 中当 mIsTips=true && !mIsShowedTips 时被触发，之后 mIsShowedTips=true 防止重复显示，ClosePanel() 重置为 false——单次触发机制完整。但契约要求「旋转和翻转按钮变为高亮状态」——代码中该函数执行时设置的是 SetMaskColor(false)/SetCanvasGroup(false)（无蒙版/无拦截），没有看到对旋转/翻转按钮的高亮驱动逻辑。触发条件中「与已安装组件重叠」的检测路径也未完整找到。

**证据**：
- `client/Assets/Scripts/ExternalClient/UI/SubUIPanel/TutorialUI/PlaneUITutorialPanel.cs` L289-344 `StartCompletePartAssemblyTutorial` — mIsTips && !mIsShowedTips 时 mIsShowedTips=true，显示 Tutorial_text_6
- `client/Assets/Scripts/ExternalClient/UI/SubUIPanel/TutorialUI/PlaneUITutorialPanel.cs` L132 `ClosePanel` — ClosePanel() → mIsShowedTips = false（每次 tutorial 关闭后重置）

**缺口**：已验证：搜索 HighlightRotate|HighlightFlip|旋转.*高亮|翻转.*高亮 无结果；旋转/翻转按钮高亮逻辑未找到代码实现。

---

### 6. 新手引导 > 一键替换 — pass (78%)

**规则**：Step 7 点击「一键替换更强组件」后，系统从背包中选取比当前装备 Rarity 更高的同 PartId 组件（GetBetterSolution 返回值）替换到飞船对应槽位，被替换组件移回背包；教程中替换目标为组件 210010（高速机枪位），同时高亮背包中的 210010 和即将被替换的飞船组件

**边界提示**：若存在多个可替换候选，仅替换 GetBetterSolution 返回的第一个匹配项；替换操作为原子操作，中途不可中断

**判定**：pass | 置信度 78%

**推理**：GetBetterSolution() 按 PartId 分组，已装部件按 Rarity 升序、候选按 Rarity 降序，首个满足 cand.Rarity>inst.Rarity 的候选即被选中并加入 result 字典——仅返回第一个匹配项。ReplacePlanePart(src, des, step, callBack) 在原位置销毁旧部件并创建新部件。OnPlaneAllInfoUpdate 中通过 GetReplacePart(partUid) 获取背包中的 210010 组件视觉体并调用 SetReplacePart 显示在教程面板。210010 由 Rarity 数据驱动匹配，不硬编码。

**证据**：
- `client/Assets/Scripts/ExternalClient/Managers/PlaneEditorManager/PlaneEditorManager.Data.cs` L1363-1428 `GetBetterSolution` — 按PartId分组、cand.Rarity>inst.Rarity，首个匹配写入result字典并break
- `client/Assets/Scripts/ExternalClient/UI/SubUIPanel/PlaneEditUI/PlaneEditPanel_Portrait.cs` L506-525 `OnReplaceClick` — foreach betterSolution: ReplacePlanePart(kvp.Value, map[kvp.Key], step, callback)
- `client/Assets/Scripts/ExternalClient/UI/SubUIPanel/PlaneEditUI/PlaneEditPanel_Portrait.cs` L427-436 `OnPlaneAllInfoUpdate` — TutorialStep==ReplacePart时 GetReplacePart(partUid) + SetReplacePart 高亮210010视觉体

**缺口**：教程中210010的飞船槽位高亮（即将被替换的飞船组件高亮）逻辑未直接确认——可能由 ReplacePart 视觉效果替代。

---

### 7. 新手引导 > 替换动画反馈 — uncertain (22%) ⚠️

**规则**：点击一键替换后：(A) 被替换组件在飞出前显示明显特效标识；(B) 新组件从背包飞入飞船槽位、被替换组件从飞船飞出回到背包的过渡动画完整播放；(C) 动画期间用户操作被屏蔽至动画结束

**边界提示**：动画结束后 UI 状态与数据一致（飞船上显示新组件，背包显示旧组件），无中间不一致状态

**判定**：uncertain | 置信度 22%

**推理**：在所有相关 .cs 文件中全面搜索 FlyIn|FlyOut|FlyAnimation|飞入|飞出|BlockInput|isInteractable 均无结果。ReplacePlanePart(src, des, step, callBack) 的实现是直接 DestroyImmediate(des.gameObject) 后创建替换部件，不存在飞行过渡动画。唯一找到的视觉反馈是 Effect_UI_SKinSuccess 特效（安装特效），但这不是飞入飞出动画。用户操作屏蔽（动画期间禁止操作）的逻辑也未找到。

**证据**：
- `client/Assets/Scripts/ExternalClient/Managers/PlaneEditorManager/PlaneEditorManager.cs` L670-709 `ReplacePlanePart` — 直接 DestroyImmediate(des.gameObject) + CreateReplacePlanePart，无动画过渡
- `client/Assets/Scripts/ExternalClient/Managers/PlaneEditorManager/PlaneEditorManager.cs` L689-699 `ReplacePlanePart` — step != None && step != CompletedSkillPartAssembly 时生成 Effect_UI_SKinSuccess 特效

**缺口**：已验证：全局搜索 FlyIn|FlyOut|FlyAnimation|BlockInput 无结果。结论：飞入飞出动画和操作屏蔽逻辑在代码层面缺失；可能存在于 Animator 控制器或 Prefab 中（需 ui_test）。

**附带发现**（隐式审查）：
- [已确认] ReplacePlanePart 存在两个重载——一个含 TutorialStepEnum 参数（教程用）、一个无（常规用）；教程版本额外生成安装特效

---

### 8. 新手引导 > 关卡绑定 — pass (68%)

**规则**：新手引导第一关与第二关通过 PreRequisiteLevel 与教程步骤进度双重绑定：第一关未通关前第二关按钮处于锁定禁用状态；两个引导关卡使用标准战斗接口（ChallengeBattleStage）发起战斗，战利品掉落与正常关卡机制一致

**边界提示**：第二关解锁条件同时依赖教程步骤进度；引导关卡战斗失败时的处理（重新挑战 vs 视为通关）需求未明确

**判定**：pass | 置信度 68%

**推理**：ChallengeBattleStage 标准接口已确认用于关卡战斗（ChapterPanel_MainPanel.StartGame()）。第一关的 PreRequisiteLevel 配置为 0（解锁），第二关依赖第一关通关。TutorialUtil.CheckMainPanelCurTutorialIn() 中 SecondBattle 步骤在检测第二关是否通关后才推进到 SecondEdit，构成与教程步骤的间接联动。

**证据**：
- `client/Assets/Scripts/ExternalClient/UI/SubUIPanel/MainUI/ChapterPanel_MainPanel.cs` L260 `StartGame` — await AccessManager.Instance.ChallengeBattleStage(stageId, scheme.SchemeId)
- `client/Assets/Scripts/ExternalClient/StarMap/StarMapPoint.cs` L51-58 `Init` — PreRequisiteLevel==0→解锁；非0→CheckBattleStageFinish 控制第二关锁定
- `client/Assets/Scripts/ExternalClient/Utils/TutorialUtil.cs` L179-184 `CheckMainPanelCurTutorialIn` — SecondBattle: CheckBattleStageFinish(2) 通关后才推进到 SecondEdit

**缺口**：引导关卡战斗失败处理逻辑（重新挑战 vs 视为通关）未找到，需进一步追踪战斗结算流程。

---

### 9. 新手引导 > 激活组件放置 — uncertain (42%)

**规则**：Step 12 激活类组件（ID=230057）拖拽阶段仅可放置于飞船组装区域预设的 4 个特定合法格位；若上一步普通组件已占用其中 N 格，则界面仅高亮显示剩余 4-N 个可用合法格位

**边界提示**：4 个合法格位坐标由 tutorialValidArea 或配置数据定义；4 格全被占满时该组件拖到任何位置均视为非法释放

**判定**：uncertain | 置信度 42%

**推理**：代码中有 tutorialValidArea (RectTransform) 对 DragSkillPart/FindValidPosition 阶段启用约束，拖拽位置被 Clamp 到 tutorialValidArea 范围内。但契约要求「4个特定合法格位」是由 RectTransform 面积与网格大小（GridSize）共同决定的运行时结果，静态代码无法确认恰好是4格。且「若前一步组件占用N格则仅高亮剩余4-N个」的动态扣减逻辑未找到。

**证据**：
- `client/Assets/Scripts/ExternalClient/Managers/PlaneEditorManager/PlaneEditorManager.Operate.cs` L136-138 `CheckInValidArea` — tutorialValidArea 在 DragSkillPart/FindValidPosition/CompletedSkillPartAssembly 阶段启用
- `client/Assets/Scripts/ExternalClient/UI/UIPanel/PlaneEditUI_Portrait.Tutorial.cs` L17-18 `ValidArea_Tutorial` — mValidArea_Tutorial RectTransform 序列化字段，由 Prefab 决定尺寸

**缺口**：已验证：搜索 4.*格位|validCount|occupiedCount 无结果；4-N 格位动态扣减逻辑未找到。tutorialValidArea 对应的实际格子数量需运行时或 Prefab 配置确认。

---

### 10. 新手引导 > 激活组件拖拽状态 — uncertain (32%) ⚠️

**规则**：拖拽激活组件过程中系统实时检测组件位置并在三种状态间循环切换：(1) 组件未在合法格位时显示 Tutorial_text_14；(2) 组件已放入合法格位但未成功激活相邻武器组件时显示 Tutorial_text_12；(3) 成功激活武器组件时显示 Tutorial_text_13

**边界提示**：三种状态随玩家操作循环检测变化，不是单向线性推进；旋转/翻转操作可能触发状态 2→3 的切换

**判定**：uncertain | 置信度 32%

**推理**：代码中存在 Tutorial_text_12（StartFindValidPositionTutorial）和 Tutorial_text_13（StartCompletedSkillPartAssemblyTutorial），但契约要求的状态1（Tutorial_text_14，组件未在合法格位）在代码中未找到——DragSkillPart 阶段使用 Tutorial_text_11 而非 Tutorial_text_14。OnExitTutorialArea() 为空实现（L209-213），即组件移出合法格位后没有回切到状态1的逻辑。三种状态的循环切换机制不存在，现有实现是单向线性推进。

**证据**：
- `client/Assets/Scripts/ExternalClient/UI/SubUIPanel/TutorialUI/PlaneUITutorialPanel.cs` L465-497 `StartDragSkillPartTutorial` — DragSkillPart 阶段使用 Tutorial_text_11（非 Tutorial_text_14）
- `client/Assets/Scripts/ExternalClient/UI/SubUIPanel/TutorialUI/PlaneUITutorialPanel.cs` L514-540 `StartFindValidPositionTutorial` — Tutorial_text_12 在 FindValidPosition 阶段显示
- `client/Assets/Scripts/ExternalClient/Managers/PlaneEditorManager/PlaneEditorManager.Operate.cs` L209-213 `OnExitTutorialArea` — OnExitTutorialArea() 为空实现——移出合法格位无回切逻辑

**缺口**：已验证：全局搜索 Tutorial_text_14 在 .cs 文件中无结果；三状态循环切换机制缺失。OnExitTutorialArea 为空实现已确认。

**附带发现**（隐式审查）：
- [已确认] `PlaneEditorManager.Operate.cs` L209-213 `OnExitTutorialArea` — 空实现，移出教程区域后状态不会回退到初始提示（三状态循环机制缺失）

---

### 11. 新手引导 > 激活影响显示 — pass (82%)

**规则**：选中带有激活效果的组件时，其 Effect 影响范围内的格位常驻显示高亮标记（ValidSprite / AreaSprite），被影响位置的已安装组件增加被激活特效；组件取消选中后影响范围高亮和特效立即消失

**边界提示**：拖拽过程中影响范围随组件位置实时更新（UpdateEffGrid）

**判定**：pass | 置信度 82%

**推理**：CPlanePartInfo 提供完整的 ShowEffGrid/UpdateEffGrid/HideEffGrid 调用链。PlaneOperatePanel_Portrait.Init() 调用 mPartInfo.ShowEffGrid() 在选中时显示。OnDrag 每帧调用 UpdateEffGrid() 实时更新。OnTouchDown 切换组件时调用 lastActivePlanePart.HideEffGrid() 清除之前组件影响范围。PlaneEditHitGrid 持有 ValidSprite（位置有组件）和 AreaSprite（位置无组件）两种精灵，UpdateEffect() 根据 HasOverlapPart 切换。

**证据**：
- `client/Assets/Scripts/ExternalClient/UI/SubUIPanel/PlaneEditUI/CPlanePartInfo.cs` L427-433 `ShowEffGrid` — ShowEffGrid → SetGrid(Effect) + UpdateEffGrid()
- `client/Assets/Scripts/ExternalClient/UI/SubUIPanel/PlaneEditUI/CPlanePartInfo.cs` L439-444 `HideEffGrid` — HideEffGrid → foreach effGrid.HideGrid()
- `client/Assets/Scripts/ExternalClient/UI/SubUIPanel/PlaneEditUI/PlaneEditHitGrid.cs` L61-65 `UpdateEffect` — ValidSprite（有组件）/ AreaSprite（无组件）切换精灵
- `client/Assets/Scripts/ExternalClient/UI/SubUIPanel/PlaneEditUI/PlaneOperatePanel_Portrait.cs` L80 `Init` — mPartInfo.ShowEffGrid() 在选中时调用

**缺口**：ValidSprite / AreaSprite 具体视觉表现由 PlaneEditHitGrid.UpdateEffect 实现，未深入确认。

---

### 12. 新手引导 > 激活按钮半径 — uncertain (25%)

**规则**：激活类组件的 4 个旋转/翻转按钮的半径配置需考虑组件 Effect 影响位置（按钮可达性需覆盖影响位置）或为按钮半径单独配置参数

**边界提示**：影响位置不应超出按钮可点击半径，否则旋转/翻转按钮无法操作覆盖范围内目标

**判定**：uncertain | 置信度 25%

**推理**：契约要求激活类组件的4个旋转/翻转按钮半径需考虑 Effect 影响位置。PlaneOperatePanel_Portrait 管理旋转/翻转按钮，但未读取其完整代码。搜索关键词（EffRadius|ButtonRadius|effectRadius|影响半径）在 .cs 文件中无结果。此为 P2 优先级需求，代码中是否有独立的激活组件按钮半径参数配置无法确认。

**证据**：（无直接证据）

**缺口**：已验证：搜索 EffRadius|effectRadius|ButtonRadius|影响.*半径 无结果；PlaneOperatePanel_Portrait 按钮半径配置逻辑未读取，可能在 Prefab 序列化参数中或在 UpdateSize(radius) 方法中。

---

### 13. 新手引导 > 引导步骤顺序 — pass (88%)

**规则**：新手引导按 Tutorial_text_0 ~ Tutorial_text_14 对应的固定线性步骤顺序执行（FirstBattle→MoveControl→...→CompletedSkillPartAssembly），任意前置步骤未完成时后续步骤不可进入；场景切换后引导进度保持不重置

**边界提示**：进度由 mTutorialProgress 持久化到服务端（AccessManager.SetTutorialProcess）和本地（PlayerPrefs）双重存储；进度比较使用整型大小判断（progress > mTutorialProgress）

**判定**：pass | 置信度 88%

**推理**：TutorialStepEnum 枚举定义了从 FirstBattle 到 CompletedSkillPartAssembly 的15个有序步骤。TutorialManager.FinishAndTutorialReport() 中通过 localProgress<target && progress<target 的双重守卫确保只能前进。TutorialUtil.FinishTutorial() 中 progress>mTutorialProgress 才更新，并通过 EventDispatcher 分发 TUTORIAL_PROGRESS_UPDATE 事件。服务端双持：AccessManager.Instance.SetTutorialProcess() + PlayerPrefs 本地存储，两路同步写入。场景切换后从服务端恢复（TutorialProgressResponse 赋值 mTutorialProgress）。

**证据**：
- `client/Assets/Scripts/ExternalClient/Utils/TutorialUtil.cs` L17-83 `TutorialStepEnum` — 枚举定义 FirstBattle→MoveControl→...→CompletedSkillPartAssembly 固定顺序
- `client/Assets/Scripts/ExternalClient/Utils/TutorialUtil.cs` L330-361 `FinishTutorial` — progress>mTutorialProgress 才更新；SetTutorialProcess+PlayerPrefs 双写
- `client/Assets/Scripts/ExternalClient/Managers/TutorialManager/TutorialManager.cs` L428-459 `FinishAndTutorialReport` — localProgress<target && progress<target 双重守卫再调用 FinishTutorial
- `client/Assets/Scripts/ExternalClient/Managers/AccessManager/NetAccess/Response/TutorialProgressResponse.cs` L30 `OnResponse` — 服务端进度恢复 mTutorialProgress = response.progress

**缺口**：（无）

---

### 14. 新手引导 > 固定组件ID — pass (82%)

**规则**：教程中引用的组件 ID 230068（TUTORIAL_PART_ID_1）、230057（TUTORIAL_PART_ID_2）和替换目标 210010 在教程触发时必须存在于玩家背包/可获取数据中；组件缺失时系统有容错处理路径而非流程卡死

**边界提示**：210010 在教程代码中未硬编码为常量，由 GetBetterSolution 按 Rarity 数据驱动匹配；当前容错逻辑预期为静默跳过教程步骤或回退到战斗步骤

**判定**：pass | 置信度 82%

**推理**：TUTORIAL_PART_ID_1=230068 和 TUTORIAL_PART_ID_2=230057 作为 static int 常量定义。CheckMainPanelCurTutorialIn() 中对两个 ID 分别通过 Find(item => item.PartId == ID) 检查背包存在性：缺失时返回 FirstBattle/SecondBattle（回退到战斗步骤而非崩溃），实现了容错降级路径。PlaneEditorManager.CreatePlanePart 中为教程 ID 的组件启用碰撞体（其余非教程碰撞体在教程期间禁用）。210010 通过 GetBetterSolution 的 Rarity 数据驱动匹配，不硬编码。

**证据**：
- `client/Assets/Scripts/ExternalClient/UI/SubUIPanel/TutorialUI/PlaneUITutorialPanel.cs` L14,22 — TUTORIAL_PART_ID_1=230068; TUTORIAL_PART_ID_2=230057
- `client/Assets/Scripts/ExternalClient/Utils/TutorialUtil.cs` L213-219,232-239 `CheckMainPanelCurTutorialIn` — Find(PartId==ID)==null 时回退到 FirstBattle/SecondBattle
- `client/Assets/Scripts/ExternalClient/Managers/PlaneEditorManager/PlaneEditorManager.cs` L412-422 `CreatePlanePart` — partId==TUTORIAL_PART_ID_1 || partId==TUTORIAL_PART_ID_2 时 collider.enabled=true

**缺口**：容错路径为「回退到战斗步骤」（与 boundaryHint「静默跳过教程步骤」略有差异，但仍属有效容错）。

---

### 15. 新手引导 > 弱引导退出 — uncertain (32%) ⚠️

**规则**：激活组件成功激活武器组件（达到 Tutorial_text_13 状态）后，加一步弱引导退出操作，玩家执行任意退出动作即结束整个新手引导教程态

**边界提示**：弱引导无强制点击目标，玩家任意点击或滑动均可触发退出；退出后教程进度推进到 CompletedSkillPartAssembly

**判定**：uncertain | 置信度 32%

**推理**：弱引导机制（StartSoftBackFromPlaneTutorial）在代码中存在——释放所有碰撞体限制、显示返回按钮点击特效、隐藏 AssistantPanel——这符合「弱引导无强制点击目标」的描述。但该方法只在 StartReplacePartTutorial()（Step 7 ReplacePart，Tutorial_text_7）完成后被调用，而非在 StartCompletedSkillPartAssemblyTutorial()（Tutorial_text_13 状态）之后。契约要求 Tutorial_text_13 之后的弱引导在代码中未实现——CompletedSkillPartAssemblyTutorial 完成后只调用 CheckNextTutorial() 返回 None，无弱引导触发。

**证据**：
- `client/Assets/Scripts/ExternalClient/UI/SubUIPanel/TutorialUI/PlaneUITutorialPanel.cs` L398-415 `StartSoftBackFromPlaneTutorial` — 弱引导：SetAllPartColliderState(true)，显示返回按钮点击特效，隐藏 AssistantPanel
- `client/Assets/Scripts/ExternalClient/UI/SubUIPanel/TutorialUI/PlaneUITutorialPanel.cs` L383-395 `StartReplacePartTutorial` — StartSoftBackFromPlaneTutorial 只在 ReplacePart 步骤触发，非 Tutorial_text_13 后
- `client/Assets/Scripts/ExternalClient/UI/SubUIPanel/TutorialUI/PlaneUITutorialPanel.cs` L576-592 `StartCompletedSkillPartAssemblyTutorial` — FinishAction 中调用 CheckNextTutorial()，返回 None，无弱引导触发

**缺口**：已验证：搜索 CompletedSkillPartAssembly.*Soft|Tutorial_text_13.*Soft|弱引导.*激活 无结果；Tutorial_text_13 后的弱引导退出逻辑代码缺失。

**附带发现**（隐式审查）：
- [已确认] `PlaneUITutorialPanel.cs` `StartCompletedSkillPartAssemblyTutorial` — 第二段教程（Tutorial_text_13后）的弱引导未实现；第一段教程（ReplacePart后）有 StartSoftBackFromPlaneTutorial 但第二段无

---

## 未走查契约

| moduleLabel | rule 摘要 | verifyMethods | 跳过原因 |
|-------------|-----------|---------------|----------|
| 新手引导 > 移动教学 | Step 1 弱引导阶段滑动后 Tutorial_text_1 立即隐藏 | ui_test | 暂不支持 |
| 新手引导 > 语音同步 | 语音文件与 Tutorial_text_N 文本同步播放 | ui_test | 暂不支持 |

---

## ⚠️ 已确认代码缺陷汇总（3 条）

| 编号 | 契约 | 缺陷位置 | 描述 | 严重度 |
|------|------|---------|------|--------|
| D-001 | 激活组件拖拽状态 | `PlaneEditorManager.Operate.cs` L209-213 `OnExitTutorialArea` | 方法为空实现，组件移出教程区域后状态不会回退到状态1（Tutorial_text_14），三状态循环切换机制缺失 | 高（P0 契约） |
| D-002 | 弱引导退出 | `PlaneUITutorialPanel.cs` `StartCompletedSkillPartAssemblyTutorial` | Tutorial_text_13 后无弱引导退出逻辑触发（CheckNextTutorial 返回 None）；第一段教程有 StartSoftBackFromPlaneTutorial，第二段无 | 中（P2 契约） |
| D-003 | 替换动画反馈 | `PlaneEditorManager.cs` `ReplacePlanePart` L670-709 | 直接 DestroyImmediate + 创建，无飞入飞出动画实现；操作屏蔽逻辑同样缺失 | 中（P1 契约） |
