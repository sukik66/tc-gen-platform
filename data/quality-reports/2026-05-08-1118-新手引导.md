# 质量契约走查报告

- 日期：2026-05-08 11:18
- 需求文档：新手引导-一测.docx
- 走查引擎：Cursor Agent

---

## 总览

| 指标 | 数量 |
|------|------|
| 契约总数 | 19 |
| 代码走查 (code_review) | 18 |
| 通过 (pass) | 0（见注¹） |
| 违规 (fail) | 0（见注¹） |
| 存疑 (uncertain) | 18 |
| 跳过 (非 code_review) | 1（新手引导 > 移动教学，ui_test only） |
| 调度模式 | 并行（1 组，18条 Task 派发） |
| ast-grep | 已启用（5文件扫描，methods/calls/events 提取成功） |
| Evidence 验证 | total=73, verified=43, failed=30, downgraded=6（路径格式差异导致磁盘命中失败） |
| 映射缓存(v2) | 18个语义聚类已构建，12条语义链接已添加 |

> **注¹**：verify_evidence.py 因 evidence 文件路径使用相对路径格式（`client/Assets/...`）导致磁盘校验失败，将 6 条强证据结果降级为 uncertain。基于推理置信度分类：**高置信支持 pass（≥72%）4 条**、**发现明确代码违反 4 条**、**真正存疑 10 条**。详见下方「⚡ 走查重点汇总」。

---

## ⚡ 走查重点汇总

### 发现明确代码违反（需确认/修复）

| # | 契约 | 违反点 | 置信度 |
|---|------|--------|--------|
| 1 | 拖拽安装-有效路径 | `EndDragNormalPartTutorial` 调用 `ClosePanel()` 关闭整个教程面板，**文字提示随之消失**，违反「文字提示保留」要求 | 38% |
| 2 | 替换动画反馈 | `ReplacePlanePart` 直接创建新部件，无飞入/飞出 Tween 动画；`OnReplaceClick` 回调链无操作屏蔽机制 | 72% |
| 3 | 替换完成后续流程 | 退出组装界面用 `StartSoftBackFromPlaneTutorial`（弱引导/无蒙版），与契约「强引导退出」描述不符 | 42% |
| 4 | 激活组件拖拽三状态 | 状态(1)代码显示 `Tutorial_text_11`，契约要求 `Tutorial_text_14`；三状态为线性步骤，**无组件移出合法格位后回切到状态(1)的循环逻辑** | 68% |

### 高置信支持通过（evidence 验证路径问题，建议人工确认）

| # | 契约 | 支撑证据摘要 | 置信度 |
|---|------|-------------|--------|
| 1 | 引导蒙版射线拦截 | `HoleMaskImage.IsRaycastLocationValid`：镂空内返回 false（放行），镂空外返回 true（拦截），精确匹配契约 | 80% |
| 2 | 激活影响显示 | `ShowEffGrid(选中)` / `UpdateEffGrid(拖拽中)` / `HideEffGrid(取消选中)` 三方法均在正确时机调用 | 72% |
| 3 | 引导步骤顺序与持久化 | `progress > mTutorialProgress` 防倒退；双存储 `SetTutorialProcess`（服务端）+ `PlayerPrefs`（本地）完整 | 82% |
| 4 | 固定组件ID容错 | 230068/230057 均有 null 检查容错路径（缺失→回退到战斗步骤，不卡死）；210010 非硬编码 | 85% |

### 真正存疑（需进一步探查或 ui_test 验证）

关卡选择（实现用蒙版而非 interactable=false）、武器自动战斗提示（持久文字无自动消失逻辑）、组件tips层级（依赖 Prefab 配置）、拖拽安装-失败回退（手势重置逻辑未找到）、重叠位置提示（旋转/翻转按钮高亮代码未找到）、一键替换逻辑（背包210010高亮未实现）、关卡绑定（单重绑定非双重）、激活组件放置（矩形区域非4格位，动态4-N格缺失）、激活按钮半径配置（mMinRadius可配置但与Effect无显式关联）、弱引导退出（anyClick存在但SetMaskColor(true)属强引导特征）。

---

## 走查详情

### 1. 新手引导 > 关卡选择 — uncertain (40%)

**规则**：新手引导 Step 0 触发时，关卡选择界面默认聚焦第一关，且第一关是唯一可发起战斗的关卡；其他所有关卡按钮不可交互（interactable=false）

**边界提示**：锁定状态不受玩家数据中解锁进度影响，引导阶段强制执行；默认选中第一关指界面打开时选中态停在第一关

**判定**：uncertain | 置信度 40%

**推理**：`MainUITutorialPanel.StartMatchTutorial` 对 FirstBattle 步骤使用 `AssistantPanel.SetMaskColor(true)` 在 BattleButton 上显示 CircleAim 蒙版，功能上阻断其他区域点击。但契约要求「其他所有关卡按钮 interactable=false」——代码中未找到任何对非第一关按钮的 interactable 属性的显式置 false 操作。蒙版方式功能等效（阻止点击），但实现方式与契约描述不完全一致；「界面默认聚焦第一关」逻辑也未找到。

**证据**：
- `client/Assets/Scripts/ExternalClient/UI/SubUIPanel/TutorialUI/MainUITutorialPanel.cs` L74-123 `StartMatchTutorial` — SetMaskColor(true) 激活全屏蒙版并在 BattleButton 处打 CircleAim 镂空，功能上阻止其他按钮点击，但未设置 interactable=false

**缺口**：【已验证】搜索 `interactable.*tutorial|FirstBattle.*lock|forced.*interactable` 均无结果；未找到对非第一关卡按钮显式 interactable=false 的代码。蒙版方式能否完全等效于 interactable=false 需运行时验证。

---

### 2. 新手引导 > 武器自动战斗提示 — uncertain (42%)

**规则**：Step 1 弱引导阶段，「武器会自动寻敌射击，无需操作」提示在玩家进入该步骤时显示，该提示不依赖玩家手动操作即可自动消失或随步骤推进而隐藏

**边界提示**：该提示与滑动移动提示属同一 Step 1 弱引导阶段内的两段独立提示；需求文档未明确自动消失的触发时机（计时器或事件驱动）

**判定**：uncertain | 置信度 42%

**推理**：`MoveTutorialPanel.cs L23` 确认 Tutorial_text_1 显示在 mContentText 中；`L37-48 ShowTipUI` 对浮动提示（Tutorial_text_14，持续3秒）使用 DOFade 自动淡出。但 Tutorial_text_1 作为持久底部文字，代码中仅在 StartMoveTutorial 中设置，未找到其自动消失的计时器或事件驱动逻辑。

**证据**：
- `client/Assets/Scripts/Client/BattleUI/TutorialPanel/MoveTutorialPanel.cs` L20-48 `StartMoveTutorial` — Tutorial_text_1 设置到 mContentText（持久显示），Tutorial_text_14 通过 ShowTipUI 以3秒定时自动淡出（DOFade），但 Tutorial_text_1 本身无自动消失逻辑

**缺口**：【已验证】搜索 `Tutorial_text_1.*timer|Tutorial_text_1.*DOFade|mContentText.*fade` 无结果；Tutorial_text_1 持久文字的自动消失机制代码缺失。

---

### 3. 新手引导 > 引导蒙版射线拦截 — uncertain (80%) ⚡ 强证据支持 pass

**规则**：强引导进行中，非当前步骤高亮区域的 UI 被半透明蒙版覆盖且射线检测被拦截（IsRaycastLocationValid 返回 false），仅高亮镂空区域内的 UI 可响应点击

**边界提示**：步骤切换时蒙版镂空区域与高亮目标同步更新，不得出现全屏遮挡的中间帧；高亮区域由 GuideCutoutImage 的 targetArea 定义

**判定**：uncertain（路径验证失败降级）| 置信度 80% | **建议人工确认为 pass**

**推理**：`HoleMaskImage.IsRaycastLocationValid` 精确实现了契约：`hasHole=false` 时返回 true（全屏拦截）；`hasHole=true` 且 `holeRect.Contains(localPoint)` 时返回 false（镂空区域放行）；否则返回 true（遮罩区域拦截）。`TutorialMask.SetHoleAtPosition` 在步骤切换时更新镂空位置，关闭时调用 `OnTutorialCloseHoleMask` 禁用蒙版。契约核心要求已代码实现。

**证据**：
- `client/Assets/Scripts/ExternalClient/UI/HoleMaskImage.cs` L16-34 `IsRaycastLocationValid` — 镂空区域（holeRect.Contains）返回 false 放行，其余区域返回 true 拦截，实现精确射线控制
- `client/Assets/Scripts/ExternalClient/Utils/TutorialMask.cs` L44-73 `SetHoleAtPosition` — 步骤切换时调用，更新镂空区域位置并激活蒙版 GameObject

**缺口**：证据路径磁盘验证失败（相对路径格式问题）。

**附带发现**：
- [已确认] `PlaneEditUI_Portrait.Tutorial.cs` L131 `ClosePanel` 时禁用蒙版，避免全屏遮挡中间帧

---

### 4. 新手引导 > 组件tips层级 — uncertain (45%)

**规则**：Step 4（组件230068）和 Step 11（组件230057）点击后弹出的 tips 面板渲染层级高于引导蒙版，tips 内容完整可见不被蒙版遮挡

**边界提示**：原文：「点击后，组件tips要在高亮显示，不要在蒙版下面」；适用于两个教学组件 ID 230068 和 230057

**判定**：uncertain | 置信度 45%

**推理**：`PlaneUITutorialPanel.StartClickAndCheckPartAttrTutorial` 在按钮点击时先调用 `ClosePanel()`（禁用蒙版），再调用 `onClick?.Invoke()` 触发 tips 显示——蒙版关闭后 tips 渲染，理论上不被遮挡。但 `CheckNextTutorial()` 随后会重新打开新的 PlaneUITutorialPanel，其 `SetAsFirstSibling()` 将教程面板置为最先渲染，tips 是否仍在前面取决于 Prefab 层级。`PlanePartTipsPanel_Portrait.cs` 中未找到主动设置 sortingOrder 或 SetAsLastSibling 的代码。

**证据**：
- `client/Assets/Scripts/ExternalClient/UI/SubUIPanel/TutorialUI/PlaneUITutorialPanel.cs` L270-295 `StartClickAndCheckPartAttrTutorial` — 点击时先 ClosePanel()（禁用蒙版），再调用 onClick?.Invoke() 触发 tips；确保 tips 打开时蒙版已关闭
- `client/Assets/Scripts/ExternalClient/UI/UIPanel/PlaneEditUI_Portrait.Tutorial.cs` L47-50 `OpenPlaneUITutorial` — PlaneUITutorialPanel 通过 SetAsFirstSibling() 设置为第一个子节点（最底层渲染）

**缺口**：【已验证】搜索 `PlanePartTipsPanel.*sortingOrder|SetAsLastSibling` 无结果；tips 层级高于蒙版的保证依赖 Prefab 配置，无法从 C# 代码层面确认。

---

### 5. 新手引导 > 拖拽安装-有效路径 — uncertain (38%) ⚠️ 发现代码违反

**规则**：Step 5/6 拖拽教学中，玩家将组件成功拖至飞船组装区域黄色框有效位置并释放后：手势提示消失，文字提示保留，组件 tips 关闭，底版黄色框高亮持续直至组件落位确认

**边界提示**：黄色框位置由 tutorialValidArea 定义；「有效位置」为区域内非重叠格位；落位确认后底版高亮解除

**判定**：uncertain | 置信度 38% | **⚠️ 发现「文字提示保留」违反**

**推理**：`OnEnterTutorialArea` 在拖入有效区域时先派发 `TUTORIAL_IS_COMPLETED`，触发 `EndDragNormalPartTutorial`：
- 手势消失（mMoveEffectGameObject 禁用）✓
- 底版黄色框高亮（SetMaskArea 重激活 TutorialMask）✓  
- 组件 tips 关闭（OnDrag 中 CloseTips() 已提前关闭）✓
- **「文字提示保留」× ← ClosePanel() 会禁用整个 PlaneUITutorialPanel GameObject，文字提示随之消失**

**证据**：
- `client/Assets/Scripts/ExternalClient/Managers/PlaneEditorManager/PlaneEditorManager.Operate.cs` L193-210 `OnEnterTutorialArea` — 进入 tutorialValidArea 后触发 TUTORIAL_IS_COMPLETED，并调用 SetMaskArea 重激活底版高亮
- `client/Assets/Scripts/ExternalClient/UI/SubUIPanel/TutorialUI/PlaneUITutorialPanel.cs` L226-244 `EndDragNormalPartTutorial` — **调用 ClosePanel()，整个教程面板包括文字提示均被禁用，违反「文字提示保留」要求**

**缺口**：无（违反点已明确）。

**附带发现**：
- [已确认] `PlaneEditorManager.Operate.cs` OnDrag L524 调用 `CloseTips()`，确保拖拽过程中 tips 关闭

---

### 6. 新手引导 > 拖拽安装-失败回退 — uncertain (40%)

**规则**：Step 5/6 拖拽教学中，玩家仅点击组件未进行拖拽，或拖拽后未释放到飞船有效区域内，组件回退到背包原位且手势引导重新触发

**边界提示**：「回退原位」指组件在背包中的排列顺序与操作前一致；手势引导重新出现时重置到初始拖拽起始状态

**判定**：uncertain | 置信度 40%

**推理**：`PlaneEditUI_Portrait.HandleTouchUp` 在无拖拽（mDragCloneGO==null）时清理 DragItem；有拖拽但未进入有效区域时 `DestroyImmediate` 克隆并恢复 `DragItem.CanvasGroup.alpha=1`，组件视觉上回到背包原位（✓）。手势引导（mMoveEffectGameObject）在 `StartDragNormalPartTutorial` 中持续显示，未被关闭，继续可见（✓）。但「手势引导重新触发」要求重置到拖拽起始状态，代码中手势仅维持当前状态，未见重新播放/重置的逻辑。

**证据**：
- `client/Assets/Scripts/ExternalClient/UI/UIPanel/PlaneEditUI_Portrait.cs` L224-290 `HandleTouchUp` — 无效拖拽时 DestroyImmediate 克隆、恢复 DragItem.CanvasGroup.alpha=1，组件回到背包视觉位置

**缺口**：【已验证】搜索 `ResetTutorialGesture|RestartTutorial|mMoveEffect.*reset|replay.*gesture` 无结果；手势「重新触发/重置到初始状态」逻辑未在代码中找到。

---

### 7. 新手引导 > 重叠位置提示 — uncertain (45%)

**规则**：玩家首次将组件拖至与已安装组件重叠位置时，旋转和翻转按钮高亮并显示 Tutorial_text_6（含「点击旋转或翻转按钮调整方向」语义），提示持续至组件被正确安装到非重叠有效位置

**边界提示**：同一教程周期内该提示仅显示一次（首次触发后设置已触发标志，后续重叠不再重复弹出）

**判定**：uncertain | 置信度 45%

**推理**：`PlaneEditorManager.Data.cs L392` 在 CompletePartAssembly 步骤检测 hasInvalid（重叠）时调用 `ShowTutorialTips(CompletePartAssembly, hasInvalid)`，触发 `PlaneUITutorialPanel.StartCompletePartAssemblyTutorial(isTips=true)`，显示 Tutorial_text_6（✓），通过 `mIsShowedTips` 标志确保同周期内仅触发一次（✓）。但**「旋转和翻转按钮高亮」**：代码使用 `AssistantPanelData(CircleAim_UI)` 打在组件上，而非旋转/翻转按钮，专门高亮 RotateButton/FlipButton 的代码未找到。

**证据**：
- `client/Assets/Scripts/ExternalClient/Managers/PlaneEditorManager/PlaneEditorManager.Data.cs` L390-395 `CheckValidInstall` — CompletePartAssembly 步骤检测 hasInvalid 时触发 ShowTutorialTips
- `client/Assets/Scripts/ExternalClient/UI/SubUIPanel/TutorialUI/PlaneUITutorialPanel.cs` L290-315 `StartCompletePartAssemblyTutorial` — mIsShowedTips 标志防止重复触发（✓），但 CircleAim 圆形指示打在组件上，旋转/翻转按钮未见专门高亮

**缺口**：【已验证】搜索 `RotateButton.*highlight|FlipButton.*tutorial|Tutorial_text_6.*rotate` 无结果；旋转/翻转按钮的专属高亮代码未找到。

---

### 8. 新手引导 > 一键替换逻辑 — uncertain (50%)

**规则**：Step 7 点击「一键替换更强组件」时，系统通过 GetBetterSolution 从背包中选取 Rarity 更高的同 PartId 组件替换到飞船对应槽位，被替换组件移回背包；教程中同时高亮背包中的 210010 组件及飞船上即将被替换的组件

**边界提示**：多个候选时仅替换 GetBetterSolution 返回的第一个匹配项；替换操作为原子操作不可中断；210010 在代码中不作为硬编码常量，由 GetBetterSolution 按 Rarity 数据驱动匹配

**判定**：uncertain | 置信度 50%

**推理**：`OnReplaceClick` 调用 `GetBetterSolution()` 并逐个执行 `ReplacePlanePart`（✓）。`GetBetterSolution` 按 Rarity 数据驱动匹配，非硬编码 210010（✓）。但「教程中同时高亮背包中的 210010 组件及飞船上即将被替换的组件」：`StartReplacePartTutorial` 中 CircleAim 仅打在「一键替换」按钮上，**背包 210010 组件同时高亮的代码未找到**。

**证据**：
- `client/Assets/Scripts/ExternalClient/UI/SubUIPanel/PlaneEditUI/PlaneEditPanel_Portrait.cs` L506-525 `OnReplaceClick` — GetBetterSolution 数据驱动匹配，逐个调用 ReplacePlanePart 并传入 PlaneTutorialStep，替换逻辑正确
- `client/Assets/Scripts/ExternalClient/UI/SubUIPanel/TutorialUI/PlaneUITutorialPanel.cs` L347-396 `StartReplacePartTutorial` — CircleAim 打在 PlaneEditReplacePart 按钮上，未见背包 210010 组件同时高亮

**缺口**：【已验证】搜索 `backpack.*highlight.*replace|inventory.*tutorial.*highlight|210010.*tutorial` 无结果；背包 210010 组件的高亮逻辑未在代码中找到。

---

### 9. 新手引导 > 替换动画反馈 — uncertain (72%) ⚠️ 发现代码违反

**规则**：点击一键替换后：(A) 被替换组件在飞出前显示明显特效标识；(B) 新组件从背包飞入飞船槽位、被替换组件从飞船飞出回背包的过渡动画完整播放；(C) 动画播放期间用户操作被屏蔽直至动画结束

**边界提示**：动画结束后 UI 状态与数据一致（飞船显示新组件，背包显示旧组件），无中间不一致帧

**判定**：uncertain | 置信度 72% | **⚠️ 发现 (B)(C) 两项代码违反**

**推理**：
- (A) `ReplacePlanePart` L688-709：`step != None && step != CompletedSkillPartAssembly` 时调用 `AssetsLoad_Effect` 播放特效 ✓
- (B) **飞入/飞出动画**：`CreateReplacePlanePart` 直接在槽位创建新部件，未发现任何 Tween/Animation/DoTween 飞行动画逻辑 ✗
- (C) **操作屏蔽**：`OnReplaceClick` 回调链中无 `blocksRaycasts=true` 或临时 `interactable=false` 屏蔽机制，替换完成后立即调用 `OnBtnSaveClick`，无等待动画结束的 await/yield ✗

**证据**：
- `client/Assets/Scripts/ExternalClient/Managers/PlaneEditorManager/PlaneEditorManager.cs` L670-709 `ReplacePlanePart` — AssetsLoad_Effect 特效（A ✓），但 CreateReplacePlanePart 直接替换无飞行动画，无操作屏蔽（B/C ✗）

**缺口**：证据路径磁盘验证失败。

**附带发现**：
- [已确认] `OnReplaceClick` 中 `replaceCount==betterSolution.Count` 时调用 `OnBtnSaveClick`，无操作屏蔽间隔，用户可在替换中途再次操作

---

### 10. 新手引导 > 替换完成后续流程 — uncertain (42%) ⚠️ 发现代码违反

**规则**：点击一键替换完成后，系统强引导玩家退出组装界面；退出后立即强引导玩家进入第二关战斗（Tutorial_text_8：「改装完毕，前往下一关！」），此两步为顺序强引导不可跳过

**边界提示**：退出→第二关为顺序强引导，第二关必须在第一关已通关且替换操作完成后才可触发

**判定**：uncertain | 置信度 42% | **⚠️ 第一步为弱引导非强引导**

**推理**：`PlaneUITutorialPanel.StartReplacePartTutorial` 完成后调用 `StartSoftBackFromPlaneTutorial()`，该方法仅显示返回按钮点击特效（`mClickEffectGameObject`），属于**弱引导（无蒙版遮挡）**；契约要求「强引导退出」。返回主界面后，`MainUITutorialPanel.StartMatchTutorial(SecondBattle)` 显示 Tutorial_text_8 并使用 `SetMaskColor(true)` 强引导（✓）。退出→第二关顺序正确，但第一步实现与契约描述不符。

**证据**：
- `client/Assets/Scripts/ExternalClient/UI/SubUIPanel/TutorialUI/PlaneUITutorialPanel.cs` L398-415 `StartSoftBackFromPlaneTutorial` — **弱引导**：显示返回按钮点击特效，关闭文字面板，无蒙版
- `client/Assets/Scripts/ExternalClient/UI/SubUIPanel/TutorialUI/MainUITutorialPanel.cs` L74-123 `StartMatchTutorial` — SecondBattle 步骤显示 Tutorial_text_8，SetMaskColor(true) 强引导进入第二关（✓）

**附带发现**：
- [已确认] `StartSoftBackFromPlaneTutorial` 使用弱引导（无蒙版），与契约「强引导退出」描述不符

---

### 11. 新手引导 > 关卡绑定 — uncertain (52%)

**规则**：新手引导两个教学关卡通过 PreRequisiteLevel 与教程步骤进度双重绑定：第一关未通关前第二关按钮保持锁定禁用；教学关卡使用标准战斗接口（ChallengeBattleStage）发起，掉落机制与普通关卡一致

**边界提示**：第二关解锁同时依赖教程步骤进度（必须完成组装引导）；教学关卡战斗失败的处理需求文档未明确

**判定**：uncertain | 置信度 52%

**推理**：`StarMapPoint.Init L51` 通过 `BattleLevelConfig.PreRequisiteLevel` 判断第二关锁定状态（✓）；`ChapterPanel_MainPanel.StartGame` 使用 `ChallengeBattleStage` 发起战斗（✓）；掉落机制与普通关卡一致（✓）。但契约要求「双重绑定（PreRequisiteLevel + 教程步骤进度）」——代码中解锁仅依赖 `PreRequisiteLevel` 配置，未发现额外的教程步骤进度双重绑定逻辑，当前为单重绑定。

**证据**：
- `client/Assets/Scripts/ExternalClient/StarMap/StarMapPoint.cs` L39-76 `Init` — mIsLocked = !CheckBattleStageFinish(PreRequisiteLevel)，第一关通关后第二关解锁（✓）
- `client/Assets/Scripts/ExternalClient/UI/SubUIPanel/MainUI/ChapterPanel_MainPanel.cs` L238-290 `StartGame` — ChallengeBattleStage 标准接口发起战斗（✓）

**缺口**：【已验证】搜索 `tutorial.*level.*lock|level.*tutorial.*progress|双重绑定` 无结果；教程步骤进度与关卡解锁的双重绑定机制代码未找到，当前仅 PreRequisiteLevel 单重绑定。

---

### 12. 新手引导 > 激活组件放置 — uncertain (32%)

**规则**：Step 11/12 激活类组件（ID=230057）拖拽阶段仅可放置于飞船组装区域预设的 4 个特定合法格位；若上一步普通组件已占用其中 N 格，则界面仅高亮显示剩余 4-N 个可用合法格位

**边界提示**：4 个合法格位坐标由 tutorialValidArea 或配置数据定义；4 格全被占满时任何放置位置均视为非法释放

**判定**：uncertain | 置信度 32%

**推理**：`PlaneEditorManager.Operate.cs CheckInValidArea` 和 `OnDrag` 对 `DragSkillPart/FindValidPosition` 步骤使用单个 `tutorialValidArea`（RectTransform）做矩形区域限制。契约要求「仅可放置于4个特定合法格位」且「若上一步占用N格则仅高亮4-N个」——`tutorialValidArea` 是一个整体矩形区域，不是4个独立格位；也未找到基于已占用格子数量动态缩小有效区域的代码。格子级合法性通过 `CheckValidInstall` 的重叠检测实现，但**高亮「剩余4-N格」的动态更新逻辑缺失**。

**证据**：
- `client/Assets/Scripts/ExternalClient/Managers/PlaneEditorManager/PlaneEditorManager.Operate.cs` L113-171 `CheckInValidArea` — DragSkillPart/FindValidPosition 步骤用单个 tutorialValidArea 矩形做限制，非4独立格位

**缺口**：【已验证】搜索 `skillValidSlots|4.*validGrid|remainingSlots|validSlot.*skill` 无结果；4个独立合法格位的定义及「剩余4-N格高亮」动态更新代码未找到。

---

### 13. 新手引导 > 激活组件拖拽三状态 — uncertain (68%) ⚠️ 发现代码违反

**规则**：拖拽激活组件过程中系统实时检测组件位置，在三种状态间循环切换：(1) 组件未在合法格位 → 显示 Tutorial_text_14；(2) 在合法格位但未激活 → 显示 Tutorial_text_12；(3) 成功激活 → 显示 Tutorial_text_13

**边界提示**：三状态为循环检测，非单向线性；组件从合法格位移出后状态回切到第(1)种

**判定**：uncertain | 置信度 68% | **⚠️ 发现两处代码违反**

**推理**：
- 状态(2) Tutorial_text_12 ✓、状态(3) Tutorial_text_13 ✓
- **状态(1) 严重偏差**：`StartDragSkillPartTutorial` 显示 `Tutorial_text_11`，而非契约要求的 `Tutorial_text_14`；`Tutorial_text_14` 仅在战斗场景 `MoveTutorialPanel.cs` 中出现
- **无循环回退逻辑**：三状态为线性步骤（DragSkillPart → FindValidPosition → CompletedSkillPartAssembly），组件移出合法格位后**无回切到状态(1)的逻辑**

**证据**：
- `client/Assets/Scripts/ExternalClient/UI/SubUIPanel/TutorialUI/PlaneUITutorialPanel.cs` L420-460 `StartDragSkillPartTutorial` — 状态(1)显示 Tutorial_text_11 而非 Tutorial_text_14，与契约不符
- `client/Assets/Scripts/Client/BattleUI/TutorialPanel/MoveTutorialPanel.cs` L37-48 `ShowTipUI` — Tutorial_text_14 仅在战斗场景出现，非组装界面

**附带发现**：
- [已确认] 三状态为线性步骤（DragSkillPart→FindValidPosition→CompletedSkillPartAssembly），无循环回退到状态(1)的逻辑

---

### 14. 新手引导 > 激活影响显示 — uncertain (72%) ⚡ 强证据支持 pass

**规则**：选中带有激活效果的组件时，其 Effect 影响范围内的格位常驻显示高亮标记（ValidSprite/AreaSprite），被影响位置的已安装组件增加被激活特效；取消选中后影响范围高亮和特效立即消失

**边界提示**：拖拽过程中影响范围随组件位置实时更新（UpdateEffGrid）

**判定**：uncertain（路径验证失败降级）| 置信度 72% | **建议人工确认为 pass**

**推理**：`CPlanePartInfo` 拥有 `EffGridList`（List\<PlaneEditHitGrid\>）并提供三方法：
- `ShowEffGrid()` 在 `PlaneOperatePanel_Portrait.Init() L80` 调用（选中时显示 ✓）
- `HideEffGrid()` 在 `PlaneEditorManager.Operate.cs L268` 调用（取消选中时隐藏 ✓）
- `UpdateEffGrid()` 在 `OnDrag L517` 调用（拖拽中实时更新 ✓）
三方法均在正确时机调用，与契约一致。

**证据**：
- `client/Assets/Scripts/ExternalClient/UI/SubUIPanel/PlaneEditUI/PlaneOperatePanel_Portrait.cs` L80 `Init` — 选中时调用 mPartInfo.ShowEffGrid() 显示 Effect 影响范围
- `client/Assets/Scripts/ExternalClient/Managers/PlaneEditorManager/PlaneEditorManager.Operate.cs` L268 `OnClickPlanePart` — 取消选中时调用 lastActivePlanePart.HideEffGrid() 立即隐藏
- `client/Assets/Scripts/ExternalClient/Managers/PlaneEditorManager/PlaneEditorManager.Operate.cs` L517 `OnDrag` — 拖拽中调用 ActivePlanePart.UpdateEffGrid() 实时更新影响范围

**缺口**：证据路径磁盘验证失败（相对路径格式问题）。

---

### 15. 新手引导 > 激活按钮半径配置 — uncertain (48%)

**规则**：激活类组件的旋转/翻转按钮的点击半径配置需覆盖组件 Effect 影响位置，或为按钮半径单独提供可配置参数

**边界提示**：原文：「4个按钮的半径，需要考虑到组件的影响位置（Effect）位置，或者单独配置」

**判定**：uncertain | 置信度 48%

**推理**：`PlaneOperatePanel_Portrait.cs L37` 声明 `[SerializeField] private float mMinRadius`，提供可在 Inspector 中配置的最小半径参数（✓），满足「或为按钮半径单独提供可配置参数」的要求。但契约的核心要求「覆盖 Effect 影响位置」——代码中 `UpdateSize` 基于组件本身形状计算，Effect 影响格位可能超出组件形状边界，是否被覆盖取决于 `mMinRadius` 的实际配置值，无代码层面强制保证。

**证据**：
- `client/Assets/Scripts/ExternalClient/UI/SubUIPanel/PlaneEditUI/PlaneOperatePanel_Portrait.cs` L36-38 class declaration — [SerializeField] private float mMinRadius 提供可在 Inspector 中配置的最小半径，满足「可配置参数」要求

**缺口**：【已验证】搜索 `effGrid.*radius|radius.*effect|effect.*button.*size` 无结果；Effect 影响位置与按钮半径的显式关联计算代码未找到。

---

### 16. 新手引导 > 引导步骤顺序与持久化 — uncertain (82%) ⚡ 强证据支持 pass

**规则**：新手引导按固定线性步骤顺序执行，任意前置步骤未完成时后续步骤不可进入；场景切换（战斗↔主界面↔组装界面）后引导进度不重置

**边界提示**：进度通过 mTutorialProgress 持久化到服务端（AccessManager.SetTutorialProcess）和本地（PlayerPrefs）双重存储；进度比较使用整型大小判断（progress > mTutorialProgress）

**判定**：uncertain（路径验证失败降级）| 置信度 82% | **建议人工确认为 pass**

**推理**：`TutorialUtil.FinishTutorial L324-361`：
- 进度比较使用 `progress > mTutorialProgress`（防倒退 ✓）
- 双存储：`AccessManager.Instance.SetTutorialProcess(progress, true)`（服务端 ✓）+ `PlayerPrefs.SetInt(GetTutorialTotalProgressPlayerPrefabDataKey(), progress)`（本地 ✓）
- `TutorialManager.FinishAndTutorialReport` 调用 `TutorialUtil.GetLocalTutorialProgress()` 和 `FinishTutorial(step)`，场景切换时 `CheckCurrentTutorial/CheckPlaneEditCurTutorialIn` 读取持久化进度恢复状态（✓）

**证据**：
- `client/Assets/Scripts/ExternalClient/Utils/TutorialUtil.cs` L324-361 `FinishTutorial` — progress > mTutorialProgress 防止倒退；双存储 SetTutorialProcess + PlayerPrefs 跨场景持久化
- `client/Assets/Scripts/ExternalClient/Managers/TutorialManager/TutorialManager.cs` L428-460 `FinishAndTutorialReport` — GetLocalTutorialProgress + FinishTutorial 调用链

**缺口**：证据路径磁盘验证失败（相对路径格式问题）。

---

### 17. 新手引导 > 固定组件ID容错 — uncertain (85%) ⚡ 强证据支持 pass

**规则**：教程中硬编码引用的组件 ID 230068（TUTORIAL_PART_ID_1）和 230057（TUTORIAL_PART_ID_2）在教程触发时必须存在于玩家背包/可获取数据中；组件缺失时存在容错处理路径而非流程卡死

**边界提示**：210010（高速机枪）由 GetBetterSolution 按 Rarity 数据驱动匹配，未硬编码为常量

**判定**：uncertain（路径验证失败降级）| 置信度 85% | **建议人工确认为 pass**

**推理**：`TutorialUtil.CheckMainPanelCurTutorialIn`：
- L213-219：查找 TUTORIAL_PART_ID_1（230068），不存在时 `return TutorialStepEnum.FirstBattle`（容错回退，不卡死 ✓）
- L232-239：查找 TUTORIAL_PART_ID_2（230057），不存在时 `return TutorialStepEnum.SecondBattle`（容错 ✓）
- 两处均为 null 检查后回退，不会引发 NullReferenceException；210010 由 `GetBetterSolution` 数据驱动，非硬编码（✓）

**证据**：
- `client/Assets/Scripts/ExternalClient/Utils/TutorialUtil.cs` L213-239 `CheckMainPanelCurTutorialIn` — TUTORIAL_PART_ID_1 缺失→return FirstBattle；TUTORIAL_PART_ID_2 缺失→return SecondBattle；两处均有 null 检查容错路径
- `client/Assets/Scripts/ExternalClient/UI/SubUIPanel/TutorialUI/PlaneUITutorialPanel.cs` L14,22 class declaration — TUTORIAL_PART_ID_1=230068，TUTORIAL_PART_ID_2=230057，常量定义确认

**缺口**：证据路径磁盘验证失败（相对路径格式问题）。

---

### 18. 新手引导 > 弱引导退出 — uncertain (45%)

**规则**：激活类组件成功激活武器组件（达到 Tutorial_text_13 状态）后，触发一步弱引导退出操作；玩家执行任意退出动作（无强制指定交互目标）即结束整个新手引导教程态，教程进度推进到最终完成状态（CompletedSkillPartAssembly）

**边界提示**：弱引导无强制点击目标，玩家任意点击或滑动均可触发退出

**判定**：uncertain | 置信度 45%

**推理**：`PlaneUITutorialPanel.StartCompletedSkillPartAssemblyTutorial L565-591`：`FinishAction` 同时作为 `click` 和 `anyClick` 回调传入（理论上任意点击触发 ✓）；调用 `FinishAndTutorialReport(FindValidPosition)` + `FinishAndTutorialReport(CompletedSkillPartAssembly)` 推进进度（✓）。但 **`SetMaskColor(true)` 被调用**——激活了完整蒙版，属于强引导行为特征；契约要求「弱引导无强制点击目标」，当前实现带蒙版（强引导特征），`anyClick` 参数是否真正放行任意屏幕点击取决于 AssistantPanel 内部实现细节。

**证据**：
- `client/Assets/Scripts/ExternalClient/UI/SubUIPanel/TutorialUI/PlaneUITutorialPanel.cs` L565-591 `StartCompletedSkillPartAssemblyTutorial` — FinishAction 作为 anyClick 传入，但 SetMaskColor(true) 激活蒙版（强引导），与契约「弱引导」定义有偏差

**缺口**：【已验证】搜索 `SetMaskColor.*false.*completed|weak.*tutorial.*exit|CompletedSkillPart.*soft` 无结果；弱引导（无蒙版）退出的代码未找到，当前实现为带蒙版的强引导触发 anyClick。

---

## 未走查契约

| moduleLabel | rule | verifyMethods | 跳过原因 |
|-------------|------|---------------|----------|
| 新手引导 > 移动教学 | Step 1 弱引导阶段，玩家执行滑动屏幕操作后 Tutorial_text_1 提示立即隐藏... | ui_test | 暂不支持，仅含 ui_test |
