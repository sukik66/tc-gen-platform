# 质量契约走查报告

- 日期：2026-05-09 20:10
- 需求文档：新手引导-一测.docx
- 走查引擎：Cursor Agent（refreshContracts=true 重新提取 + refreshSearch=true 全量重搜）

## 总览

| 指标 | 数量 |
|------|------|
| 契约总数 | 19 |
| 代码走查 (code_review) | 18 |
| 通过 (pass) | 7 |
| 违规 (fail) | 4 |
| 存疑 (uncertain) | 7 |
| 跳过 (非 code_review) | 1 |
| 调度模式 | 并行 3 组（A基础设施 / B组装拖拽 / C替换激活）|
| ast-grep | 已启用（20文件, 11调用, 25 custom_rules 命中）|

### 结论分布

```
pass:      ███████░░░░░░░░░░░ 7/18 (39%)
uncertain: ███████░░░░░░░░░░░ 7/18 (39%)
fail:      ████░░░░░░░░░░░░░░ 4/18 (22%)
```

### 高风险项速览（P0 fail / uncertain）

| 契约 | 优先级 | 结论 | 核心问题 |
|------|--------|------|---------|
| #7 拖拽安装-失败回退 | P0 | **fail** | 回退+手势重播逻辑完全不存在，教程用 Clamp 绕过 |
| #9 一键替换逻辑 | P0 | **fail** | 替换逻辑完整但 SR-2 高亮显示全库 0 命中 |
| #14 激活拖拽三状态 | P0 | **fail** | `OnExitTutorialArea` 空方法体，状态回切不可能 |
| #18 固定组件ID容错 | P0 | **uncertain** | TutorialUtil 有 null 检查但下游步骤方法缺 null 保护 |
| #13 激活组件放置 | P0 | **uncertain** | tutorialValidArea 存在但无显式 4 格位约束 |

---

## 走查详情

### 1. 新手引导 > 关卡选择 — pass (88%)

**规则**：新手引导 Step 0 触发时，关卡选择界面进入强引导态：全屏蒙版激活（SetMaskColor(true)），BattleButton 位置设有圆形镂空高亮（CircleAim），非镂空区域点击被拦截；第一关是唯一可发起战斗的关卡
**边界提示**：实现方式为蒙版拦截而非 interactable=false；「默认选中第一关」指界面打开时战斗目标为第一关
**判定**：pass | 置信度 88%

**子要求判定**：
| SR | 描述 | 类型 | 判定 | 置信度 | 关键证据 |
|----|------|------|------|--------|---------|
| SR-1 | StartMatchTutorial 对 FirstBattle 调用 SetMaskColor(true) 并设 CircleAim | grep_existence | pass | 90% | `MainUITutorialPanel.cs` L74-123 `StartMatchTutorial()` |
| SR-2 | 强引导态下仅第一关可发起战斗 | llm_semantic | pass | 85% | `ChapterPanel_MainPanel.cs` L508-513 + `StarMapPoint.cs` L51-57 |

**推理**：`StartMatchTutorial` 在 FirstBattle 分支获取 BattleButton 位置 → 构造 `CircleAim_UI(param2=300)` → `ShowAssistantPanel` → `SetMaskColor(true)` 激活全屏蒙版。`AssistantPanel.ShowAssistantPanel` 对 CircleAim_UI 类型调用 `SetCircleGuide_UI` 设置圆形镂空。关卡锁定通过 `StarMapPoint.PreRequisiteLevel` + `ChapterPanel_MainPanel.SelectLevel(isLocked)` → `SetCanvasGroupActive(!isLocked)` 控制。
**证据**：
- `MainUITutorialPanel.cs` L74-123 `StartMatchTutorial` — CircleAim_UI+SetMaskColor(true)+BattleButton 目标
- `AssistantPanel.cs` L109-149 `ShowAssistantPanel` — CircleAim_UI 调用 SetCircleGuide_UI
- `AssistantPanel.cs` L244-247 `SetMaskColor` — SetMaskColor(true) 设蒙版遮挡态
- `ChapterPanel_MainPanel.cs` L508-513 `SelectLevel` — isLocked 时禁用 BattleButton
**缺口**：无
**附带发现**：AssistantPanel.cs L325-327 通过 CircleGuide.IsRaycastLocationValid 判定点击是否在镂空内，实现镂空外拦截

---

### 2. 新手引导 > 移动教学 — 跳过

**验证方式**：ui_test（需实机验证移动操作反馈），不在 code_review 范围内

---

### 3. 新手引导 > 武器自动战斗提示 — pass (82%)

**规则**：Step 1 弱引导阶段，Tutorial_text_1 在进入步骤时设置到持久文字区域（mContentText）显示；该文字提示存在隐藏触发机制（事件回调或步骤推进），不永久残留到教程结束
**判定**：pass | 置信度 82%

**子要求判定**：
| SR | 描述 | 类型 | 判定 | 置信度 | 关键证据 |
|----|------|------|------|--------|---------|
| SR-1 | Tutorial_text_1 启动时设置到 mContentText | grep_existence | pass | 85% | `MoveTutorialPanel.cs` L23 `Start()` |
| SR-2 | 存在隐藏/清除 Tutorial_text_1 文字提示的逻辑 | llm_semantic | pass | 78% | `GameUI.cs` L467-471 `Destroy(mMoveTutorialGameObject)` |

**推理**：`MoveTutorialPanel.Start()` 在实例化时将 `"Tutorial_text_1".GetLocaliztion()` 设置到 `mContentText`。隐藏通过 `GameUI.CheckMoveOperationTutorialUI(false)` → `Destroy(mMoveTutorialGameObject)` 实现——面板销毁 = 文字消除。
**证据**：
- `MoveTutorialPanel.cs` L23 `Start` — mContentText.text = "Tutorial_text_1".GetLocaliztion()
- `GameUI.cs` L467-471 `CheckMoveOperationTutorialUI` — isOpen=false 时 Destroy 面板
**缺口**：方法名为 Unity `Start()` 而非 `StartMoveTutorial`，但功能等价
**附带发现**：L24 `ShowTipUI("Tutorial_text_14", 3f)` 显示额外气泡提示（3秒自动淡出），与持久文字 Tutorial_text_1 共存

---

### 4. 新手引导 > 引导蒙版射线拦截 — pass (92%)

**规则**：HoleMaskImage.IsRaycastLocationValid 实现「镂空内放行（返回 false）、镂空外拦截（返回 true）」；步骤切换时 SetHoleAtPosition 同步更新镂空区域位置；蒙版关闭时通过 OnTutorialCloseHoleMask 禁用蒙版
**判定**：pass | 置信度 92%

**子要求判定**：
| SR | 描述 | 类型 | 判定 | 置信度 | 关键证据 |
|----|------|------|------|--------|---------|
| SR-1 | IsRaycastLocationValid 实现镂空拦截逻辑 | llm_semantic | pass | 95% | `HoleMaskImage.cs` L16-34 |
| SR-2 | SetHoleAtPosition 步骤切换时被调用 | grep_existence | pass | 90% | `TutorialMask.cs` L44-73 + `PlaneEditUI_Portrait.Tutorial.cs` L149-156 |
| SR-3 | 蒙版关闭方法存在 | grep_existence | pass | 92% | `TutorialMask.cs` L36-42 `OnTutorialCloseHoleMask` |

**推理**：`HoleMaskImage.IsRaycastLocationValid`：hasHole=false → 返回 true（全屏拦截）；hasHole=true → `holeRect.Contains(localPoint)` → 镂空内 false（放行），镂空外 true（拦截）。`TutorialMask.SetHoleAtPosition` 先设 holeRect 再设 hasHole=true 并调用 SetAllDirty()，无中间帧全屏遮挡。`OnTutorialCloseHoleMask` 通过事件监听调用 ClearHole + SetActive(false)。
**证据**：
- `HoleMaskImage.cs` L16-34 `IsRaycastLocationValid` — 完整镂空射线拦截
- `TutorialMask.cs` L44-73 `SetHoleAtPosition` — 世界→本地坐标转换+设 holeRect
- `TutorialMask.cs` L36-42 `OnTutorialCloseHoleMask` — ClearHole+SetActive(false)
- `PlaneEditUI_Portrait.Tutorial.cs` L149-156 `SetMaskHoleAtPosition` — 步骤切换调用
**缺口**：无
**附带发现**：`GuideCutoutMaskController.cs` L251-258 也实现了 ICanvasRaycastFilter，逻辑一致，提供第二套镂空方案

---

### 5. 新手引导 > 组件tips层级 — uncertain (40%)

**规则**：Step 4/11 点击后弹出的 tips 面板渲染层级高于引导蒙版，tips 关闭由下一步引导步骤触发
**判定**：uncertain | 置信度 40%

**子要求判定**：
| SR | 描述 | 类型 | 判定 | 置信度 | 关键证据 |
|----|------|------|------|--------|---------|
| SR-1 | tips 面板层级高于蒙版 | llm_semantic | uncertain | 35% | sortingOrder/SetSiblingIndex 在 CS 中 0 命中 |
| SR-2 | 教程步骤推进时 tips 关闭 | grep_existence | pass | 70% | `PlaneEditorManager.Operate.cs` L524 `CloseTips()` |

**推理**：CloseTips 调用链存在（PlaneEditUI_Portrait → PlanePartTipsPanel → PlanePartTips.CloseTips → SetActive(false)），在拖拽时触发。但 tips 面板的 sortingOrder 在全部 CS 代码中 0 命中。
**缺口**：tips 面板的层级设置极可能通过 Prefab Canvas 组件静态配置（overrideSorting=true + 高 sortingOrder），但代码走查无法确认
**附带发现**：`AssistantPanel.cs` L246 SetMaskColor 仅设半透明颜色，非 sortingOrder

---

### 6. 新手引导 > 拖拽安装-有效路径 — uncertain (50%)

**规则**：拖拽释放到有效位置后：(A)手势消失；(B)文字保留；(C)tips关闭；(D)黄色框高亮持续
**判定**：uncertain | 置信度 50%

**子要求判定**：
| SR | 描述 | 类型 | 判定 | 置信度 | 关键证据 |
|----|------|------|------|--------|---------|
| SR-1 | 手势提示被隐藏 | grep_existence | pass | 90% | `PlaneUITutorialPanel.cs` L276 `mMoveEffectGameObject.SetActive_Self(false)` |
| SR-2 | 文字提示保留 | llm_semantic | pass | 72% | `StartCompletePartAssemblyTutorial(isTips=true)` 重新显示 Tutorial_text_6 |
| SR-3 | tips 关闭 | grep_existence | pass | 75% | `PlaneEditorManager.Operate.cs` L524 `CloseTips()` |
| SR-4 | 黄色框高亮持续 | llm_semantic | uncertain | 55% | `mInstallVaildAreaGameObject` 实例化存在但无显式 show 逻辑 |

**推理**：SR-1/2/3 均有代码支撑。SR-4 中 `hitInstallEffectGO` 在 isTips=true 分支实例化，在 isTips=false 时 Destroy，推断其间持续显示，但代码无显式 SetActive(true) 维持逻辑。
**缺口**：`ShowYellowArea` 等命名变体全库 0 命中，黄色框持续显示依赖 GameObject 默认 active 状态
**附带发现**：L322 `hitInstallEffectGO.transform.SetAsFirstSibling()` 将高亮区域放在底层

---

### 7. 新手引导 > 拖拽安装-失败回退 — fail (78%) 🔴

**规则**：玩家仅点击未拖拽或拖到无效区域时，组件回退到背包原位，手势引导重新触发
**判定**：**fail** | 置信度 78%

**子要求判定**：
| SR | 描述 | 类型 | 判定 | 置信度 | 关键证据 |
|----|------|------|------|--------|---------|
| SR-1 | 组件回退到背包原位 | llm_semantic | **fail** | 80% | ReturnToBag/ResetPartPosition/DragFail/BackToBag 全库 0 命中 |
| SR-2 | 手势引导重新触发 | grep_existence | **fail** | 85% | ShowHandGuide/RestartGuide/ResetGesture 全库 0 命中 |

**推理**：教程阶段通过 `tutorialValidArea` 的 Clamp 机制限制拖拽范围，使"拖到无效区域"在代码中被预防了——组件位置被强制限制在教程指定区域内。但这绕过了需求定义的"失败回退+手势重播"行为，两个 SR 的全部关键词变体均全库 0 命中。
**证据**：
- `PlaneEditorManager.Operate.cs` L352 `OnTouchUp` — `ActivePlanePart.transform.position = HitHolder.position`（通用归位，非"回背包"）
- `PlaneEditorManager.Operate.cs` L136-153 `CheckInValidArea+OnDrag` — Clamp 限制拖拽范围
**缺口**：
1. "组件回退到背包原位"——代码中无任何 BackToBag/ReturnToBag 实现
2. "手势引导重新触发"——mMoveEffectGameObject 无循环触发/重播代码
3. 整个"失败回退+手势重播"功能在 CS 代码中不存在实现
**附带发现**：`PlaneEditorManager.Operate.cs` L156-158 OnEnterTutorialArea 代码被注释掉

---

### 8. 新手引导 > 重叠位置提示 — uncertain (45%)

**规则**：首次将组件拖至重叠位置时，旋转和翻转按钮高亮并显示中文提示；同一教程周期内最多触发一次
**判定**：uncertain | 置信度 45%

**子要求判定**：
| SR | 描述 | 类型 | 判定 | 置信度 | 关键证据 |
|----|------|------|------|--------|---------|
| SR-1 | 旋转/翻转按钮高亮 | grep_existence | uncertain | 30% | 高亮代码全库 0 命中，豁免检查 C-Prefab 可能覆盖 |
| SR-2 | 单次触发标志存在 | grep_existence | pass | 75% | `PlaneUITutorialPanel.cs` L46+L293-295 `mIsShowedTips` |

**推理**：`mIsShowedTips` 单次触发标志存在且有 `if (!mIsShowedTips) { mIsShowedTips = true; ... }` 保护，但该标志控制的是 Tutorial_text_6 文字提示（组装完成提示），而非"重叠位置旋转/翻转按钮高亮"。按钮本身存在但无高亮代码。
**缺口**：旋转/翻转按钮高亮在 CS 代码中完全不存在；`mIsShowedTips` 控制的是 Tutorial_text_6 而非重叠提示

---

### 9. 新手引导 > 一键替换逻辑 — fail (82%) 🔴

**规则**：「一键替换更强组件」按钮点击时：(A) GetBetterSolution 从背包找 Rarity 更高同 PartId 组件替换；(B) 同时高亮背包候选组件和飞船待替换组件
**判定**：**fail** | 置信度 82%

**子要求判定**：
| SR | 描述 | 类型 | 判定 | 置信度 | 关键证据 |
|----|------|------|------|--------|---------|
| SR-1 | GetBetterSolution 替换逻辑 | llm_semantic | pass | 90% | `PlaneEditorManager.Data.cs` L1363-1428 |
| SR-2 | 背包+飞船组件同时高亮 | grep_existence | **fail** | 85% | 全部高亮关键词全库 0 命中 |

**推理**：`GetBetterSolution` 按 PartId 分组、Rarity 升序匹配降序候选，`OnReplaceClick` 遍历结果调用 `ReplacePlanePart` 执行替换，替换逻辑完整。但高亮显示（SR-2）全库 0 命中：HighlightBagItem/ShowItemHighlight/mHighlightPartId/SetPartHighlight 以及通用 Highlight 均无结果。强制豁免自检 A/B/C 均未命中。
**证据**：
- `PlaneEditorManager.Data.cs` L1363-1428 `GetBetterSolution` — 按 PartId 分组 + Rarity 排序匹配
- `PlaneEditPanel_Portrait.cs` L506-526 `OnReplaceClick` — 调用 GetBetterSolution+ReplacePlanePart
**缺口**：替换时背包候选组件和飞船待替换组件均无高亮显示逻辑
**附带发现**：`SetReplacePart` 仅做 blocksRaycasts=false + SetParent，无视觉高亮

---

### 10. 新手引导 > 替换动画反馈 — fail (90%) 🔴

**规则**：一键替换后：(A) 新组件飞入动画；(B) 被替换组件特效标识；(C) 动画期间输入屏蔽
**判定**：**fail** | 置信度 90%

**子要求判定**：
| SR | 描述 | 类型 | 判定 | 置信度 | 关键证据 |
|----|------|------|------|--------|---------|
| SR-1 | 飞入动画 | grep_existence | **fail** | 92% | DOMove/TweenFly/FlyToSlot 全库 0 命中（DOMove 仅在相机控制） |
| SR-2 | 飞出/特效 | grep_existence | **fail** | 92% | FlyToBag/ReplaceEffect/SwapEffect 全库 0 命中 |
| SR-3 | 输入屏蔽 | grep_existence | **fail** | 88% | BlockInput/DisableInteraction/LockInput 全库 0 命中 |

**推理**：替换实现是"销毁旧部件→原位创建新部件"的同步操作（DestroyImmediate），唯一视觉反馈是新部件位置播放 `Effect_UI_SKinSuccess` 粒子特效。无飞入/飞出 Tween 动画，无输入屏蔽机制。
**证据**：
- `PlaneEditorManager.cs` L670-709 `ReplacePlanePart` — L683 DestroyImmediate 销毁旧部件，L685 CreateReplacePlanePart 原位创建
**缺口**：三个 SR 全部缺失，替换过程为同步瞬切，无动画反馈

---

### 11. 新手引导 > 替换完成后续流程 — pass (85%)

**规则**：替换完成后引导退出组装界面；退出后推进到引导进入第二关战斗（Tutorial_text_8），顺序不可倒转
**判定**：pass | 置信度 85%

**子要求判定**：
| SR | 描述 | 类型 | 判定 | 置信度 | 关键证据 |
|----|------|------|------|--------|---------|
| SR-1 | 替换完成后引导退出 | grep_existence | pass | 88% | `PlaneUITutorialPanel.cs` L398-415 `StartSoftBackFromPlaneTutorial` |
| SR-2 | 退出后推进到第二关 | grep_existence | pass | 82% | `MainUITutorialPanel.cs` L99-100 SecondBattle → Tutorial_text_8 |

**推理**：替换完成回调 → ClosePanel → FinishAndTutorialReport(ReplacePart) → CheckNextTutorial → StartSoftBackFromPlaneTutorial 高亮 PlaneBackButton。`CheckPlaneEditCurTutorial` 的 stepEnum+1 机制保证 ReplacePart → SecondBattle 顺序确定性。
**证据**：
- `PlaneUITutorialPanel.cs` L369-386 替换完成回调链
- `PlaneUITutorialPanel.cs` L398-415 `StartSoftBackFromPlaneTutorial` — 高亮返回按钮
- `TutorialUtil.cs` L255-277 `CheckPlaneEditCurTutorial` — stepEnum+1 保证顺序
- `MainUITutorialPanel.cs` L99-100 SecondBattle → Tutorial_text_8
**缺口**：无

---

### 12. 新手引导 > 关卡绑定 — pass (85%)

**规则**：第二关在第一关未通关时锁定（PreRequisiteLevel）；两关均通过 ChallengeBattleStage 发起战斗
**判定**：pass | 置信度 85%

**子要求判定**：
| SR | 描述 | 类型 | 判定 | 置信度 | 关键证据 |
|----|------|------|------|--------|---------|
| SR-1 | PreRequisiteLevel 锁定 | grep_existence | pass | 85% | `StarMapPoint.cs` L51-59 + `ChapterPanel_MainPanel.cs` L508-513 |
| SR-2 | ChallengeBattleStage 标准接口 | grep_existence | pass | 88% | `ChapterPanel_MainPanel.cs` L260 |

**推理**：`StarMapPoint.Init` 通过 `PreRequisiteLevel != 0 → CheckBattleStageFinish(PreRequisiteLevel)` 判定锁定；`ChapterPanel_MainPanel.SelectLevel` 根据 isLocked 禁用 BattleButton。`ChallengeBattleStage(stageId, scheme.SchemeId)` 为统一战斗发起接口。
**证据**：
- `StarMapPoint.cs` L51-59 `Init` — PreRequisiteLevel 锁定逻辑
- `ChapterPanel_MainPanel.cs` L508-513 `SelectLevel` — isLocked 时禁用 BattleButton
- `ChapterPanel_MainPanel.cs` L260 — ChallengeBattleStage 调用
**缺口**：无

---

### 13. 新手引导 > 激活组件放置 — uncertain (55%)

**规则**：激活组件（230057）仅可放置于最多 4 个特定合法格位（tutorialValidArea）；已占用 N 格则高亮 4-N 个
**判定**：uncertain | 置信度 55%

**子要求判定**：
| SR | 描述 | 类型 | 判定 | 置信度 | 关键证据 |
|----|------|------|------|--------|---------|
| SR-1 | 合法放置区域定义存在 | grep_existence | pass | 85% | `PlaneEditUI_Portrait.Tutorial.cs` L17-18 `mValidArea_Tutorial` |
| SR-2 | 格位占用检测排除已占用 | llm_semantic | uncertain | 45% | RectTransform 约束存在但无"4格位"或"4-N"逻辑 |

**推理**：`mValidArea_Tutorial` 为 `[SerializeField] RectTransform`，在 DragSkillPart/FindValidPosition 步骤约束拖拽范围。`HasEffectPart` 通过 `UpdateEffGrid` 检测格位重叠判定激活成功。但代码使用 RectTransform 面积约束而非离散格位列表，无"最多 4 格位"或"4-N"动态计数。
**证据**：
- `PlaneEditUI_Portrait.Tutorial.cs` L17-18 — SerializeField RectTransform mValidArea_Tutorial
- `PlaneEditorManager.Operate.cs` L135-163 `CheckInValidArea` — 区域约束检测
**缺口**：具体格位数由 Prefab/场景 RectTransform 大小隐式定义，代码中无法确认

---

### 14. 新手引导 > 激活组件拖拽三状态 — fail (85%) 🔴

**规则**：拖拽中三状态循环：(1)不在合法格位→Tutorial_text_14/11+黄色高亮；(2)在合法格位未激活→Tutorial_text_12；(3)成功激活→Tutorial_text_13；移出后可回切至(1)
**判定**：**fail** | 置信度 85%

**子要求判定**：
| SR | 描述 | 类型 | 判定 | 置信度 | 关键证据 |
|----|------|------|------|--------|---------|
| SR-1 | 状态(1) Tutorial_text_14/11 | grep_existence | uncertain | 55% | Tutorial_text_11 找到但 Tutorial_text_14 仅在 MoveTutorialPanel（战斗引导） |
| SR-2 | 状态(2) Tutorial_text_12 | grep_existence | pass | 90% | `PlaneUITutorialPanel.cs` L527 `StartFindValidPositionTutorial` |
| SR-3 | 状态(3) Tutorial_text_13 | grep_existence | pass | 90% | `PlaneUITutorialPanel.cs` L559 `StartCompletedSkillPartAssemblyTutorial` |
| SR-4 | 移出后状态回切 | llm_semantic | **fail** | 95% | `OnExitTutorialArea` 空方法体（L210-213）|

**推理**：SR-2/3 的文案显示成立。SR-4 确定性 fail：`PlaneEditorManager.Operate.cs` L210-213 `OnExitTutorialArea` 方法体为空，违反空实现条款（空方法体 = 确定性 fail，豁免无效）。组件移出合法格位后无任何状态重置逻辑。
**证据**：
- `PlaneUITutorialPanel.cs` L465-497 `StartDragSkillPartTutorial` — Tutorial_text_11 显示
- `PlaneUITutorialPanel.cs` L514-541 `StartFindValidPositionTutorial` — Tutorial_text_12
- `PlaneUITutorialPanel.cs` L543-598 `StartCompletedSkillPartAssemblyTutorial` — Tutorial_text_13
- `PlaneEditorManager.Operate.cs` L210-213 `OnExitTutorialArea` — **空方法体**
**缺口**：OnExitTutorialArea 空方法体使状态回切不可能实现；L159-162 退出检测中 mIsOverTutorialArea=false 也被注释掉
**附带发现**：L154-163 进入教程区域的检测逻辑中 OnEnterTutorialArea 在 L156-157 被注释掉

---

### 15. 新手引导 > 激活影响显示 — uncertain (60%)

**规则**：选中带激活效果的组件时，ShowEffGrid 显示格位高亮、ShowActivateEffect 显示被影响组件特效；取消选中后 HideEffGrid 清除
**判定**：uncertain | 置信度 60%

**子要求判定**：
| SR | 描述 | 类型 | 判定 | 置信度 | 关键证据 |
|----|------|------|------|--------|---------|
| SR-1 | ShowEffGrid 显示高亮 | grep_existence | pass | 92% | `CPlanePartInfo.cs` L427-434 + `PlaneOperatePanel_Portrait.cs` L78-81 |
| SR-2 | 被影响组件激活特效 | grep_existence | uncertain | 45% | EdgeLight 为 Rarity 流光，非激活特效 |
| SR-3 | HideEffGrid 清除高亮 | grep_existence | pass | 92% | `CPlanePartInfo.cs` L439-445 + `PlaneEditUI_Portrait.cs` L208-212 |

**推理**：ShowEffGrid/HideEffGrid 调用链完整（CPlanePartInfo → EffGridList → SetGrid/HideGrid）。但 SR-2 中 `PlanePartEdgeLight` 根据 Rarity 设置流光颜色，并非"激活特效"。`UpdateEffect` 仅切换格位 sprite（mValidSprite/mAreaSprite），是格位层视觉变化而非对已安装组件的特效。
**证据**：
- `CPlanePartInfo.cs` L427-434 `ShowEffGrid` — 遍历 EffGridList 设 PlaneHitGrid.Effect
- `PlaneOperatePanel_Portrait.cs` L78-81 `Init` — 选中时调用 ShowEffGrid
- `CPlanePartInfo.cs` L439-445 `HideEffGrid` — 遍历 EffGridList 调用 HideGrid
- `PlanePartEdgeLight.cs` L8-15 `SetData` — Rarity 颜色流光，非激活特效
**缺口**：被影响位置已安装组件的"激活特效"（ShowActivateEffect/ActivateEdgeEffect）在代码中不存在

---

### 16. 新手引导 > 激活按钮半径配置 — pass (85%)

**规则**：旋转/翻转按钮点击半径参数（mMinRadius）考虑 Effect 影响范围，或提供独立可配置参数
**判定**：pass | 置信度 85%

**子要求判定**：
| SR | 描述 | 类型 | 判定 | 置信度 | 关键证据 |
|----|------|------|------|--------|---------|
| SR-1 | mMinRadius 可配置 | grep_existence | pass | 90% | `PlaneOperatePanel_Portrait.cs` L36-37 `[SerializeField] float mMinRadius` |
| SR-2 | 半径与 Effect 范围关联 | llm_semantic | pass | 82% | `GetAllGridPos()` 包含 EffGridList → `GetMaxVertexDistance` → `UpdateSize` |

**推理**：`mMinRadius` 为 SerializeField float，通过 Inspector 配置。`OperateUIFollow` 调用 `GetAllGridPos()` 获取所有格位坐标（含 EffGridList），再用 `GetMaxVertexDistance` 计算到核心格位的最大顶点距离作为 radius 传入 `UpdateSize`。`GetAllGridPos` 明确包含 EffGridList（CPlanePartInfo.cs L355-358），半径自动与 Effect 范围关联。
**证据**：
- `PlaneOperatePanel_Portrait.cs` L36-37 — `[SerializeField] private float mMinRadius`
- `PlaneOperatePanel_Portrait.cs` L232-248 `UpdateSize` — `radius = Mathf.Max(radius, mMinRadius) + 10`
- `PlaneEditorManager.Operate.cs` L575-584 `OperateUIFollow` — GetAllGridPos → GetMaxVertexDistance → UpdateSize
- `CPlanePartInfo.cs` L348-360 `GetAllGridPos` — 返回 HitGridList + EffGridList
**缺口**：无

---

### 17. 新手引导 > 引导步骤顺序与持久化 — pass (95%)

**规则**：步骤严格按枚举顺序执行，progress > mTutorialProgress 防倒退；双重持久化（服务端 SetTutorialProcess + 本地 PlayerPrefs）；场景切换后不重置
**判定**：pass | 置信度 95%

**子要求判定**：
| SR | 描述 | 类型 | 判定 | 置信度 | 关键证据 |
|----|------|------|------|--------|---------|
| SR-1 | progress > mTutorialProgress 防倒退 | grep_existence | pass | 97% | `TutorialUtil.cs` L334 |
| SR-2 | SetTutorialProcess 服务端持久化 | grep_existence | pass | 95% | `TutorialUtil.cs` L337 + `AccessManager.Query.cs` L14-22 |
| SR-3 | PlayerPrefs 本地持久化 | grep_existence | pass | 95% | `TutorialUtil.cs` L346-354 |

**推理**：`TutorialUtil.FinishTutorial(int progress)` L334 明确 `if (progress > mTutorialProgress)` → 更新内存 → `SetTutorialProcess`（服务端）→ `PlayerPrefs.SetInt`+Save（本地）。`UserData.PlayerInfo.cs` L111-120 登录恢复时也执行同样比较后调用 FinishTutorial 对齐进度。
**证据**：
- `TutorialUtil.cs` L330-361 `FinishTutorial` — progress > mTutorialProgress + 双重持久化
- `AccessManager.Query.cs` L14-22 `SetTutorialProcess` — 服务端接口
- `UserData.PlayerInfo.cs` L111-120 — 登录恢复对齐进度
**缺口**：无
**附带发现**：TutorialManager.FinishAndTutorialReport(L428-437) 额外做 localProgress < target && progress < target 双重检查，提供防重复推进保护

---

### 18. 新手引导 > 固定组件ID容错 — uncertain (45%)

**规则**：TUTORIAL_PART_ID_1=230068 和 TUTORIAL_PART_ID_2=230057 使用处有 null 检查；缺失时有明确回退路径
**判定**：uncertain | 置信度 45%

**子要求判定**：
| SR | 描述 | 类型 | 判定 | 置信度 | 关键证据 |
|----|------|------|------|--------|---------|
| SR-1 | 230068 有 null 检查+容错 | llm_semantic | uncertain | 50% | `TutorialUtil.cs` L213-214 有检查但下游方法缺 null 保护 |
| SR-2 | 230057 有 null 检查+容错 | llm_semantic | uncertain | 45% | `TutorialUtil.cs` L233-234 有检查但下游同样缺口 |

**推理**：`TutorialUtil.CheckMainPanelCurTutorialIn` 中 `CPlanePartData.Find(PartId == 230068)` 返回 null 时有 if(targetPart != null) 检查，回退到 FirstBattle。但回退路径粗糙（直接跳回战斗步骤）。下游 `PlaneUITutorialPanel` 多个协程（StartClickAndCheckPartAttrTutorial L201, StartDragNormalPartTutorial 等）直接访问 `mTargetPart.transform.position`，若上游传入 null 将 NullReferenceException。
**证据**：
- `TutorialUtil.cs` L213-219 — TUTORIAL_PART_ID_1 null 检查 → 回退 FirstBattle
- `TutorialUtil.cs` L233-239 — TUTORIAL_PART_ID_2 null 检查 → 回退 SecondBattle
- `PlaneUITutorialPanel.cs` L201 — StartClickAndCheckPartAttrTutorial 直接使用 mTargetPart 无 null 保护
**缺口**：TutorialUtil 层有 null 检查但下游步骤方法缺 null 保护链，容错路径不完整
**附带发现**：仅 `StartCompletedSkillPartAssemblyTutorial(L545)` 有 if(mTargetPart != null) 保护，其余步骤方法均无

---

### 19. 新手引导 > 弱引导退出 — uncertain (50%)

**规则**：激活组件成功激活武器后触发弱引导退出；玩家执行退出动作后教程推进至最终完成状态
**判定**：uncertain | 置信度 50%

**子要求判定**：
| SR | 描述 | 类型 | 判定 | 置信度 | 关键证据 |
|----|------|------|------|--------|---------|
| SR-1 | 激活成功后弱引导退出 | grep_existence | pass | 70% | `PlaneUITutorialPanel.cs` L398-415 `StartSoftBackFromPlaneTutorial` |
| SR-2 | 退出后推进至最终完成 | grep_existence | uncertain | 40% | 缺少退出后显式 FinishTutorial 调用 |

**推理**：`StartSoftBackFromPlaneTutorial` 在 ReplacePart 完成后调用，实现解除组件限制 + 高亮返回按钮（PlaneBackButton）。但内部仅做 UI 展示，未看到点击返回后将教程推进至最终完成步骤的显式 FinishTutorial 调用。可能依赖返回主界面后 `CheckMainPanelCurTutorialIn` 的隐式重新判定，但缺乏显式证据。
**证据**：
- `PlaneUITutorialPanel.cs` L398-415 `StartSoftBackFromPlaneTutorial` — 弱引导退出逻辑
- `PlaneUITutorialPanel.cs` L384-385 — 替换成功后调用 StartSoftBackFromPlaneTutorial
**缺口**：弱引导退出后如何将进度推至最终完成状态不明确

---

## 风险汇总

### 确定性缺失（fail）— 4 项

| 契约 | 优先级 | 缺失内容 | 影响 | 建议 |
|------|--------|----------|------|------|
| #7 拖拽失败回退 | P0 | 回退到背包+手势重播全部不存在 | 玩家拖拽失败后无指引 | 实现回退逻辑或改需求为 Clamp 预防策略 |
| #9 一键替换高亮 | P0 | 替换时无高亮显示 | 玩家不知道哪个组件被替换 | 添加候选/被替换组件的视觉高亮 |
| #10 替换动画 | P1 | 飞入/飞出/输入屏蔽全部不存在 | 替换为瞬切，用户体验差 | 添加 Tween 动画 + 输入锁定 |
| #14 状态回切 | P0 | OnExitTutorialArea 空方法体 | 移出合法格位后状态无法重置 | 实现 OnExitTutorialArea 回切逻辑 |

### 高风险存疑 — 3 项（需实机验证或 Prefab 检查）

| 契约 | 优先级 | 存疑内容 | 建议验证方式 |
|------|--------|----------|------------|
| #18 组件ID容错 | P0 | 下游方法缺 null 保护 | 实机删除对应组件 ID 后观察是否崩溃 |
| #13 激活格位约束 | P0 | 无显式 4 格位约束 | 检查 Prefab 中 mValidArea_Tutorial 的 RectTransform 尺寸 |
| #5 tips层级 | P1 | sortingOrder 代码 0 命中 | 检查 tips 面板 Prefab 的 Canvas 组件 sortingOrder |

### 中风险存疑 — 4 项

| 契约 | 优先级 | 存疑内容 |
|------|--------|----------|
| #6 黄色框高亮 | P0 | 依赖 GameObject 默认 active 状态 |
| #8 重叠提示 | P1 | 按钮高亮逻辑在 CS 中不存在 |
| #15 激活特效 | P1 | EdgeLight 为 Rarity 流光，非激活特效 |
| #19 弱引导退出 | P2 | 退出后推进逻辑不完整 |

---

## 附录

### 核心文件索引

| 文件 | 职责 | 走查命中契约数 |
|------|------|---------------|
| PlaneUITutorialPanel.cs | 飞船编辑教程主面板，控制各步骤流转 | 12 |
| PlaneEditorManager.Operate.cs | 拖拽操作/区域检测/有效位置判定 | 8 |
| TutorialUtil.cs | 进度管理/双重持久化/步骤检查 | 6 |
| MainUITutorialPanel.cs | 主界面教程（关卡选择/战斗引导） | 4 |
| HoleMaskImage.cs | 镂空射线拦截实现 | 2 |
| TutorialMask.cs | 蒙版管理（镂空/关闭） | 2 |
| PlaneEditorManager.cs | 组件替换/创建 | 4 |
| PlaneEditorManager.Data.cs | GetBetterSolution/GetMaxVertexDistance | 3 |
| AssistantPanel.cs | 圆形镂空/蒙版颜色 | 2 |
| PlaneOperatePanel_Portrait.cs | 按钮半径/组件操作 | 3 |
| CPlanePartInfo.cs | ShowEffGrid/HideEffGrid/GetAllGridPos | 3 |

### ast-grep 扫描摘要

- 扫描文件数：20
- 跨类调用：11（TutorialUtil 被 6 个类调用）
- 事件监听：3（PlaneOperatePanel_Portrait AddListener/RemoveListener）
- GetComponent 无 null 检查：25 处（info 级，非阻塞）
