# 质量契约走查报告

- 日期：2026-05-11 14:11
- 需求文档：新手引导模块质量契约（6 条 code_review）
- 走查引擎：Cursor Agent

## 总览

| 指标 | 数量 |
|------|------|
| 契约总数 | 6 |
| 代码走查 (code_review) | 6 |
| 通过 (pass) | 2 |
| 违规 (fail) | 0 |
| 存疑 (uncertain) | 4 |
| 跳过 (非 code_review) | 0 |
| 调度模式 | 串行（同模块，共享搜索） |
| ast-grep | 已启用（引用已有扫描结果：empty-method-body 5处命中） |
| Evidence 验证 | 待执行 verify_evidence.py |
| 映射缓存(v2) | 重新搜索模式，跳过缓存 |

## 走查详情

### 1. 新手引导 > 关卡选择 — pass (85%)

**规则**：新手引导 Step 0 触发时，关卡选择界面进入强引导态：全屏蒙版激活（SetMaskColor(true)），BattleButton 位置设有圆形镂空高亮（CircleAim），非镂空区域点击被拦截；第一关是唯一可发起战斗的关卡

**边界提示**：实现方式为蒙版拦截而非 interactable=false；「默认选中第一关」指界面打开时战斗目标为第一关

**判定**：pass | 置信度 85%

**子要求判定**：

| SR | 描述 | 类型 | 判定 | 置信度 | 关键证据 |
|----|------|------|------|--------|---------|
| SR-1 | StartMatchTutorial 对 FirstBattle 调用 SetMaskColor(true) + CircleAim 镂空到 BattleButton | grep_existence | pass | 90% | MainUITutorialPanel.cs L74-117 完整实现 |
| SR-2 | 强引导态下仅第一关可发起战斗 | llm_semantic | pass | 80% | AssistantPanel 蒙版拦截 + StarMapPoint.mIsLocked 锁定机制 |

**汇总**：两个 SR 均 pass，整条 pass。

**证据**：
- `MainUITutorialPanel.cs` L74-117 `StartMatchTutorial` — FirstBattle 步骤获取 BattleButton→CircleAim_UI→SetMaskColor(true)
- `AssistantPanel.cs` L244-336 `SetMaskColor+OnEventAction` — 蒙版半透明黑色覆盖，CircleGuide 控制镂空区射线穿透
- `StarMapPoint.cs` L51-62 `Init` — PreRequisiteLevel==0 解锁第一关，其余按前置条件锁定

**缺口**：无

---

### 2. 新手引导 > 武器自动战斗提示 — uncertain (55%)

**规则**：Step 1 弱引导阶段，Tutorial_text_1 在进入步骤时设置到持久文字区域（mContentText）显示；该文字提示存在隐藏触发机制（事件回调或步骤推进），不永久残留到教程结束

**边界提示**：需求文档未明确自动消失时机；Tutorial_text_14（浮动提示）与 Tutorial_text_1（持久底部文字）是两段独立提示

**判定**：uncertain | 置信度 55%

**子要求判定**：

| SR | 描述 | 类型 | 判定 | 置信度 | 关键证据 |
|----|------|------|------|--------|---------|
| SR-1 | Tutorial_text_1 在 StartMoveTutorial 时被设置到 mContentText | grep_existence | pass | 90% | MoveTutorialPanel.cs L23 |
| SR-2 | 存在隐藏/清除 Tutorial_text_1 的触发逻辑 | llm_semantic | uncertain | 40% | HideContentText 等 5 种变体全库 0 命中 |

**汇总**：SR-2 uncertain → 整条 uncertain。

**证据**：
- `MoveTutorialPanel.cs` L21-24 `Start` — mContentText.text = "Tutorial_text_1".GetLocaliztion()
- `MoveTutorialPanel.cs` L37-48 `ShowTipUI` — Tutorial_text_14 通过 DOFade 淡出，但 mContentText 无对应隐藏逻辑

**缺口**：mContentText（Tutorial_text_1）无独立隐藏/清除方法。面板 GameObject 销毁时隐式清除，但无事件驱动的显式隐藏机制。需确认 MoveTutorialPanel 的生命周期管理。

---

### 3. 新手引导 > 引导蒙版射线拦截 — pass (92%)

**规则**：强引导进行中，HoleMaskImage.IsRaycastLocationValid 实现「镂空内放行（返回 false）、镂空外拦截（返回 true）」逻辑；步骤切换时 SetHoleAtPosition 同步更新镂空区域位置；蒙版关闭时通过 OnTutorialCloseHoleMask 或等效方法禁用蒙版

**边界提示**：步骤切换时不得出现全屏遮挡的中间帧；镂空区域由 holeRect（矩形）定义；hasHole=false 时全屏拦截

**判定**：pass | 置信度 92%

**子要求判定**：

| SR | 描述 | 类型 | 判定 | 置信度 | 关键证据 |
|----|------|------|------|--------|---------|
| SR-1 | IsRaycastLocationValid 镂空内 false / 镂空外 true | llm_semantic | pass | 95% | HoleMaskImage.cs L16-34 完整实现 |
| SR-2 | 步骤切换时 SetHoleAtPosition 同步更新 | grep_existence | pass | 90% | TutorialMask.cs L44 + PlaneEditUI_Portrait.Tutorial.cs L141/151/156 |
| SR-3 | 蒙版关闭时调用禁用方法 | grep_existence | pass | 92% | TutorialMask.cs L36-42 OnTutorialCloseHoleMask |

**汇总**：三个 SR 均 pass，整条 pass。

**证据**：
- `HoleMaskImage.cs` L16-34 `IsRaycastLocationValid` — 镂空内返回 false（放行）、镂空外返回 true（拦截）
- `TutorialMask.cs` L44-73 `SetHoleAtPosition` — 世界坐标→本地矩形→赋值 holeRect/hasHole
- `TutorialMask.cs` L36-42 `OnTutorialCloseHoleMask` — 禁用 gameObject + ClearHole

**缺口**：无

**附带发现**：

| 级别 | 文件 | 行号 | 方法 | 问题 | 违规性 |
|------|------|------|------|------|--------|
| [已确认] | `TutorialMask.cs` | L26 | `Start()` | 空方法体 | 建议改进 |
| [已确认] | `TutorialMask.cs` | L83 | `Update()` | 空方法体，有 Unity 回调开销 | 建议改进 |

---

### 4. 新手引导 > 组件tips层级 — uncertain (50%)

**规则**：Step 4（组件230068）和 Step 11（组件230057）点击后弹出的 tips 面板渲染层级高于引导蒙版，tips 内容完整可见不被遮挡；tips 关闭由下一步引导步骤触发

**边界提示**：层级控制可通过 Canvas sortingOrder 或 SetSiblingIndex 实现

**判定**：uncertain | 置信度 50%

**子要求判定**：

| SR | 描述 | 类型 | 判定 | 置信度 | 关键证据 |
|----|------|------|------|--------|---------|
| SR-1 | tips 面板通过 sortingOrder 或 SetSiblingIndex 设到蒙版之上 | llm_semantic | uncertain | 40% | 无显式层级控制，仅有教程面板 SetAsFirstSibling 隐式效果 |
| SR-2 | 教程步骤推进时触发 tips 关闭 | grep_existence | pass | 75% | OnDrag L524 CloseTips + LODDY_PLANE_PART_TIPS_CLOSE 事件 |

**汇总**：SR-1 uncertain → 整条 uncertain。

**证据**：
- `PlaneEditUI_Portrait.Tutorial.cs` L50 `OpenPlaneUITutorial` — SetAsFirstSibling 将教程面板放底层
- `PlaneEditorManager.Operate.cs` L274,524 `OnDrag+OnTouchDown` — CloseTips 和事件派发

**缺口**：tips 面板无显式 sortingOrder/SetSiblingIndex 控制。需查看 Prefab 中 TutorialMask 与 PlanePartTipsPanel 的 sibling 顺序确认隐式层级是否满足需求。

---

### 5. 新手引导 > 拖拽安装-有效路径 — uncertain (62%)

**规则**：Step 5/6 拖拽教学中，玩家将组件拖至黄色框有效位置并释放后：(A)手势提示消失；(B)文字提示（Tutorial_text_6）保留不被 ClosePanel 关闭；(C)组件 tips 关闭；(D)底版黄色框高亮持续显示直至组件落位确认

**边界提示**：原文 EndDragNormalPartTutorial 调用 ClosePanel 存在关闭文字提示的风险

**判定**：uncertain | 置信度 62%

**子要求判定**：

| SR | 描述 | 类型 | 判定 | 置信度 | 关键证据 |
|----|------|------|------|--------|---------|
| SR-1 | 拖拽释放到有效区域后手势提示被隐藏 | grep_existence | pass | 88% | EndDragNormalPartTutorial L276 SetActive_Self(false) |
| SR-2 | 拖拽完成后文字提示不随 ClosePanel 消失 | llm_semantic | uncertain | 45% | Tutorial_text_6 设置在 CompletePartAssembly 步骤（L306），CheckNextTutorial 的 CompletePartAssembly case 仅 break |
| SR-3 | 拖拽完成后组件 tips 被关闭 | grep_existence | pass | 80% | OnDrag L524 CloseTips + LODDY_PLANE_PART_TIPS_CLOSE |
| SR-4 | 黄色框高亮持续显示直至落位确认 | llm_semantic | pass | 75% | mInstallVaildAreaGameObject L321 Instantiate, L342 Destroy |

**汇总**：SR-2 uncertain → 整条 uncertain。

**证据**：
- `PlaneUITutorialPanel.cs` L274-286 `EndDragNormalPartTutorial` — 手势隐藏 + ClosePanel
- `PlaneUITutorialPanel.cs` L289-344 `StartCompletePartAssemblyTutorial` — Tutorial_text_6 + 黄色框
- `PlaneEditorManager.Operate.cs` L524 `OnDrag` — CloseTips

**缺口**：Tutorial_text_6 的完整显示链路需确认——CheckNextTutorial 对 CompletePartAssembly 只有 break，需通过外部 ShowTutorialTips 触发。

**附带发现**：

| 级别 | 文件 | 行号 | 方法 | 问题 | 违规性 |
|------|------|------|------|------|--------|
| [已确认] | `PlaneUITutorialPanel.cs` | L84 | `OnTutorialIsCompleted` | CompletePartAssembly case 仅 break，不通过事件推进 | 建议改进 |

---

### 6. 新手引导 > 拖拽安装-失败回退 — uncertain (50%)

**规则**：Step 5/6 中，玩家仅点击未拖拽或将组件拖拽后未释放到有效区域时，组件回退到背包原位，手势引导重新触发进入下一轮尝试

**边界提示**：「回退原位」指组件恢复到背包中的原排列位置；手势引导重新出现时重置到初始拖拽起始状态

**判定**：uncertain | 置信度 50%

**子要求判定**：

| SR | 描述 | 类型 | 判定 | 置信度 | 关键证据 |
|----|------|------|------|--------|---------|
| SR-1 | 拖拽释放到无效区域时组件回退到背包原位 | llm_semantic | uncertain | 55% | 克隆拖拽模型隐式处理（DestroyImmediate），但 OnDragCloneExitArea 空实现 |
| SR-2 | 回退后手势引导重新触发 | grep_existence | uncertain | 50% | mMoveEffectGameObject 持续显示（未被隐藏），但无显式重新触发/重置动画代码 |

**汇总**：两个 SR 均 uncertain → 整条 uncertain。

**证据**：
- `PlaneEditUI_Portrait.cs` L244-258 `HandleTouchUp` — 克隆体 DestroyImmediate 实现隐式回退
- `PlaneUITutorialPanel.cs` L249-276 `StartDragNormalPartTutorial+EndDragNormalPartTutorial` — 手势引导持续可见（仅成功时隐藏）

**缺口**：1) OnDragCloneExitArea 空实现，回退时可能缺少视觉反馈。2) 手势动画循环播放需查看 Prefab/Animator 确认。

**附带发现**：

| 级别 | 文件 | 行号 | 方法 | 问题 | 违规性 |
|------|------|------|------|------|--------|
| [已确认] | `PlaneEditUI_Portrait.cs` | L396 | `OnDragCloneExitArea` | 空方法体：拖拽克隆件离开有效区域无处理逻辑 | 潜在违规 |
| [已确认] | `PlaneEditorManager.Operate.cs` | L210 | `OnExitTutorialArea` | 空方法体：退出教程区域无处理逻辑 | 建议改进 |
| [已确认] | `PlaneEditorManager.Operate.cs` | L157 | `CheckInValidArea` | OnEnterTutorialArea 调用被注释掉，构成死代码 | 建议改进 |
| [已确认] | `PlaneUITutorialPanel.cs` | L417 | `StartSecondEditTutorial` | 空方法体：第二次编辑教程入口未实现 | 潜在违规 |
| [已确认] | `AssistantPanel.cs` | L67 | `OnDisable` | 空方法体 | 建议改进 |

---

## 未走查契约

无（6 条均为 code_review，全部走查完成）。
