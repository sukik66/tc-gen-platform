# 质量契约走查报告

- 日期：2026-05-13 23:00
- 需求文档：新手引导-一测.docx
- 走查引擎：Cursor Agent（refreshContracts=true 重新提取 + refreshSearch=true 全量重搜）

## 总览

| 指标 | 数量 |
|------|------|
| 契约总数 | 18 |
| 代码走查 (code_review) | 18 |
| 通过 (pass) | 4 |
| 违规 (fail) | 1 |
| 部分实现 (partial) | 13 |
| 存疑 (inconclusive) | 0 |
| 调度模式 | 并行 3 组（A 基础引导 001-006 / B 组装替换 007-012 / C 激活持久化 013-018）|
| ast-grep | 降级为纯 Grep+Read（ExternalClient 目录超时）|

### 结论分布

```
pass:         ████░░░░░░░░░░░░░░ 4/18 (22%)
partial:      █████████████░░░░░ 13/18 (72%)
fail:         █░░░░░░░░░░░░░░░░░ 1/18 (6%)
inconclusive: ░░░░░░░░░░░░░░░░░░ 0/18 (0%)
```

### 全局关键发现

**Tutorial_text_N 完整映射表**（本次走查最重要的发现）：

| 文本Key | TutorialStepEnum | 音频Key | 所在文件 | 行号 |
|---------|-----------------|---------|---------|------|
| Tutorial_text_0 | FirstBattle | ⚠️ 无音频 | MainUITutorialPanel.cs | L95 |
| Tutorial_text_1 | ❌ 代码未引用 | — | — | — |
| Tutorial_text_2 | FirstReturn | Audio_Vos_Tutorial_001 | SettlementUITutorialPanel.cs | L76 |
| Tutorial_text_3 | FirstEdit | Audio_Vos_Tutorial_002 | MainUITutorialPanel.cs | L146 |
| Tutorial_text_4 | ClickAndCheckPartAttr | Audio_Vos_Tutorial_003 | PlaneUITutorialPanel.cs | L215 |
| Tutorial_text_5 | DragNormalPart | Audio_Vos_Tutorial_004 | PlaneUITutorialPanel.cs | L255 |
| Tutorial_text_6 | CompletePartAssembly(tips) | Audio_Vos_Tutorial_005 | PlaneUITutorialPanel.cs | L306 |
| Tutorial_text_7 | ReplacePart | Audio_Vos_Tutorial_006 | PlaneUITutorialPanel.cs | L364 |
| Tutorial_text_8 | SecondBattle | Audio_Vos_Tutorial_007 | MainUITutorialPanel.cs | L100 |
| Tutorial_text_9 | SecondReturn | Audio_Vos_Tutorial_008 | SettlementUITutorialPanel.cs | L82 |
| Tutorial_text_10 | SecondEdit | Audio_Vos_Tutorial_009 | MainUITutorialPanel.cs | L152 |
| Tutorial_text_11 | SecondClickAndCheckPartAttr + DragSkillPart | Audio_Vos_Tutorial_010 | PlaneUITutorialPanel.cs | L441+L481 |
| Tutorial_text_12 | FindValidPosition | Audio_Vos_Tutorial_011 | PlaneUITutorialPanel.cs | L527 |
| Tutorial_text_13 | CompletedSkillPartAssembly | Audio_Vos_Tutorial_012 | PlaneUITutorialPanel.cs | L559 |
| Tutorial_text_14 | ❌ 代码未引用 | — | — | — |

### 高风险项速览（fail + P0 partial）

| 契约 | 优先级 | 结论 | 核心问题 |
|------|--------|------|---------|
| #12 激活拖拽三状态 | P0 | **fail** | Tutorial_text_14 全库不存在 + `OnExitTutorialArea` 空方法 + 线性状态机不支持循环切换 |
| #5 拖拽成功反馈 | P0 | **partial** | 手势消失/底版高亮/tips关闭均 pass，但 `ClosePanel` 将文字提示一并关闭 |
| #6 拖拽失败回退 | P0 | **partial** | `ResetPart` 回退存在(pass)，但手势引导无重播机制(fail) |
| #1 关卡选择强引导 | P0 | **partial** | 「默认选中第一关」逻辑未找到(inconclusive)，CircleAim 强引导 pass |
| #7 旋转翻转引导 | P1 | **partial** | Tutorial_text_6 + 关闭逻辑 pass，但旋转翻转按钮高亮缺失 |
| #9 替换动画屏蔽 | P1 | **partial** | `Effect_UI_SKinSuccess` 特效存在(pass)，但无操作屏蔽(fail) |
| #2 滑动弱引导 | P1 | **partial** | Tutorial_text_1 未在 .cs 引用（MoveControl 使用独立 prefab） |
| #8 一键替换高亮 | P1 | **partial** | GetBetterSolution Rarity 逻辑完整(pass)，高亮为 GameObject 复制非原组件高亮 |
| #8 一键替换高亮 | P1 | **partial** | GetBetterSolution 逻辑完整但高亮显示 0 命中 |

---

## 走查详情

### 1. 新手引导 > 关卡选择强引导 — partial (80%)

**契约ID**：qc-1747148000001
**规则**：Step 0 触发时关卡选择界面进入强引导态：默认选中第一关作为战斗目标，仅第一关战斗按钮可交互；屏幕显示 Tutorial_text_0 文本
**判定**：partial | 置信度 80%

| SR | 描述 | 类型 | 判定 | 置信度 | 关键证据 |
|----|------|------|------|--------|---------|
| SR-1 | 默认选中第一关 | llm_semantic | partial | 75% | `MainUITutorialPanel.cs` L78 获取 BattleButton 位置用于 CircleAim，但无显式"选中第一关"逻辑 |
| SR-2 | 仅第一关按钮可交互 | llm_semantic | pass | 88% | `MainUITutorialPanel.cs` L107-117 CircleAim_UI + SetMaskColor(true) |
| SR-3 | Tutorial_text_0 显示 | grep_existence | pass | 95% | `MainUITutorialPanel.cs` L95 `content = "Tutorial_text_0"` |

**推理**：Step 0 = FirstBattle。`StartMatchTutorial()` 获取 `TutorialUIEnum.BattleButton` 位置，以 `CircleAim_UI(param2=300)` 创建圆形镂空，`SetMaskColor(true)` 激活全屏蒙版。关卡选择界面的"默认选中第一关"概念在代码中通过直接引导到 BattleButton 实现，而非关卡列表选中。
**缺口**：需求描述的"关卡选择界面"与代码的"主界面战斗按钮引导"存在概念差异。⚠️ Tutorial_text_0 无配套音频。
**证据**：
- `MainUITutorialPanel.cs` L74-123 `StartMatchTutorial` — CircleAim_UI + SetMaskColor(true) + BattleButton 目标
- `MainUITutorialPanel.cs` L95 `Tutorial_text_0` 仅文本无音频

---

### 2. 新手引导 > 滑动弱引导 — partial (70%)

**契约ID**：qc-1747148000002
**规则**：Step 1 弱引导阶段，Tutorial_text_1 显示；玩家滑动后弱引导被隐藏
**判定**：partial | 置信度 70%

| SR | 描述 | 类型 | 判定 | 置信度 | 关键证据 |
|----|------|------|------|--------|---------|
| SR-1 | Tutorial_text_1 显示 | grep_existence | fail | 90% | 全库搜索 `Tutorial_text_1` 仅在 MainUITutorialPanel 中匹配到 Tutorial_text_10/11 等（substring），Tutorial_text_1 本身未被引用 |
| SR-2 | 滑动后隐藏 | llm_semantic | pass | 75% | `GameUI.cs` L467-471 `Destroy(mMoveTutorialGameObject)` 在 `isOpen=false` 时销毁 |

**推理**：MoveControl 步骤通过 `GameUI.cs` L490 加载独立预制体 `MoveTutorialPanel`，文本内容在 prefab 内定义，不在 C# 代码中引用 Tutorial_text_1。隐藏逻辑通过 `Destroy(mMoveTutorialGameObject)` 实现，触发时机为 `CHECK_MOVE_OPERATION_TUTORIAL_UI` 事件 isOpen=false。
**缺口**：Tutorial_text_1 key 未在 C# 代码中出现。实际文本可能嵌入 Unity prefab 中。
**证据**：
- `GameUI.cs` L80-82 MoveTutorialPanel 相关字段
- `GameUI.cs` L490 异步加载 "MoveTutorialPanel" prefab
- `GameUI.cs` L467-471 销毁 MoveTutorialPanel

---

### 3. 新手引导 > 蒙版射线穿透 — pass (92%)

**契约ID**：qc-1747148000003
**规则**：HoleMaskImage 实现镂空内外射线区分；步骤切换时更新镂空区域；蒙版可关闭
**判定**：pass | 置信度 92%

| SR | 描述 | 类型 | 判定 | 置信度 | 关键证据 |
|----|------|------|------|--------|---------|
| SR-1 | IsRaycastLocationValid 镂空内 false/外 true | llm_semantic | pass | 95% | `HoleMaskImage.cs` L16-34 完整实现 |
| SR-2 | 步骤切换时更新镂空区域 | grep_existence | pass | 90% | `GuideCutoutMaskController.cs` L192 `SetHoleFromTarget` |
| SR-3 | 蒙版关闭方法存在 | grep_existence | pass | 88% | `PlaneUITutorialPanel.cs` L131 `ClosePanel()` → `TutorialMask.gameObject.SetActive_Self(false)` |

**推理**：`HoleMaskImage.cs` 完整实现了 `IsRaycastLocationValid`：`!hasHole → return true`（全遮挡），`holeRect.Contains(localPoint) → return false`（镂空透过），否则 `return true`（遮挡）。`GuideCutoutMaskController` 提供更高级的镂空控制（矩形/圆形/椭圆）。`ClosePanel()` 在 PlaneUITutorialPanel.cs L131 关闭蒙版。
**证据**：
- `HoleMaskImage.cs` L16-34 — 完整的 IsRaycastLocationValid 实现
- `GuideCutoutMaskController.cs` L40 `SetHoleFromTarget` 位置同步注释
- `GuideCutoutMaskController.cs` L192 `SetHoleFromTarget(Canvas, RectTransform)`
- `PlaneUITutorialPanel.cs` L131 `mPlaneEditUI.TutorialMask.gameObject.SetActive_Self(false)`

---

### 4. 新手引导 > tips层级渲染 — partial (65%)

**契约ID**：qc-1747148000004
**规则**：tips 面板渲染层级高于蒙版；tips 关闭由引导步骤推进触发
**判定**：partial | 置信度 65%

| SR | 描述 | 类型 | 判定 | 置信度 | 关键证据 |
|----|------|------|------|--------|---------|
| SR-1 | tips 层级高于蒙版 | llm_semantic | inconclusive | 40% | 教程面板 `SetAsFirstSibling()` 设为最低层级，tips 理论上在其之上，但实际层级依赖 Prefab Canvas sortingOrder 配置 |
| SR-2 | tips 关闭由步骤推进触发 | llm_semantic | pass | 85% | `PlaneEditorManager.Operate.cs` L524 `CloseTips()` + `PlaneUITutorialPanel.cs` 各步骤的 `ClosePanel()` 调用 |

**推理**：`PlaneEditUI_Portrait.Tutorial.cs` L50 将 tutorial panel 设为 `SetAsFirstSibling()`（同级子节点中最低层级）。Tips 面板（PlanePartTipsPanel_Portrait）是 PlaneEditUI_Portrait 的另一个子物体，sibling index 更高则渲染更靠前。但 PlanePartInfoPanel 等具体 tips 弹出面板通过 GeneralUIController 管理，其层级取决于 Prefab Canvas sortingOrder 配置，代码中未找到 tips 面板显式设置高于蒙版 sortingOrder 的代码。
**勘误**：初始判定 pass 过于乐观。子 Agent 指出 SR-1 的层级关系依赖 Unity Prefab 配置而非代码显式设置，降级为 inconclusive。
**证据**：
- `PlaneEditUI_Portrait.Tutorial.cs` L50 `SetAsFirstSibling()` 教程面板设为最低
- `PlaneEditorManager.Operate.cs` L524 `PlaneEditUI_Portrait.Instance.CloseTips()`
- `PlaneUITutorialPanel.cs` L312-313 `SetMaskColor(false); SetCanvasGroup(false)` tips 模式

---

### 5. 新手引导 > 拖拽成功反馈 — partial (78%)

**契约ID**：qc-1747148000005
**规则**：拖拽成功后手势消失、文字保留、组件tips关闭、底版高亮持续
**判定**：partial | 置信度 78%

| SR | 描述 | 类型 | 判定 | 置信度 | 关键证据 |
|----|------|------|------|--------|---------|
| SR-1 | 手势提示消失 | llm_semantic | pass | 95% | `PlaneUITutorialPanel.cs` L274 `mMoveEffectGameObject.SetActive_Self(false)` |
| SR-2 | 文字提示保留 | llm_semantic | fail | 75% | `EndDragNormalPartTutorial` 调用 `ClosePanel()` 隐藏整个面板（含文字），与契约要求不符 |
| SR-3 | 组件 tips 关闭 | llm_semantic | pass | 85% | `PlaneEditorManager.Operate.cs` L524 `CloseTips()` + `ClosePanel()` |
| SR-4 | 底版高亮持续 | llm_semantic | pass | 80% | `mInstallVaildAreaGameObject` 在 CompletePartAssembly(tips) 实例化，持续到非tips时才 Destroy |

**推理**：`EndDragNormalPartTutorial()` (L274) 首先 `mMoveEffectGameObject.SetActive_Self(false)` 隐藏手势特效（SR-1 pass），然后 `ClosePanel()` 关闭整个教程面板（包含 AssistantPanel 文字提示）。文字在 EndDragNormalPartTutorial 到 CompletePartAssembly 之间存在不可见窗口期（SR-2 fail）。底版高亮通过 `mInstallVaildAreaGameObject`（CompletePartAssembly tips 模式 L321 实例化）持续显示直到组装完成时才 Destroy (L340)。
**勘误**：初始搜索遗漏了 `mMoveEffectGameObject`（手势特效）和 `mInstallVaildAreaGameObject`（底版高亮），子 Agent 在 PlaneUITutorialPanel.cs 中找到了正确证据。
**证据**：
- `PlaneUITutorialPanel.cs` L274 `mMoveEffectGameObject.SetActive_Self(false)` — 手势消失
- `PlaneUITutorialPanel.cs` L277 `ClosePanel()` — 文字也一并关闭（不符合"保留"要求）
- `PlaneUITutorialPanel.cs` L321 `hitInstallEffectGO = Instantiate(mInstallVaildAreaGameObject)` — 底版高亮
- `PlaneUITutorialPanel.cs` L340 `Destroy(hitInstallEffectGO)` — 仅组装完成后才销毁

---

### 6. 新手引导 > 拖拽失败回退 — partial (75%)

**契约ID**：qc-1747148000006
**规则**：拖拽失败时组件回退到背包原位；手势引导重新触发
**判定**：partial | 置信度 75%

| SR | 描述 | 类型 | 判定 | 置信度 | 关键证据 |
|----|------|------|------|--------|---------|
| SR-1 | 回退到背包原位 | llm_semantic | pass | 80% | `CPlanePartInfo.cs` L824 `ResetPart()` 恢复 parent/position/rotation/flip，L539 `Cancel()` 调用 |
| SR-2 | 手势引导重新触发 | llm_semantic | fail | 70% | 手势特效仅在 `StartDragNormalPartTutorial` 初始化时显示一次，无重播机制 |

**推理**：`CPlanePartInfo.Cancel()` (L539) 在非首次安装时调用 `ResetPart()` (L824)，将组件 parent 恢复到 `OriginaParentPos`，localPosition 恢复到 `LastTmpPos`，旋转和翻转也恢复到原始状态。但 `OnDrop` (L599) 仅重置拖拽标志位，不触发位置回退。手势引导仅在步骤初始化时通过 `mMoveEffectGameObject.SetActive_Self(true)` 显示一次，代码中无拖拽失败后重新触发的路径。
**勘误**：初始判定 fail 过于严格。子 Agent 发现 `ResetPart()` 提供了通用的组件位置恢复逻辑（通过 Cancel 操作触发），SR-1 升级为 pass。
**证据**：
- `CPlanePartInfo.cs` L824 `ResetPart()` — parent/position/rotation 恢复
- `CPlanePartInfo.cs` L539 `Cancel()` — 非首次安装时调用 ResetPart + Install
- `PlaneEditorManager.Operate.cs` L599 `OnDrop` — 仅重置拖拽标志，无自动回退

---

### 7. 新手引导 > 旋转翻转引导 — partial (75%)

**契约ID**：qc-1747148000007
**规则**：重叠位置时旋转翻转按钮高亮；显示 Tutorial_text_6；正确组装后关闭
**判定**：partial | 置信度 75%

| SR | 描述 | 类型 | 判定 | 置信度 | 关键证据 |
|----|------|------|------|--------|---------|
| SR-1 | 旋转翻转按钮高亮 | llm_semantic | fail | 75% | 旋转/翻转按钮在 `PlaneOperatePanel_Portrait.cs` 存在，但教程面板无高亮引导逻辑 |
| SR-2 | Tutorial_text_6 显示 | grep_existence | pass | 95% | `PlaneUITutorialPanel.cs` L306 用于 CompletePartAssembly(tips) |
| SR-3 | 正确组装后关闭 | llm_semantic | pass | 85% | `PlaneUITutorialPanel.cs` L334-340 `ClosePanel()` + `Destroy(hitInstallEffectGO)` |

**推理**：Tutorial_text_6 确实存在（L306），在 `StartCompletePartAssemblyTutorial` 的 tips 模式中使用。正确组装后（非tips回调）明确调用 `ClosePanel()`、`SetActive_Self(false)` 隐藏移动特效、`Destroy(hitInstallEffectGO)` 销毁安装区域特效。但旋转/翻转按钮（mBtnHorizontalFlip、mBtnRightRotate）虽在 `PlaneOperatePanel_Portrait.cs` L19/L21 存在，教程面板中搜索 Rotate/Flip/Highlight 均无命中。
**勘误**：初始判定 fail 过于严格。子 Agent 确认 SR-2/SR-3 通过（文本存在 + 关闭逻辑完整），仅 SR-1 高亮缺失。但注意 Tutorial_text_6 语义为「组装完成提示」而非「旋转翻转方向」，与契约描述可能不一致（需查 i18n 表确认）。
**证据**：
- `PlaneUITutorialPanel.cs` L306 `content = "Tutorial_text_6"` — 存在
- `PlaneUITutorialPanel.cs` L334-340 `ClosePanel()` + `Destroy(hitInstallEffectGO)` — 关闭完整
- `PlaneOperatePanel_Portrait.cs` L19/L21 旋转翻转按钮存在但无教程高亮

---

### 8. 新手引导 > 一键替换逻辑 — partial (75%)

**契约ID**：qc-1747148000008
**规则**：GetBetterSolution 按 Rarity 选取更高品质组件；高亮背包候选和飞船待替换组件
**判定**：partial | 置信度 75%

| SR | 描述 | 类型 | 判定 | 置信度 | 关键证据 |
|----|------|------|------|--------|---------|
| SR-1 | GetBetterSolution 按 Rarity 选取 | llm_semantic | pass | 90% | `PlaneEditorManager.Data.cs` L1329 `GetBetterSolution(CPlaneScheme)` |
| SR-2 | 高亮候选和待替换组件 | llm_semantic | fail | 85% | HighlightPart/ShowPartHighlight 全库 0 命中 |

**推理**：`GetBetterSolution` 方法在 PlaneEditorManager.Data.cs L1329 存在，接受方案参数返回替换字典。`ReplacePlanePart` 方法在 L670 有带 `TutorialStepEnum` 参数的重载。但组件高亮显示逻辑（视觉上标记候选和待替换组件）在代码中未找到。
**证据**：
- `PlaneEditorManager.Data.cs` L1329 `GetBetterSolution(CPlaneScheme)`
- `PlaneEditorManager.cs` L670 `ReplacePlanePart(CPlanePartData, CPlanePartInfo, TutorialStepEnum, Action)`

---

### 9. 新手引导 > 替换动画屏蔽 — partial (78%)

**契约ID**：qc-1747148000009
**规则**：替换时触发视觉特效或动画；替换期间存在操作屏蔽
**判定**：partial | 置信度 78%

| SR | 描述 | 类型 | 判定 | 置信度 | 关键证据 |
|----|------|------|------|--------|---------|
| SR-1 | 替换视觉特效/动画 | llm_semantic | pass | 85% | `PlaneEditorManager.cs` L689-692 `Effect_UI_SKinSuccess` 特效在教程步骤替换时加载并实例化 |
| SR-2 | 操作屏蔽机制 | llm_semantic | fail | 80% | BlockInput/LockInput/isReplacing 全库 0 命中。教程遮罩层可能间接提供部分屏蔽 |

**推理**：`ReplacePlanePart` (L670) 在创建替换组件回调中（L685-709），当 step 非 None 且非 CompletedSkillPartAssembly 时，加载并实例化 `Effect_UI_SKinSuccess` 特效到组件中心位置。教程步骤触发替换时（step=ReplacePart）会触发此特效。但不是「组件飞过去替换」的动画，是安装成功的静态特效。替换期间无显式操作屏蔽，教程流程中的遮罩层可能间接保护。
**勘误**：初始判定 fail 过于严格。子 Agent 发现 `Effect_UI_SKinSuccess` 替换特效确实存在（L692），SR-1 升级为 pass。
**证据**：
- `PlaneEditorManager.cs` L689 `if (step != TutorialStepEnum.None && step != CompletedSkillPartAssembly)` — 教程步骤条件
- `PlaneEditorManager.cs` L692 `AssetsLoad_Effect.Instance.GetAssetAsync("Effect_UI_SKinSuccess", ...)` — 特效加载
- `PlaneEditPanel_Portrait.cs` L506 `OnReplaceClick()` — 无输入屏蔽

---

### 10. 新手引导 > 替换后续引导 — partial (82%)

**契约ID**：qc-1747148000010
**规则**：替换完成后触发退出组装界面引导（弱引导）；推进到第二关
**判定**：partial | 置信度 82%

| SR | 描述 | 类型 | 判定 | 置信度 | 关键证据 |
|----|------|------|------|--------|---------|
| SR-1 | 退出组装界面引导 | llm_semantic | pass | 95% | `PlaneUITutorialPanel.cs` L382-385 替换按钮 onClick 执行后调用 `StartSoftBackFromPlaneTutorial()` |
| SR-2 | 推进到第二关（Tutorial_text_8） | grep_existence | partial | 75% | `MainUITutorialPanel.cs` L100 `Tutorial_text_8` 存在于 SecondBattle，但步骤从 ReplacePart→SecondBattle 的流转路径涉及中间步骤，完整性需运行时验证 |

**推理**：`StartReplacePartTutorial` 的回调（L382-385）先调用替换按钮 `onClick.Invoke()` 执行替换，然后立即调用 `StartSoftBackFromPlaneTutorial()`（L398）。该方法解除所有组件限制、获取 PlaneBackButton 位置、显示点击引导特效。Tutorial_text_8 存在于 `MainUITutorialPanel.cs` L100（SecondBattle 步骤），但从 ReplacePart 到 SecondBattle 的步骤流转涉及中间状态（SecondEdit 等），且 SecondBattle 在枚举定义中排在 ReplacePart 之前，完整步骤链需运行时验证。
**勘误**：初始判定 pass。子 Agent 指出 SR-2 的步骤流转完整性（ReplacePart→SecondBattle 之间的中间步骤跳转逻辑不完全明确）需运行时验证，降级为 partial。
**证据**：
- `PlaneUITutorialPanel.cs` L382 `buttonGameObject.GetComponent<Button>().onClick?.Invoke()` — 执行替换
- `PlaneUITutorialPanel.cs` L385 `StartSoftBackFromPlaneTutorial()` — 弱引导退出
- `MainUITutorialPanel.cs` L100 `Tutorial_text_8` — SecondBattle 步骤

---

### 11. 新手引导 > 激活组件放置 — partial (65%)

**契约ID**：qc-1747148000011
**规则**：激活组件仅可放置于预设合法格位；已占用格位扣除后高亮可用格位
**判定**：partial | 置信度 65%

| SR | 描述 | 类型 | 判定 | 置信度 | 关键证据 |
|----|------|------|------|--------|---------|
| SR-1 | 合法格位放置 | llm_semantic | partial | 70% | `EffectSlots` 定义格位坐标，`HasEffectPart` 校验放置有效性，但非显式合法格位列表而是 EffGrid 空间碰撞检测 |
| SR-2 | 高亮可用格位 | llm_semantic | inconclusive | 45% | `ShowEffGrid()` 显示全部 EffGrid 格位，是否动态过滤已占用格位需深入 `UpdateEffect()` 确认 |

**推理**：`EffectSlots` 由配置定义坐标（ExportConfig.cs L223），通过 `AddEffGrid` 添加到组件信息。教程步骤 FindValidPosition 中 `HasEffectPart` (PlaneOperatePanel_Portrait.cs L299-301) 用于校验组件是否正确放置到影响范围位置。但「预设4个特定合法格位」的限制不是显式列表，而是通过 EffGrid 与武器组件的空间重叠关系（`UpdateEffGrid → HasEffectPart`）隐式判定。`ShowEffGrid()` (CPlanePartInfo.cs L427) 显示所有 EffGrid 格位并调用 `UpdateEffGrid()`，但是否动态过滤已占用格位取决于 `PlaneEditHitGrid.UpdateEffect()` 的具体实现。
**勘误**：初始判定 pass 过于乐观。子 Agent 指出格位校验是间接的空间碰撞检测而非显式合法格位列表，降级 SR-1 为 partial。
**证据**：
- `PlaneEditorManager.cs` L370/L479 `EffectSlots` 遍历
- `PlaneOperatePanel_Portrait.cs` L299 `TutorialStepEnum.FindValidPosition && HasEffectPart` 校验
- `CPlanePartInfo.cs` L427 `ShowEffGrid()` — 显示全部 EffGrid
- `CPlanePartInfo.cs` L479 `UpdateEffGrid()` — 更新 HasEffectPart 状态

---

### 12. 新手引导 > 激活拖拽三状态 — fail (88%)

**契约ID**：qc-1747148000012
**规则**：不在合法格位显示 Tutorial_text_14；在合法格位未激活显示 Tutorial_text_12；成功激活显示 Tutorial_text_13；三状态循环切换
**判定**：fail | 置信度 88%

| SR | 描述 | 类型 | 判定 | 置信度 | 关键证据 |
|----|------|------|------|--------|---------|
| SR-1 | Tutorial_text_14 显示 | grep_existence | fail | 95% | Tutorial_text_14 全库 0 命中 |
| SR-2 | Tutorial_text_12 显示 | grep_existence | pass | 95% | `PlaneUITutorialPanel.cs` L527 |
| SR-3 | Tutorial_text_13 显示 | grep_existence | pass | 95% | `PlaneUITutorialPanel.cs` L559 |
| SR-4 | 三状态循环切换 | llm_semantic | fail | 90% | `OnExitTutorialArea` 方法体为空（PlaneEditorManager.Operate.cs L210-213） |

**推理**：Tutorial_text_12（FindValidPosition）和 Tutorial_text_13（CompletedSkillPartAssembly）在代码中存在。但 Tutorial_text_14（不在合法格位时的提示）全库未找到。更严重的是，`OnExitTutorialArea`（L210-213）方法体为空，意味着离开有效区域时不会切换回状态1，三状态循环无法实现。
**证据**：
- `PlaneEditorManager.Operate.cs` L210-213 `OnExitTutorialArea` 空方法
- `PlaneUITutorialPanel.cs` L527 Tutorial_text_12（FindValidPosition）
- `PlaneUITutorialPanel.cs` L559 Tutorial_text_13（CompletedSkillPartAssembly）

---

### 13. 新手引导 > 激活影响范围 — partial (85%)

**契约ID**：qc-1747148000013
**规则**：选中时 Effect 格位高亮；被影响组件增加特效；取消选中后消失
**判定**：partial | 置信度 85%

| SR | 描述 | 类型 | 判定 | 置信度 | 关键证据 |
|----|------|------|------|--------|---------|
| SR-1 | Effect 格位高亮 | llm_semantic | pass | 95% | `CPlanePartInfo.cs` L427 `ShowEffGrid()` 遍历 EffGridList 调用 `SetGrid(PlaneHitGrid.Effect)` |
| SR-2 | 被影响组件特效 | llm_semantic | partial | 60% | `PlaneEditHitGrid.cs` L60 `UpdateEffect()` 仅切换 Sprite（mValidSprite/mAreaSprite），无独立粒子特效 |
| SR-3 | 取消选中消失 | llm_semantic | pass | 95% | `CPlanePartInfo.cs` L439 `HideEffGrid()` + `PlaneEditorManager.Operate.cs` L269/L286 调用 |

**推理**：`ShowEffGrid()` 在 `CPlanePartInfo.cs` L427 定义，遍历 `EffGridList` 对每个格位调用 `SetGrid(PlaneHitGrid.Effect)` 设置高亮。`HideEffGrid()` L439 在取消选中/切换选中时被调用。`PlaneEditHitGrid.cs` L60 的 `UpdateEffect()` 根据格位是否有重叠组件切换 Sprite（mValidSprite vs mAreaSprite），这是视觉区分但非独立的"激活特效"。代码中未找到独立的粒子特效或动画效果组件。
**勘误**：初始判定 pass 过于乐观（SR-2 为 inconclusive），子 Agent 在 `CPlanePartInfo.cs` 和 `PlaneEditHitGrid.cs` 中找到了更精确的证据，明确 SR-2 为 partial。

---

### 14. 新手引导 > 按钮半径可配置 — pass (92%)

**契约ID**：qc-1747148000014
**规则**：旋转翻转按钮半径参数可配置
**判定**：pass | 置信度 92%

| SR | 描述 | 类型 | 判定 | 置信度 | 关键证据 |
|----|------|------|------|--------|---------|
| SR-1 | 半径参数可配置 | grep_existence | pass | 92% | `PlaneOperatePanel_Portrait.cs` L37 `[SerializeField] private float mMinRadius` + L234 `Mathf.Max(radius, mMinRadius)` |

**推理**：`mMinRadius` 在 `PlaneOperatePanel_Portrait.cs` L37 和 `PlaneOperatePanel.cs` L43 均以 `[SerializeField]` 标注，可在 Unity Inspector 中配置。`UpdateSize(float radius)` 方法使用 `Mathf.Max(radius, mMinRadius) + 10` 确保按钮区域不小于最小半径。`GuideCutoutMaskController.cs` 还提供了 `mCircleRadius` 和 `mCircleRadiusExtra` 用于遮罩引导圆的半径控制。
**勘误**：初始搜索范围未包含 `PlaneOperatePanel` 目录，导致误判为 fail。由子 Agent 在 `UI/SubUIPanel/PlaneEditUI/` 下发现正确证据。
**证据**：
- `PlaneOperatePanel_Portrait.cs` L37 `[SerializeField] private float mMinRadius`
- `PlaneOperatePanel_Portrait.cs` L234 `radius = Mathf.Max(radius, mMinRadius) + 10`
- `PlaneOperatePanel.cs` L43 `[SerializeField] private float mMinRadius`
- `GuideCutoutMaskController.cs` L57+L68 `mCircleRadius` / `mCircleRadiusExtra`

---

### 15. 新手引导 > 步骤持久化 — pass (92%)

**契约ID**：qc-1747148000015
**规则**：步骤推进使用递增比较防倒退；进度通过 PlayerPrefs 持久化；场景切换不重置
**判定**：pass | 置信度 92%

| SR | 描述 | 类型 | 判定 | 置信度 | 关键证据 |
|----|------|------|------|--------|---------|
| SR-1 | 递增比较防倒退 | llm_semantic | pass | 95% | `TutorialUtil.cs` L334 `if (progress > mTutorialProgress)` |
| SR-2 | PlayerPrefs 持久化 | grep_existence | pass | 95% | `TutorialUtil.cs` L346-354 `PlayerPrefs.SetInt / PlayerPrefs.Save()` |
| SR-3 | 场景切换不重置 | llm_semantic | pass | 85% | `TutorialUtil.cs` L160 从 `mTutorialProgress` 恢复 + PlayerPrefs 双保险 |

**推理**：`TutorialUtil.FinishTutorial(int progress)` (L330-361) 核心逻辑：
1. `if (progress > mTutorialProgress)` 确保只能递增，不能倒退
2. `AccessManager.Instance.SetTutorialProcess(progress, true)` 服务器端持久化
3. `PlayerPrefs.SetInt(key, 1)` + `PlayerPrefs.Save()` 本地持久化
4. 同时维护 `mFinishedTutorialList` 内存列表

场景切换后通过 `CheckMainPanelCurTutorialIn()` 从 `mTutorialProgress` 恢复状态。
**证据**：
- `TutorialUtil.cs` L334 `if (progress > UserDataManager.Instance.LocalUserData.mTutorialProgress)` — 递增防倒退
- `TutorialUtil.cs` L337 `AccessManager.Instance.SetTutorialProcess(progress, true)` — 服务器同步
- `TutorialUtil.cs` L346-354 `PlayerPrefs.SetInt/Save` — 本地持久化
- `TutorialUtil.cs` L348-352 `progress > oldProgress` 二次防倒退

---

### 16. 新手引导 > ID容错 — partial (75%)

**契约ID**：qc-1747148000016
**规则**：230068/230057 组件使用时有 null 检查
**判定**：partial | 置信度 75%

| SR | 描述 | 类型 | 判定 | 置信度 | 关键证据 |
|----|------|------|------|--------|---------|
| SR-1 | 230068 null 检查 | llm_semantic | partial | 75% | `TutorialUtil.cs` L213 有检查，但 `PlanePartSelPanel_Portrait.cs` L94 赋值无检查 |
| SR-2 | 230057 null 检查 | llm_semantic | partial | 75% | `TutorialUtil.cs` L233 有检查，但 `PlanePartSelPanel_Portrait.cs` L98 赋值无检查 |

**推理**：`TutorialUtil.cs` L213/L233 在使用 `Find(item => item.PartId == TUTORIAL_PART_ID_1/2)` 后立即做 `if (targetPart != null)` 检查并提供回退路径（回退到战斗步骤而非卡死）。但 `PlanePartSelPanel_Portrait.cs` 中直接赋值 `mTutorialPartId` 时无检查组件是否存在。核心路径有容错，但非所有使用点都有完备 null 保护。
**勘误**：初始判定 pass 过于乐观，子 Agent 发现 `PlanePartSelPanel_Portrait.cs` 中的未保护使用点。
**证据**：
- `TutorialUtil.cs` L213 `var targetPart = ...Find(...TUTORIAL_PART_ID_1); if (targetPart != null)` — 有回退
- `TutorialUtil.cs` L233 `var targetPart = ...Find(...TUTORIAL_PART_ID_2); if (targetPart != null)` — 有回退
- `PlanePartSelPanel_Portrait.cs` L94 `mTutorialPartId = PlaneUITutorialPanel.TUTORIAL_PART_ID_1` — 无检查
- `PlanePartSelPanel_Portrait.cs` L98 `mTutorialPartId = PlaneUITutorialPanel.TUTORIAL_PART_ID_2` — 无检查
- 风险：组件缺失时教程回退到战斗阶段是隐式容错，无明确错误提示给用户

---

### 17. 新手引导 > 弱引导退出 — pass (85%)

**契约ID**：qc-1747148000017
**规则**：替换完成后弱引导退出（解除限制+引导返回按钮）；教程进度推进至最终完成状态
**判定**：pass | 置信度 85%

| SR | 描述 | 类型 | 判定 | 置信度 | 关键证据 |
|----|------|------|------|--------|---------|
| SR-1 | 弱引导退出 | llm_semantic | pass | 88% | `PlaneUITutorialPanel.cs` L398-415 `StartSoftBackFromPlaneTutorial` |
| SR-2 | 进度推至完成 | grep_existence | pass | 90% | `PlaneEditorManager.cs` L412 检查 `CompletedSkillPartAssembly` |

**推理**：`StartSoftBackFromPlaneTutorial()` 完整实现了弱引导退出：
1. L403 `SetAllPartColliderState(true)` 解除所有组件限制
2. L406 获取 `PlaneBackButton` 位置
3. L412 显示点击引导特效
4. L414 隐藏 AssistantPanel（不遮挡）

`CompletedSkillPartAssembly` 的 `FinishAction` (L577-592) 调用 `FinishAndTutorialReport` 推进到最终状态。
**证据**：
- `PlaneUITutorialPanel.cs` L398-415 `StartSoftBackFromPlaneTutorial` 完整实现
- `PlaneUITutorialPanel.cs` L586-587 `FinishAndTutorialReport(FindValidPosition)` + `FinishAndTutorialReport(mTutorialStep)`
- `PlaneEditorManager.cs` L412 `CompletedSkillPartAssembly` 状态检查

---

### 18. 新手引导 > 文本语音配对 — partial (88%)

**契约ID**：qc-1747148000018
**规则**：Tutorial_text 系列文本 key 在引导步骤中被引用；Audio_Vos_Tutorial 系列语音文件被引用
**判定**：partial | 置信度 88%

| SR | 描述 | 类型 | 判定 | 置信度 | 关键证据 |
|----|------|------|------|--------|---------|
| SR-1 | Tutorial_text 被引用 | grep_existence | partial | 88% | 13/15 个 Tutorial_text_N key 被引用（缺 text_1 和 text_14），覆盖率 87% |
| SR-2 | Audio_Vos_Tutorial 被引用 | grep_existence | pass | 95% | 12/12 个 Audio_Vos_Tutorial 文件全部被引用 |

**推理**：Grep 确认 Tutorial_text_0~13（除1和14缺失）和 Audio_Vos_Tutorial_001~012 在以下三个文件中被引用：
- `PlaneUITutorialPanel.cs`: text_4/5/6/7/11/12/13 + audio_003~006/010~012
- `MainUITutorialPanel.cs`: text_0/3/8/10 + audio_002/007/009
- `SettlementUITutorialPanel.cs`: text_2/9 + audio_001/008

⚠️ **注意**：Tutorial_text_0（FirstBattle）无配套音频，Tutorial_text_1 和 Tutorial_text_14 在代码中不存在。
**证据**：
- `PlaneUITutorialPanel.cs` L215/255/306/364/441/481/527/559 — 7个 text key + 对应 audio
- `MainUITutorialPanel.cs` L95/100/146/152 — 4个 text key（text_0 无 audio）
- `SettlementUITutorialPanel.cs` L76/82 — 2个 text key + 对应 audio

---

## 附录

### A. 跨契约风险汇总

1. **TutorialManager.EnableTutorial 始终返回 false**（`TutorialManager.cs` L63-69）：这可能导致某些依赖此开关的教程逻辑被跳过。需确认是否为调试遗留或有意设计。

2. **OnExitTutorialArea 空方法**（`PlaneEditorManager.Operate.cs` L210-213）：影响契约 #12 的三状态循环。虽有调用点（L162），但方法体为空，等同于 no-op。

3. **Tutorial_text_1 和 Tutorial_text_14 缺失**：影响契约 #2 和 #12。MoveControl 使用独立 prefab 绕过了 text key 机制。text_14 完全缺失。

4. **Tutorial_text_0 无音频**：FirstBattle 步骤仅有文本引导，无语音配套。

### B. 文件索引

| 文件 | 行数 | 涉及契约 |
|------|------|---------|
| PlaneUITutorialPanel.cs | 601 | #1-7,10,12,17 |
| MainUITutorialPanel.cs | 177 | #1,10,18 |
| SettlementUITutorialPanel.cs | ~90 | #18 |
| TutorialUtil.cs | 363 | #1,2,15,16 |
| HoleMaskImage.cs | 36 | #3 |
| GuideCutoutMaskController.cs | 356 | #3,14 |
| PlaneEditorManager.cs | ~750 | #6,8,11,16,17 |
| PlaneEditorManager.Operate.cs | ~540 | #4,5,12,13 |
| PlaneEditorManager.Data.cs | ~1400 | #8,11,13,15 |
| PlaneEditUI_Portrait.Tutorial.cs | 169 | #4,10 |
| GameUI.cs | ~525 | #2 |
