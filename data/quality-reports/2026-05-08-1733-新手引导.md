# 质量契约走查报告

- 日期：2026-05-08 17:33
- 需求文档：新手引导-一测.docx
- 走查引擎：Cursor Agent（refreshSearch=true 全量重搜）

## 总览

| 指标 | 数量 |
|------|------|
| 契约总数 | 19 |
| 代码走查 (code_review) | 18 |
| 通过 (pass) | 8 |
| 违规 (fail) | 1 |
| 存疑 (uncertain) | 9 |
| 跳过 (非 code_review) | 1 |
| 调度模式 | 串行（单组）|
| ast-grep | 已启用（输出空，降级为 Grep+Read 模式）|
| Evidence 验证 | total=101, verified=81, failed=20（method_not_found）, downgraded=0 |
| 映射缓存(v2) | modules_updated=1, clusters_built=12 |

---

## 走查详情

### 1. 新手引导 > 关卡选择 — pass (80%)

**规则**：新手引导 Step 0 触发时，关卡选择界面进入强引导态：全屏蒙版激活（SetMaskColor(true)），BattleButton 位置设有圆形镂空高亮（CircleAim），非镂空区域点击被拦截；第一关是唯一可发起战斗的关卡
**边界提示**：实现方式为蒙版拦截而非 interactable=false；「默认选中第一关」指界面打开时战斗目标为第一关
**判定**：pass | 置信度 80%

**子要求判定**：
| SR | 描述 | 类型 | 判定 | 置信度 | 关键证据 |
|----|------|------|------|--------|---------|
| SR-1 | StartMatchTutorial 对 FirstBattle 调用 SetMaskColor(true) 并设 CircleAim | grep_existence | pass | 85% | `MainUITutorialPanel.cs` L74-123 `StartMatchTutorial()` |
| SR-2 | 强引导态下仅第一关可发起战斗 | llm_semantic | pass | 75% | `StarMapPoint.cs` L51-58 PreRequisiteLevel 锁定；ChapterPanel默认选中第一关 |

**汇总**：两个 SR 均 pass，整条 pass。
**推理**：`MainUITutorialPanel.StartMatchTutorial()` 在 FirstBattle 分支使用 `AssistantTypeEnum.CircleAim_UI` + `mAssistantPanel.SetMaskColor(true)` 实现全屏蒙版+圆形镂空；`SetCanvasGroup(blocksRaycasts=true)` 拦截非镂空区域点击；`ChapterPanel_MainPanel.UpdatePanel()` 初始化时设 `mCurrentLevelId=1` 默认第一关。
**证据**：
- `Assets/Scripts/ExternalClient/UI/SubUIPanel/TutorialUI/MainUITutorialPanel.cs` L74-123 `StartMatchTutorial` — CircleAim_UI+SetMaskColor(true)+BattleButton 目标
- `Assets/Scripts/ExternalClient/StarMap/StarMapPoint.cs` L51-58 `Init` — PreRequisiteLevel 锁定逻辑
**缺口**：无

---

### 2. 新手引导 > 武器自动战斗提示 — uncertain (55%)

**规则**：Step 1 弱引导阶段，Tutorial_text_1 在进入步骤时设置到持久文字区域（mContentText）显示；该文字提示存在隐藏触发机制
**边界提示**：需求文档未明确自动消失时机；Tutorial_text_14（浮动提示）与 Tutorial_text_1（持久底部文字）是两段独立提示
**判定**：uncertain | 置信度 55%

**子要求判定**：
| SR | 描述 | 类型 | 判定 | 置信度 | 关键证据 |
|----|------|------|------|--------|---------|
| SR-1 | Tutorial_text_1 在启动时被设置到 mContentText | grep_existence | pass | 80% | `MoveTutorialPanel.cs` L23 `mContentText.text = "Tutorial_text_1"` |
| SR-2 | 存在隐藏/清除 Tutorial_text_1 的触发逻辑 | llm_semantic | uncertain | 30% | 无对应隐藏逻辑 |

**汇总**：SR-2 uncertain 决定整条 uncertain。
**推理**：`MoveTutorialPanel.Start()` 直接设置 `mContentText.text`，但代码中无对应清除逻辑；仅浮动提示（Tutorial_text_14）通过 `DOFade` 在3秒后淡出，持久文字区域的隐藏时机不明。
**证据**：
- `Assets/Scripts/Client/BattleUI/TutorialPanel/MoveTutorialPanel.cs` L22-24 `Start` — mContentText.text 设置
**缺口**：HideContentText/ClearMoveTutorial/OnMoveComplete 等关键词全库0命中，持久文字隐藏机制不明

---

### 3. 新手引导 > 引导蒙版射线拦截 — pass (90%)

**规则**：HoleMaskImage.IsRaycastLocationValid 实现镂空内放行/镂空外拦截；SetHoleAtPosition 同步更新镂空区域；OnTutorialCloseHoleMask 禁用蒙版
**边界提示**：步骤切换时不得出现全屏遮挡的中间帧；hasHole=false 时全屏拦截
**判定**：pass | 置信度 90%

**子要求判定**：
| SR | 描述 | 类型 | 判定 | 置信度 | 关键证据 |
|----|------|------|------|--------|---------|
| SR-1 | IsRaycastLocationValid 实现镂空内 false/镂空外 true | llm_semantic | pass | 95% | `HoleMaskImage.cs` L16-34 |
| SR-2 | SetHoleAtPosition 步骤切换时被调用 | grep_existence | pass | 90% | `TutorialMask.cs` L44-73; `PlaneEditUI_Portrait.Tutorial.cs` L139-157 |
| SR-3 | OnTutorialCloseHoleMask 禁用蒙版 | grep_existence | pass | 90% | `TutorialMask.cs` L37-41 |

**汇总**：三个 SR 均 pass，整条 pass。
**推理**：`HoleMaskImage.IsRaycastLocationValid()` 代码逻辑完全正确：`!hasHole → return true`（全屏拦截），`holeRect.Contains(localPoint) → return false`（镂空放行），`else → return true`（镂空外拦截）。
**证据**：
- `Assets/Scripts/ExternalClient/UI/HoleMaskImage.cs` L16-34 `IsRaycastLocationValid` — 完整实现
- `Assets/Scripts/ExternalClient/Utils/TutorialMask.cs` L37-41 `OnTutorialCloseHoleMask` — 事件监听关闭
**缺口**：无
**附带发现**：
- [已确认] `OnExitTutorialArea()` 方法体为空（PlaneEditorManager.Operate.cs L210-213），步骤退出时蒙版不自动重置位置

---

### 4. 新手引导 > 组件tips层级 — uncertain (45%)

**规则**：Step 4/11 点击后弹出的 tips 面板渲染层级高于引导蒙版；tips 关闭由下一步引导步骤触发
**边界提示**：层级控制可通过 Canvas sortingOrder 或 SetSiblingIndex 实现
**判定**：uncertain | 置信度 45%

**子要求判定**：
| SR | 描述 | 类型 | 判定 | 置信度 | 关键证据 |
|----|------|------|------|--------|---------|
| SR-1 | tips 面板 sortingOrder 高于蒙版层 | llm_semantic | uncertain | 35% | 未找到教程蒙版 vs tips 的层级对比代码 |
| SR-2 | 教程步骤推进时触发 tips 关闭 | grep_existence | uncertain | 55% | ClosePartTip/HidePartTips 全库0命中 |

**汇总**：两个 SR 均 uncertain，整条 uncertain。
**推理**：`PlaneEditUI_Portrait.Tutorial.cs` L50 对教程面板调用 `SetAsFirstSibling()`（最底层），可能导致 tips 被教程蒙版遮挡，与「高于蒙版」要求相悖。tips 关闭机制通过 `LODDY_PLANE_PART_TIPS_CLOSE` 事件，但与教程步骤推进的连接逻辑未确认。
**证据**：
- `Assets/Scripts/ExternalClient/UI/UIPanel/PlaneEditUI_Portrait.Tutorial.cs` L50 `SetAsFirstSibling()` — 设为最低层级（潜在问题）
**缺口**：tips 面板相对教程蒙版的显式层级提升代码未找到；ClosePartTip/HidePartTips 全库0命中
**附带发现**：
- [待确认] `PlaneEditUI_Portrait.Tutorial.cs` L50 `SetAsFirstSibling()` 将教程面板置于最底层，可能导致 tips 面板在教程蒙版上方而非下方

---

### 5. 新手引导 > 拖拽安装-有效路径 — uncertain (45%)

**规则**：有效拖拽释放后：(A)手势消失；(B)Tutorial_text_6 保留不被 ClosePanel 关闭；(C)组件 tips 关闭；(D)黄色框高亮持续至落位确认
**边界提示**：EndDragNormalPartTutorial 调用 ClosePanel 存在关闭文字提示的风险
**判定**：uncertain | 置信度 45%

**子要求判定**：
| SR | 描述 | 类型 | 判定 | 置信度 | 关键证据 |
|----|------|------|------|--------|---------|
| SR-1 | 手势提示被隐藏 | grep_existence | pass | 75% | `PlaneUITutorialPanel.cs` L276 `mMoveEffectGameObject.SetActive_Self(false)` |
| SR-2 | Tutorial_text_6 不随 ClosePanel 消失 | llm_semantic | uncertain | 50% | ClosePanel 后重开新会话展示 text_6，是关闭后重开而非保留 |
| SR-3 | 拖拽完成后组件 tips 被关闭 | grep_existence | uncertain | 30% | ClosePartTip/HidePartTips 全库0命中 |
| SR-4 | 黄色框高亮持续至落位确认 | llm_semantic | pass | 80% | `PlaneUITutorialPanel.cs` L321-343 hitInstallEffectGO 落位后 Destroy |

**汇总**：SR-2 和 SR-3 uncertain 决定整条 uncertain。
**推理**：`EndDragNormalPartTutorial()` 调用 `ClosePanel()` 关闭面板后，`CheckNextTutorial()` 开新会话展示 Tutorial_text_6（不是保留，是重开），与契约的「保留」描述存在语义差异。组件 tips 关闭机制无直接代码证据。
**证据**：
- `Assets/Scripts/ExternalClient/UI/SubUIPanel/TutorialUI/PlaneUITutorialPanel.cs` L274-344 — 完整拖拽完成/失败分支
**缺口**：SR-3 ClosePartTip/HidePartTips 全库0命中

---

### 6. 新手引导 > 拖拽安装-失败回退 — uncertain (35%)

**规则**：拖拽至无效区域时，组件回退到背包原位，手势引导重新触发
**边界提示**：「回退原位」指组件恢复到背包中的原排列位置
**判定**：uncertain | 置信度 35%

**子要求判定**：
| SR | 描述 | 类型 | 判定 | 置信度 | 关键证据 |
|----|------|------|------|--------|---------|
| SR-1 | 无效区域释放组件回退背包原位 | llm_semantic | uncertain | 35% | ReturnToBag/DragFail/OnDragEnd.*invalid 全库0命中 |
| SR-2 | 回退后手势引导重新触发 | grep_existence | uncertain | 35% | ShowHandGuide/RestartGuide/ResetGesture 全库0命中 |

**汇总**：两个 SR 均 uncertain，整条 uncertain。
**推理**：代码未找到明确的"回退背包"逻辑；当 `TUTORIAL_IS_COMPLETED(DragNormalPart)` 未触发（未进入 tutorialValidArea）时，`EndDragNormalPartTutorial()` 不会被调用，移动特效保持可见（手势引导持续存在），但这是被动保持而非主动重置，与契约要求的「重新触发」存在差异。
**缺口**：ReturnToBag/ResetPartPosition/DragFail/BackToBag/ShowHandGuide/RestartGuide 等关键词全库0命中

---

### 7. 新手引导 > 重叠位置提示 — uncertain (50%)

**规则**：玩家首次将组件拖至与已安装组件重叠位置时，旋转和翻转按钮高亮并显示 Tutorial_text_6；单次触发标志 mIsShowedTips
**边界提示**：单次触发标志设置后后续重叠不再重复弹出
**判定**：uncertain | 置信度 50%

**子要求判定**：
| SR | 描述 | 类型 | 判定 | 置信度 | 关键证据 |
|----|------|------|------|--------|---------|
| SR-1 | 重叠后旋转/翻转按钮高亮 | grep_existence | uncertain | 30% | HighlightRotate/BtnRotate.*highlight/FlipButton.*glow 全库0命中 |
| SR-2 | mIsShowedTips 单次触发标志存在 | grep_existence | pass | 80% | `PlaneUITutorialPanel.cs` L46 字段存在，L292-295 使用 |

**汇总**：SR-1 uncertain 决定整条 uncertain。
**推理**：`mIsShowedTips` 字段和使用逻辑存在，但旋转/翻转按钮高亮的具体实现未找到；`StartCompletePartAssemblyTutorial()` 的 `isTips=true` 分支使用此标志展示 Tutorial_text_6，但与「组件拖至重叠位置」的连接路径不明。
**证据**：
- `Assets/Scripts/ExternalClient/UI/SubUIPanel/TutorialUI/PlaneUITutorialPanel.cs` L46/291-295 `mIsShowedTips`
**缺口**：HighlightRotate/BtnRotate.*highlight/ShowRotateTip/FlipButton.*glow 全库0命中；重叠检测触发路径不明

---

### 8. 新手引导 > 一键替换逻辑 — uncertain (50%)

**规则**：GetBetterSolution 按 Rarity 匹配同 PartId 背包组件执行替换；同时高亮背包候选组件和飞船待替换组件
**边界提示**：210010 由 GetBetterSolution 数据驱动匹配，非硬编码
**判定**：uncertain | 置信度 50%

**子要求判定**：
| SR | 描述 | 类型 | 判定 | 置信度 | 关键证据 |
|----|------|------|------|--------|---------|
| SR-1 | GetBetterSolution 按 Rarity 匹配替换 | llm_semantic | pass | 90% | `PlaneEditorManager.Data.cs` L1363-1430 完整实现 |
| SR-2 | 背包候选组件被高亮 | grep_existence | uncertain | 40% | SetReplacePart() 创建克隆体，非直接高亮 |
| SR-3 | 飞船待替换组件被高亮 | grep_existence | uncertain | 30% | HighlightPlanePart/PlaneSlot.*highlight 全库0命中 |

**汇总**：SR-2 和 SR-3 uncertain 决定整条 uncertain。
**推理**：`GetBetterSolution()` 按 PartId 分组、Rarity 升序/降序双排序匹配，逻辑正确。但背包高亮为克隆体展示（SetReplacePart），飞船侧无高亮证据。
**证据**：
- `Assets/Scripts/ExternalClient/Managers/PlaneEditorManager/PlaneEditorManager.Data.cs` L1363-1430 `GetBetterSolution`
- `Assets/Scripts/ExternalClient/UI/SubUIPanel/PlaneEditUI/PlaneEditPanel_Portrait.cs` L430-436 `SetReplacePart`
**缺口**：飞船待替换组件高亮（SR-3）全库0命中

---

### 9. 新手引导 > 替换动画反馈 — fail (85%)

**规则**：点击一键替换后：(A)新组件飞入动画；(B)被替换组件飞出/特效；(C)动画期间输入屏蔽
**边界提示**：原代码 ReplacePlanePart 直接创建新部件无动画，待确认是否已补充
**判定**：fail | 置信度 85%

**子要求判定**：
| SR | 描述 | 类型 | 判定 | 置信度 | 关键证据 |
|----|------|------|------|--------|---------|
| SR-1 | 新组件飞入动画（DOMove/TweenFly/FlyToSlot） | grep_existence | fail | 90% | DOMove 仅在 DOTween 库文件存在，业务代码0命中 |
| SR-2 | 被替换组件飞出/特效 | grep_existence | fail | 90% | FlyToBag/ReplaceEffect/PartFlyOut 全库0命中 |
| SR-3 | 动画期间输入屏蔽 | grep_existence | fail | 90% | BlockInput/DisableInteraction/LockInput 全库0命中 |

**汇总**：三个 SR 均 fail，整条 fail。
**推理**：走查确认 boundary hint 描述准确——`ReplacePlanePart()` 直接同步替换，无飞入飞出动画，无输入屏蔽。DOTween 库虽已引入但未用于替换流程。**此为P1级违规，替换视觉反馈缺失。**
**证据**：
- `Assets/Scripts/ExternalClient/Managers/PlaneEditorManager/PlaneEditorManager.cs` L670-715 `ReplacePlanePart` — 直接替换无动画
**缺口**：无（已确认缺失）
**附带发现**：
- [已确认] `ReplacePlanePart(src, des, step, callBack)` 直接通过 `CreateReplacePlanePart()` 创建新部件，callback 完成后自动保存，全程无动画和输入屏蔽

---

### 10. 新手引导 > 替换完成后续流程 — pass (85%)

**规则**：替换完成后引导退出组装界面（弱引导）；退出后引导进入第二关（Tutorial_text_8）
**边界提示**：代码使用 StartSoftBackFromPlaneTutorial（弱引导），与原文「强引导」描述不同，确认为预期实现
**判定**：pass | 置信度 85%

**子要求判定**：
| SR | 描述 | 类型 | 判定 | 置信度 | 关键证据 |
|----|------|------|------|--------|---------|
| SR-1 | 替换完成后触发引导退出 | grep_existence | pass | 90% | `PlaneUITutorialPanel.cs` L385 `StartSoftBackFromPlaneTutorial()` |
| SR-2 | 退出后推进到 Tutorial_text_8 第二关引导 | grep_existence | pass | 85% | `MainUITutorialPanel.cs` L99-101 SecondBattle 使用 Tutorial_text_8 |

**汇总**：两个 SR 均 pass，整条 pass。
**证据**：
- `Assets/Scripts/ExternalClient/UI/SubUIPanel/TutorialUI/PlaneUITutorialPanel.cs` L383-415 — 替换回调→弱引导退出
- `Assets/Scripts/ExternalClient/UI/SubUIPanel/TutorialUI/MainUITutorialPanel.cs` L99-101 — Tutorial_text_8 SecondBattle

---

### 11. 新手引导 > 关卡绑定 — pass (85%)

**规则**：第二关锁定至第一关通关；两个教学关卡均通过 ChallengeBattleStage 发起战斗
**边界提示**：代码可能仅为单重 PreRequisiteLevel，非双重绑定
**判定**：pass | 置信度 85%

**子要求判定**：
| SR | 描述 | 类型 | 判定 | 置信度 | 关键证据 |
|----|------|------|------|--------|---------|
| SR-1 | PreRequisiteLevel 阻止第一关未完成时进入第二关 | grep_existence | pass | 90% | `StarMapPoint.cs` L51-58 PreRequisiteLevel 锁定 |
| SR-2 | 两个关卡均通过 ChallengeBattleStage 发起 | grep_existence | pass | 85% | `ChapterPanel_MainPanel.cs` L260 统一调用 |

**汇总**：两个 SR 均 pass，整条 pass。
**证据**：
- `Assets/Scripts/ExternalClient/StarMap/StarMapPoint.cs` L51-58
- `Assets/Scripts/ExternalClient/UI/SubUIPanel/MainUI/ChapterPanel_MainPanel.cs` L260
**缺口**：「教程步骤进度双重绑定」的第二重检查未找到，仅单重 PreRequisiteLevel，与 boundary hint 一致

---

### 12. 新手引导 > 激活组件放置 — uncertain (40%)

**规则**：激活类组件仅可放置于最多4个特定合法格位；占用格位动态排除（4-N显示）
**边界提示**：格位判断基于格子坐标而非矩形区域检测
**判定**：uncertain | 置信度 40%

**子要求判定**：
| SR | 描述 | 类型 | 判定 | 置信度 | 关键证据 |
|----|------|------|------|--------|---------|
| SR-1 | 4个特定合法格位坐标/ID定义 | grep_existence | uncertain | 40% | mValidArea_Tutorial 是 RectTransform（矩形区域），非格位列表 |
| SR-2 | 占用格位从合法列表排除（4-N） | llm_semantic | uncertain | 35% | CheckOccupied/RemoveOccupied/GetAvailableGrid 全库0命中 |

**汇总**：两个 SR 均 uncertain，整条 uncertain。
**推理**：`mValidArea_Tutorial`（RectTransform）实现矩形区域约束，不是「特定4格坐标列表」；占用格位动态排除逻辑未找到。
**缺口**：tutorialValidArea 以格位坐标形式定义4个特定位置的证据缺失；占用格位动态排除逻辑缺失

---

### 13. 新手引导 > 激活组件拖拽三状态 — uncertain (55%)

**规则**：三状态循环：(1)不在合法格位→Tutorial_text_11；(2)合法但未激活→Tutorial_text_12；(3)激活成功→Tutorial_text_13；可回切至(1)
**边界提示**：三状态须为循环而非线性
**判定**：uncertain | 置信度 55%

**子要求判定**：
| SR | 描述 | 类型 | 判定 | 置信度 | 关键证据 |
|----|------|------|------|--------|---------|
| SR-1 | 状态(1): Tutorial_text_11 显示 | grep_existence | pass | 85% | `PlaneUITutorialPanel.cs` L481 |
| SR-2 | 状态(2): Tutorial_text_12 显示 | grep_existence | pass | 85% | `PlaneUITutorialPanel.cs` L527 |
| SR-3 | 状态(3): Tutorial_text_13 显示 | grep_existence | pass | 85% | `PlaneUITutorialPanel.cs` L559 |
| SR-4 | 移出合法区域后状态回切至(1) | llm_semantic | uncertain | 20% | OnExitTutorialArea() 方法体为空 |

**汇总**：SR-4 uncertain 决定整条 uncertain。
**推理**：三个状态文字均已实现，但 `OnExitTutorialArea()` 方法体为空，组件移出合法区域后无回切逻辑；`OnEnterTutorialArea` 的调用被注释掉（L155-158），循环状态机不完整。
**证据**：
- `Assets/Scripts/ExternalClient/UI/SubUIPanel/TutorialUI/PlaneUITutorialPanel.cs` L481/527/559 — 三状态文字
- `Assets/Scripts/ExternalClient/Managers/PlaneEditorManager/PlaneEditorManager.Operate.cs` L210-213 — OnExitTutorialArea 空实现
**缺口**：无
**附带发现**：
- [已确认] `OnExitTutorialArea()` 方法体为空，三状态循环未完整实现，移出后不回切至状态(1)
- [已确认] `CheckInValidArea()` 中 `OnEnterTutorialArea` 调用被注释（L155-158），进入检测路径存疑

---

### 14. 新手引导 > 激活影响显示 — pass (75%)

**规则**：选中激活组件时 ShowEffGrid 显示影响范围；被影响组件增加激活特效（ShowActivateEffect/EdgeLight）；取消选中后 HideEffGrid 立即清除
**边界提示**：拖拽过程中影响范围随组件位置实时更新
**判定**：pass | 置信度 75%

**子要求判定**：
| SR | 描述 | 类型 | 判定 | 置信度 | 关键证据 |
|----|------|------|------|--------|---------|
| SR-1 | ShowEffGrid 在选中时调用 | grep_existence | pass | 85% | `PlaneOperatePanel_Portrait.cs` L80; `CPlanePartInfo.cs` L427 |
| SR-2 | 被影响组件增加激活特效（EdgeLight） | grep_existence | pass | 70% | `PlanePartEdgeLight.cs` 存在；`CPlanePartInfo.cs` UpdateEffGrid 调用链 |
| SR-3 | HideEffGrid 取消选中后立即清除 | grep_existence | pass | 85% | `PlaneEditorManager.Operate.cs` L269/286 |

**汇总**：三个 SR 均 pass，整条 pass。
**证据**：
- `Assets/Scripts/ExternalClient/UI/SubUIPanel/PlaneEditUI/CPlanePartInfo.cs` L427/439/479 — ShowEffGrid/HideEffGrid/UpdateEffGrid
- `Assets/Scripts/ExternalClient/UI/SubUIPanel/PlaneEditUI/PlaneOperatePanel_Portrait.cs` L80 — ShowEffGrid 调用时机

---

### 15. 新手引导 > 激活按钮半径配置 — uncertain (50%)

**规则**：按钮点击半径参数（mMinRadius 或等效）考虑到 Effect 影响范围，或为独立可配置参数（非硬编码）
**边界提示**：影响位置不应超出按钮点击半径覆盖范围
**判定**：uncertain | 置信度 50%

**子要求判定**：
| SR | 描述 | 类型 | 判定 | 置信度 | 关键证据 |
|----|------|------|------|--------|---------|
| SR-1 | mMinRadius 存在且为可配置值 | grep_existence | pass | 85% | `PlaneOperatePanel_Portrait.cs` L37 `[SerializeField] private float mMinRadius` |
| SR-2 | mMinRadius 与 Effect 影响范围关联 | llm_semantic | uncertain | 30% | mMinRadius 仅作最小半径下限，无与 Effect 数据挂钩的逻辑 |

**汇总**：SR-2 uncertain 决定整条 uncertain。
**证据**：
- `Assets/Scripts/ExternalClient/UI/SubUIPanel/PlaneEditUI/PlaneOperatePanel_Portrait.cs` L37/234 — mMinRadius SerializeField 字段
**缺口**：mMinRadius 与 Effect 影响范围的关联逻辑（mMinRadius.*Effect/EffectRange.*radius）全库0命中

---

### 16. 新手引导 > 引导步骤顺序与持久化 — pass (95%)

**规则**：步骤按枚举顺序线性执行，progress > mTutorialProgress 防止倒退；双重持久化（服务端+PlayerPrefs）
**边界提示**：相等或更大时才推进
**判定**：pass | 置信度 95%

**子要求判定**：
| SR | 描述 | 类型 | 判定 | 置信度 | 关键证据 |
|----|------|------|------|--------|---------|
| SR-1 | progress > mTutorialProgress 比较防倒退 | grep_existence | pass | 95% | `TutorialUtil.cs` L334; `UserData.PlayerInfo.cs` L116 |
| SR-2 | SetTutorialProcess 服务端接口调用 | grep_existence | pass | 95% | `TutorialUtil.cs` L337; `AccessManager.Query.cs` L14 |
| SR-3 | PlayerPrefs 本地备份写入 | grep_existence | pass | 95% | `TutorialUtil.cs` L345-354 SetInt+Save |

**汇总**：三个 SR 均 pass，整条 pass。
**证据**：
- `Assets/Scripts/ExternalClient/Utils/TutorialUtil.cs` L330-361 `FinishTutorial` — 完整双存储实现

---

### 17. 新手引导 > 固定组件ID容错 — pass (90%)

**规则**：230068 和 230057 各自执行 null 检查，组件缺失时有容错路径（回退前置步骤或跳过）
**边界提示**：容错路径为回退到战斗步骤或静默跳过
**判定**：pass | 置信度 90%

**子要求判定**：
| SR | 描述 | 类型 | 判定 | 置信度 | 关键证据 |
|----|------|------|------|--------|---------|
| SR-1 | 230068 使用处有 null 检查+容错路径 | llm_semantic | pass | 90% | `TutorialUtil.cs` L213-219: null 时回退 FirstBattle |
| SR-2 | 230057 使用处有 null 检查+容错路径 | llm_semantic | pass | 90% | `TutorialUtil.cs` L233-239: null 时回退 SecondBattle |

**汇总**：两个 SR 均 pass，整条 pass。
**证据**：
- `Assets/Scripts/ExternalClient/Utils/TutorialUtil.cs` L213-219/233-239 `CheckMainPanelCurTutorialIn` — null 检查+回退

---

### 18. 新手引导 > 弱引导退出 — pass (75%)

**规则**：激活成功后触发弱引导退出（anyClick/任意操作）；进度推进至 CompletedSkillPartAssembly
**边界提示**：弱引导无强制点击目标，与强引导蒙版不同
**判定**：pass | 置信度 75%

**子要求判定**：
| SR | 描述 | 类型 | 判定 | 置信度 | 关键证据 |
|----|------|------|------|--------|---------|
| SR-1 | 激活成功后弱引导退出（anyClick 等价行为） | grep_existence | pass | 75% | `PlaneUITutorialPanel.cs` L565-593: 全屏 AssistantPanel 点击任意处触发 FinishAction |
| SR-2 | 弱引导退出后推进至 CompletedSkillPartAssembly | grep_existence | pass | 85% | `PlaneUITutorialPanel.cs` L586-587: FinishAndTutorialReport(CompletedSkillPartAssembly) |

**汇总**：两个 SR 均 pass，整条 pass。
**证据**：
- `Assets/Scripts/ExternalClient/UI/SubUIPanel/TutorialUI/PlaneUITutorialPanel.cs` L565-593 `StartCompletedSkillPartAssemblyTutorial` + FinishAction

---

## 未走查契约

| moduleLabel | rule | verifyMethods | 跳过原因 |
|-------------|------|---------------|----------|
| 新手引导 > 移动教学 | Step 1 弱引导阶段，MoveTutorialPanel 展示移动操作教程，玩家完成移动操作后步骤推进 | ui_test | 运行时验收，本次仅执行代码走查 |

---

## 问题汇总（P0/P1 级别）

| 契约 | 级别 | 类型 | 问题描述 |
|------|------|------|----------|
| qc-1746685810 | P1 | **fail** | 替换动画完全缺失：ReplacePlanePart 直接同步替换，无飞入/飞出动画，无输入屏蔽 |
| qc-1746685814 | P0 | uncertain | OnExitTutorialArea() 为空实现，三状态循环不完整，移出合法区域不回切至状态(1) |
| qc-1746685813 | P0 | uncertain | 合法放置区域为矩形 RectTransform 而非「4个特定格位坐标列表」，占用格位动态排除逻辑未找到 |
| qc-1746685807 | P0 | uncertain | 拖拽失败回退背包逻辑未找到 |
| qc-1746685805 | P0 | uncertain | 组件 tips 关闭机制（SR-3）未找到；Tutorial_text_6「保留」实为关闭后重开 |
| qc-1746685804 | P0（附带发现）| 风险 | OnExitTutorialArea() 空实现可能导致步骤退出时蒙版位置不重置 |
| qc-1746685805 | P1（附带发现）| 风险 | SetAsFirstSibling() 将教程面板置于最低层，tips 可能被蒙版遮挡 |
