# 质量契约走查报告

- 日期：2026-05-12 11:18
- 需求文档：新手引导-一测.docx
- 走查引擎：Cursor Agent

## 总览

| 指标 | 数量 |
|------|------|
| 契约总数 | 19 |
| 代码走查 (code_review) | 19 |
| 通过 (pass) | 5 |
| 违规 (fail) | 3 |
| 存疑 (uncertain) | 11 |
| 跳过 (非 code_review) | 0 |
| 调度模式 | 并行（3组） |
| ast-grep | 降级（sg 进程挂起已终止） |
| Evidence 验证 | total=19, verified=0, failed=0, downgraded=0（本次未运行验证脚本） |
| 映射缓存(v2) | 本次未更新 |

## 走查详情

### 1. 新手引导 > 关卡选择 — ⚠️ uncertain (45%)

**规则**：新手引导 Step 0 触发时，关卡选择界面默认聚焦第一关，且第一关是唯一可发起战斗的关卡；其余关卡按钮不可交互

**边界提示**：需确保默认选中逻辑与关卡解锁数据一致；如果玩家已通关第一关则此约束可能不再生效

**判定**：uncertain | 置信度 45%

**子要求判定**：

| SR | 描述 | 判定 | 置信度 | 关键证据 |
|----|------|------|--------|---------|
| SR-1 |  | uncertain | 40% | StarMapManager初始化时自动SelectLevel(mInitLevelId)，但无法确认mInitLevelId在教程期间强制为第一关 [缺口: 未找到教程期间强制聚焦第一关的代码路径] |
| SR-2 |  | pass | 65% | StarMapPoint.Init中通过PreRequisiteLevel机制锁定未通关前置关卡，锁定后mBattleButtonCanvasGroup.Set |

**推理**：代码中 StarMapPoint.cs 通过 PreRequisiteLevel 判定关卡是否锁定，SelectLevel 方法中通过 mBattleButtonCanvasGroup.SetCanvasGroupActive(!isLocked) 控制战斗按钮可交互性——锁定关卡的战斗按钮不可用。StarMapManager 初始化时会 SelectLevel(mInitLevelId 对应的 starMapPoint)，但未发现教程期间「强制默认聚焦第一关」或「除第一关外其余关卡按钮不可交互」的专用教程逻辑。关卡锁定依赖通关数据(CheckBattleStageFinish)，新玩家尚未通关第一关前，后续关卡理论上均会被锁（PreRequisiteLevel 机制），但缺少代码中对此场景的显式教程约束。

**证据**：
- `Assets/Scripts/ExternalClient/StarMap/StarMapPoint.cs` L51-58 `Init` — PreRequisiteLevel==0时解锁，否则检查CheckBattleStageFinish判定锁定状态
- `Assets/Scripts/ExternalClient/UI/SubUIPanel/MainUI/ChapterPanel_MainPanel.cs` L508-513 `SelectLevel` — mBattleButtonCanvasGroup.SetCanvasGroupActive(!isLocked)控制战斗按钮可交互
- `Assets/Scripts/ExternalClient/StarMap/StarMapManager.cs` L93-99 `LoadPlanet callback` — 初始化时SelectLevel(mInitLevelId对应的starMapPoint)，自动选中一个关卡

**缺口**：未发现教程期间强制默认选中第一关的专用逻辑（如SetDefaultSelect或TutorialSelectFirstLevel），默认选中依赖mInitLevelId的值但该值来源未追踪到教程配置

**附带发现**：

| 级别 | 问题 |
|------|------|
| [info] | 关卡锁定机制基于PreRequisiteLevel通关检查，新玩家理论上只有第一关解锁，间接满足了SR-2的条件 |

---

### 2. 新手引导 > 移动教学 — ⚠️ uncertain (40%)

**规则**：Step 1 弱引导阶段，玩家执行滑动屏幕操作后 Tutorial_text_1（「滑动屏幕控制战舰移动」）提示立即隐藏，且同一教程周期内不再重复显示

**边界提示**：「立即隐藏」指当帧或下一帧执行 Hide/SetActive(false)，不含延迟动画；「不再重复」指同一次教程流程中该提示只显示一次

**判定**：uncertain | 置信度 40%

**子要求判定**：

| SR | 描述 | 判定 | 置信度 | 关键证据 |
|----|------|------|--------|---------|
| SR-1 |  | uncertain | 35% | 未发现滑动事件驱动隐藏Tutorial_text_1的代码路径，mContentText无SetActive(false)调用 [缺口: 全文件无OnSlide/OnDrag回调，无针对mContentText的隐藏逻] |
| SR-2 |  | fail | 75% | 全库搜索hasShown/isShown/ShowOnce/mHasShown均0命中，MoveTutorialPanel无防重复显示标记 |

**推理**：MoveTutorialPanel.cs 中 mContentText 在 Start() 时设置为 Tutorial_text_1 的本地化文本，但该面板没有任何关于「滑动后隐藏 mContentText」或「防止重复显示」的逻辑。面板本身无 OnSlide/OnDrag 回调、无 hasShown/isShown 标记。全库搜索 hasShown/isShown/ShowOnce/mHasShown 均无命中。推测 Tutorial_text_1 的隐藏依赖面板级别的 Destroy/SetActive(false)（随步骤推进整体销毁），但非「滑动后立即隐藏」的语义。

**证据**：
- `Assets/Scripts/Client/BattleUI/TutorialPanel/MoveTutorialPanel.cs` L23 `Start` — mContentText.text = Tutorial_text_1.GetLocaliztion(); 设置文字
- `Assets/Scripts/Client/BattleUI/TutorialPanel/MoveTutorialPanel.cs` L37-48 `ShowTipUI` — ShowTipUI通过DOFade实现淡出，但仅用于Tutorial_text_14，非mContentText

**缺口**：未找到玩家滑动后隐藏Tutorial_text_1的代码逻辑；未找到防重复显示标记(hasShown等)；搜索hasShown/isShown/ShowOnce/mHasShown全库0命中

**附带发现**：

| 级别 | 问题 |
|------|------|
| [info] | Tutorial_text_14 有自动淡出逻辑(DOFade 3s)，但Tutorial_text_1 作为 mContentText 无独立隐藏机制 |

---

### 3. 新手引导 > 武器自动战斗提示 — ✅ pass (75%)

**规则**：Step 1 弱引导阶段，「武器会自动寻敌射击，无需操作」提示在进入步骤时显示，该提示不依赖玩家手动操作即可自动消失或随步骤推进而隐藏

**边界提示**：该提示可与 Tutorial_text_1 共存；消失机制可以是定时器、步骤推进回调或事件驱动

**判定**：pass | 置信度 75%

**推理**：MoveTutorialPanel.cs 的 Start() 方法中调用 ShowTipUI('Tutorial_text_14', 3f)，该方法实现为：设置 alpha=1 → DOFade(0, 0.5f).SetDelay(3f)，即进入步骤时自动显示，3秒后自动淡出到透明。不依赖玩家手动操作（纯时间驱动的动画）。

**证据**：
- `Assets/Scripts/Client/BattleUI/TutorialPanel/MoveTutorialPanel.cs` L24 `Start` — ShowTipUI('Tutorial_text_14', 3f) — 进入步骤时调用
- `Assets/Scripts/Client/BattleUI/TutorialPanel/MoveTutorialPanel.cs` L40-41 `ShowTipUI` — mTipsCanvasGroup.alpha=1f; mTweenerAlpha = mTipsCanvasGroup.DOFade(0, 0.5f).SetDelay(duration) — 延迟duration秒后0.5秒淡出

**附带发现**：

| 级别 | 问题 |
|------|------|
| [info] | 提示内容为Tutorial_text_14的本地化文本，通过CanvasGroup alpha控制可见性 |

---

### 4. 新手引导 > 引导蒙版射线拦截 — ✅ pass (90%)

**规则**：强引导进行中，HoleMaskImage.IsRaycastLocationValid 实现「镂空内放行（返回 false）、镂空外拦截（返回 true）」逻辑；步骤切换时 SetHoleAtPosition 同步更新镂空区域位置；蒙版关闭时通过 OnTutorialCloseHoleMask 或等效方法禁用蒙版

**边界提示**：注意 IsRaycastLocationValid 返回 true 表示该位置是有效的射线检测目标（即蒙版拦截了该点击），返回 false 表示射线穿透（镂空区域放行）

**判定**：pass | 置信度 90%

**子要求判定**：

| SR | 描述 | 判定 | 置信度 | 关键证据 |
|----|------|------|--------|---------|
| SR-1 |  | pass | 95% | HoleMaskImage.cs L16-34: holeRect.Contains → false(放行), else → true(拦截) |
| SR-2 |  | pass | 90% | PlaneEditUI_Portrait.Tutorial.cs L151+L156调用SetHoleAtPosition；CheckNextTutorial中 |
| SR-3 |  | pass | 95% | TutorialMask.cs L36-42: OnTutorialCloseHoleMask → ClearHole() + gameObject.SetAc |

**推理**：三个SR均有充分代码证据。(1) HoleMaskImage.IsRaycastLocationValid实现：hasHole=true时，holeRect.Contains(localPoint)为true返回false(放行)，否则返回true(拦截)；(2) SetHoleAtPosition在PlaneEditUI_Portrait.Tutorial.cs的SetMaskHoleAtPosition中被调用，步骤切换时通过CheckNextTutorial→OpenPlaneUITutorial→各步骤教程触发蒙版更新；(3) OnTutorialCloseHoleMask通过事件TUTORIAL_CLOSE_HOLE_MASK触发，执行ClearHole()+gameObject.SetActive_Self(false)禁用蒙版。

**证据**：
- `Assets/Scripts/ExternalClient/UI/HoleMaskImage.cs` L16-34 `IsRaycastLocationValid` — 镂空内(holeRect.Contains)返回false放行，外部返回true拦截，完全符合契约
- `Assets/Scripts/ExternalClient/Utils/TutorialMask.cs` L44-73 `SetHoleAtPosition` — 接收worldPosition+width+height，计算localCenter构建Rect，设置hasHole=true并激活gameObject
- `Assets/Scripts/ExternalClient/Utils/TutorialMask.cs` L36-42 `OnTutorialCloseHoleMask` — 清除hole(ClearHole) + 隐藏遮罩(gameObject.SetActive_Self(false))
- `Assets/Scripts/ExternalClient/UI/UIPanel/PlaneEditUI_Portrait.Tutorial.cs` L149-156 `SetMaskHoleAtPosition` — 步骤切换时调用mTutorialMask.SetHoleAtPosition更新镂空位置

---

### 5. 新手引导 > 组件tips层级 — ⚠️ uncertain (50%)

**规则**：Step 4（组件230068）和 Step 11（组件230057）点击后弹出的 tips 面板渲染层级高于引导蒙版，tips 内容完整可见不被蒙版遮挡

**边界提示**：层级可通过 Canvas.sortingOrder、transform.SetAsLastSibling() 或独立 Canvas 实现；需同时验证 tips 打开和关闭时机

**判定**：uncertain | 置信度 50%

**子要求判定**：

| SR | 描述 | 判定 | 置信度 | 关键证据 |
|----|------|------|--------|---------|
| SR-1 |  | uncertain | 50% | SetAsFirstSibling间接使tips层级高于教程蒙版，但无sortingOrder显式设置的代码证据，可能依赖Prefab配置 [缺口: 需确认Prefab中Canvas sortingOrder配置] |

**推理**：拖拽时排序层级设置为DRAG_SORTING_ORDER=99/DRAG_HIT_SORTING_ORDER=100(SortingGroup)。PlanePartTipsPanel_Portrait作为UI面板显示tips，但其sortingOrder未在代码中找到显式设置高于蒙版的逻辑。蒙版层级由TutorialMask所在Canvas决定（未发现显式设置）。tips面板在PlaneEditUI_Portrait的子层级中（通过Prefab配置），可能通过Canvas层级或Sibling顺序保证渲染在蒙版之上，但代码层面缺乏显式证据。PlaneUITutorialPanel初始化时 mUITutorialPanel.transform.SetAsFirstSibling() 将教程面板置于最底层，间接使后续UI（如tips）层级更高。

**证据**：
- `Assets/Scripts/ExternalClient/Managers/PlaneEditorManager/PlaneEditorManager.Operate.cs` L16-17 `class fields` — DRAG_SORTING_ORDER=99, DRAG_HIT_SORTING_ORDER=100 — 拖拽层级定义
- `Assets/Scripts/ExternalClient/UI/UIPanel/PlaneEditUI_Portrait.Tutorial.cs` L50 `OpenPlaneUITutorial` — mUITutorialPanel.transform.SetAsFirstSibling() — 教程面板放最底层，使其他UI(tips)渲染在其上方
- `Assets/Scripts/ExternalClient/UI/UIPanel/PlaneEditUI_Portrait.cs` L476-488 `CloseTips/SetTips` — tips通过PlanePartTipsPanel_Portrait管理，但无显式sortingOrder设置

**缺口**：PlanePartTipsPanel_Portrait无显式Canvas.sortingOrder设置代码；蒙版TutorialMask的sortingOrder亦未在代码中显式配置（可能依赖Prefab配置或Sibling顺序）

**附带发现**：

| 级别 | 问题 |
|------|------|
| [info] | PlaneUITutorialPanel.transform.SetAsFirstSibling()使教程面板在sibling中排最前（渲染最底），间接使同Canvas下的tips面板层级更高 |

---

### 6. 新手引导 > 拖拽安装-有效路径 — ✅ pass (70%)

**规则**：Step 5/6 拖拽教学中，玩家将组件成功拖至飞船组装区域有效位置并释放后：(A)手势提示消失；(B)文字提示保留；(C)组件tips关闭；(D)底版高亮区域持续显示直至组件落位确认

**边界提示**：「有效位置」指飞船组装区域的黄色框区域；手势提示指箭头/手指动画

**判定**：pass | 置信度 70%

**子要求判定**：

| SR | 描述 | 判定 | 置信度 | 关键证据 |
|----|------|------|--------|---------|
| SR-1 |  | pass | 90% | EndDragNormalPartTutorial L276: mMoveEffectGameObject.SetActive_Self(false) |
| SR-2 |  | uncertain | 45% | EndDragNormalPartTutorial调用ClosePanel()关闭整个面板，文字不是独立保留；后续CheckNextTutorial重开面板展示 [缺口: 无法确认契约要求的'文字保留'语义是否被满足] |
| SR-3 |  | pass | 85% | PlaneEditorManager.Operate.cs L524: PlaneEditUI_Portrait.Instance.CloseTips() 在拖 |
| SR-4 |  | pass | 70% | mInstallVaildAreaGameObject在CompletePartAssembly教程开始时实例化(L321)，教程完成时才Destroy(L34 [缺口: 仅在CompletePartAssembly步骤看到此逻辑，DragNormal] |

**推理**：(A)手势消失: EndDragNormalPartTutorial/EndDragSkillPartTutorial中 mMoveEffectGameObject.SetActive_Self(false) 关闭手势特效。(B)文字保留: AssistantPanel的ShowAssistantPanel第三参数为false时（见StartDragNormalPartTutorial L263），mContentText不被清除，EndDrag中未调用mAssistantPanel隐藏文字内容（仅ClosePanel关闭整体面板，但CompletePartAssembly步骤的ShowTutorialTips重新打开面板显示新文字）。(C)组件tips关闭: OnDrag中当进入新格子时调用PlaneEditUI_Portrait.Instance.CloseTips()(L524)关闭tips。(D)高亮区域: mInstallVaildAreaGameObject在CompletePartAssembly教程中被实例化(L321)并持续显示直到教程完成时Destroy(L342)。

**证据**：
- `Assets/Scripts/ExternalClient/UI/SubUIPanel/TutorialUI/PlaneUITutorialPanel.cs` L276 `EndDragNormalPartTutorial` — mMoveEffectGameObject.SetActive_Self(false) — 手势特效消失
- `Assets/Scripts/ExternalClient/Managers/PlaneEditorManager/PlaneEditorManager.Operate.cs` L524 `OnDrag` — PlaneEditUI_Portrait.Instance.CloseTips() — 拖拽进入新格子时关闭tips
- `Assets/Scripts/ExternalClient/UI/SubUIPanel/TutorialUI/PlaneUITutorialPanel.cs` L320-324 `StartCompletePartAssemblyTutorial` — hitInstallEffectGO = Instantiate(mInstallVaildAreaGameObject, ...) — 实例化有效区域高亮提示
- `Assets/Scripts/ExternalClient/UI/SubUIPanel/TutorialUI/PlaneUITutorialPanel.cs` L334 `StartCompletePartAssemblyTutorial (else branch)` — mMoveEffectGameObject.SetActive_Self(false) + Destroy(hitInstallEffectGO) — 完成后清理

**缺口**：SR-2(文字保留)为间接推断：EndDragNormalPartTutorial中调用ClosePanel()会关闭整个PlaneUITutorialPanel，但随后PlaneEditUI_Portrait.Instance.CheckNextTutorial()会重新打开下一步教程面板并显示新内容——实际上原文字并非严格'保留'而是面板关闭后重开

**附带发现**：

| 级别 | 问题 |
|------|------|
| [info] | EndDragNormalPartTutorial中ClosePanel()关闭了整个教程面板，意味着文字并非在同一面板中保留，而是步骤切换后由下一步教程面板展示新内容 |

---

### 7. 新手引导 > 拖拽安装-失败回退 — ❌ fail (80%)

**规则**：Step 5/6 中，玩家仅点击组件未进行拖拽，或拖拽后未释放到飞船有效区域内，组件回退到背包原位且手势引导重新触发

**边界提示**：「回退到背包原位」指 transform.position 恢复或对象重新归属背包容器；「重新触发」指手势提示动画重新播放

**判定**：fail | 置信度 80%

**子要求判定**：

| SR | 描述 | 判定 | 置信度 | 关键证据 |
|----|------|------|--------|---------|
| SR-1 |  | fail | 80% | OnExitTutorialArea为空实现；ResetPart仅在手动Cancel路径触发；拖拽释放到无效区域时无自动回退逻辑 |
| SR-2 |  | fail | 85% | 全库搜索ShowGesture/RestartGesture/ReplayGuide/RetriggerGuide均0命中；OnExitTutorialArea |

**推理**：OnExitTutorialArea(PlaneEditorManager.Operate.cs L210-213)为空实现——方法体无任何代码。且该方法的调用处(L162)上方的OnEnterTutorialArea(L157)也被注释掉了。这意味着：(1)拖拽离开教程有效区域时无任何回退逻辑；(2)手势引导不会重新触发。全库搜索HideGesture/ShowGesture/RestartGesture/ReplayGuide/RetriggerGuide均为0命中。CPlanePartInfo.ResetPart()方法存在（将组件恢复到原位），但仅在Cancel()中被调用（Cancel由操作面板按钮触发），不在拖拽失败路径中自动调用。虽然PlaneEditUI_Portrait.cs L392中 OnEnterTutorialArea 被调用（从组件列表拖入时），但对'仅点击未拖拽'或'释放到无效区域'场景无回退+重触发手势逻辑。

**证据**：
- `Assets/Scripts/ExternalClient/Managers/PlaneEditorManager/PlaneEditorManager.Operate.cs` L210-213 `OnExitTutorialArea` — 空方法体 — 无任何实现
- `Assets/Scripts/ExternalClient/Managers/PlaneEditorManager/PlaneEditorManager.Operate.cs` L157 `CheckInValidArea` — //OnEnterTutorialArea(collider); — 被注释掉
- `Assets/Scripts/ExternalClient/UI/SubUIPanel/PlaneEditUI/CPlanePartInfo.cs` L824-843 `ResetPart` — ResetPart存在回退逻辑(恢复position/rotation/flip)，但仅在Cancel()中被调用，不在拖拽失败路径中自动触发
- `Assets/Scripts/ExternalClient/UI/SubUIPanel/PlaneEditUI/CPlanePartInfo.cs` L539-549 `Cancel` — Cancel()调用ResetPart()+Install()，是手动取消操作，非拖拽失败自动回退

**附带发现**：

| 级别 | 问题 |
|------|------|
| [info] | OnExitTutorialArea为空实现是已确认的功能缺失，非uncertain |
| [info] | 全库搜索ShowGesture/RestartGesture/ReplayGuide/RetriggerGuide/HideGesture均为0命中，无手势重触发机制 |
| [info] | CheckInValidArea中OnEnterTutorialArea(collider)调用被注释掉（L157），mIsOverTutorialArea的进入逻辑实际由PlaneEditUI_Portrait.cs L392外部直接调用OnEnterTutorialArea()完成 |

---

### 8. 新手引导 > 重叠位置提示 — ⚠️ uncertain (35%)

**规则**：玩家首次将组件拖至与已安装组件重叠位置时，旋转和翻转按钮高亮并显示中文提示（Tutorial_text_6 含「点击旋转或翻转按钮调整方向」语义）；提示持续至组件被正确安装到非重叠有效位置

**边界提示**：「首次」指同一拖拽教学步骤内第一次触发重叠检测时；旋转和翻转是两个独立按钮

**判定**：uncertain | 置信度 35%

**子要求判定**：

| SR | 描述 | 判定 | 置信度 | 关键证据 |
|----|------|------|--------|---------|
| SR-1 | 重叠检测触发时旋转和翻转按钮高亮 | fail | 50% | 全局搜索 RotateButton、FlipButton、ShowRotateFlip、旋转.*高亮、翻转.*高亮 均无命中。PlaneUITutorialPa |
| SR-2 | 重叠时显示 Tutorial_text_6 中文提示 | uncertain | 50% | Tutorial_text_6 存在于 PlaneUITutorialPanel.cs L306，但触发条件是 StartCompletePartAssembl |
| SR-3 | 提示持续至组件被正确安装到非重叠有效位置 | uncertain | 50% | StartCompletePartAssemblyTutorial 中 isTips=false 分支（L326-344）执行 ClosePanel + 销毁  |

**附带发现**：

| 级别 | 问题 |
|------|------|
| [已确认] | PlaneUITutorialPanel.cs L289 StartCompletePartAssemblyTutorial — Tutorial_text_6 的显示由教程步骤状态机驱动(isTips参数)，非由重叠检测事件触发 |
| [已确认] | 全局代码 — 不存在旋转/翻转按钮高亮逻辑（RotateButton/FlipButton/Highlight 组合搜索零命中） |
| [已确认] | PlaneEditorManager.Data.cs L486 CheckOverlap — 重叠检测仅用于保存校验(condition_7)，不触发 UI 提示 |

---

### 9. 新手引导 > 一键替换逻辑 — ⚠️ uncertain (55%)

**规则**：Step 7 点击「一键替换更强组件」时，系统通过 GetBetterSolution 从背包中选取 Rarity 更高的同 PartId 组件替换到飞船对应槽位；教程中同时高亮背包中的 210010 组件及飞船上即将被替换的组件

**边界提示**：210010 为具体组件 ID；GetBetterSolution 的选取逻辑基于 Rarity 比较；替换后被替换组件应回到背包

**判定**：uncertain | 置信度 55%

**子要求判定**：

| SR | 描述 | 判定 | 置信度 | 关键证据 |
|----|------|------|--------|---------|
| SR-1 | GetBetterSolution 从背包中选取 Rarity 更高的同 PartId 组件执行替换 | pass | 50% | PlaneEditorManager.Data.cs L1363-1428 GetBetterSolution 实现完整：按 PartId 分组(L1372)  |
| SR-2 | 教程中同时高亮背包中 210010 组件和飞船上待替换组件 | fail | 50% | 1) 「210010」在整个 C:\Demo\client 代码仓库中零命中，不存在硬编码引用。教程使用的组件ID为 TUTORIAL_PART_ID_1=23 |

**附带发现**：

| 级别 | 问题 |
|------|------|
| [已确认] | PlaneEditPanel_Portrait.cs L430 — 教程高亮的组件由 GetBetterSolution 动态决定，非固定 PartId 210010 |
| [已确认] | PlaneEditUI_Portrait.Tutorial.cs L71 — SetReplacePart 仅禁用 blocksRaycasts 并移入教程面板，无显式高亮特效(Highlight/Glow/Outline) |
| [已确认] | 全局代码 — 飞船侧待替换组件无对应高亮逻辑 |

---

### 10. 新手引导 > 替换动画反馈 — ❌ fail (85%)

**规则**：点击一键替换后：(A) 被替换组件有明显特效标识；(B) 新组件飞入动画存在并触发；(C) 动画期间用户操作被屏蔽

**边界提示**：文档原文「最好有组件飞过去替换的动画」为期望而非强制；但至少需有替换反馈（非静默替换）

**判定**：fail | 置信度 85%

**子要求判定**：

| SR | 描述 | 判定 | 置信度 | 关键证据 |
|----|------|------|--------|---------|
| SR-1 | 被替换组件有明显特效标识 | fail | 50% | 全局搜索 ReplaceEffect、替换.*特效、OldPart.*Effect、HighlightReplace、FlashEffect 均零命中。Plan |
| SR-2 | 新组件飞入动画存在并触发 | fail | 50% | 全局搜索 FlyIn、FlyAnimation、飞入.*动画、MoveAnimation、DOMove.*Replace、TweenReplace 均零命中。C |
| SR-3 | 动画期间用户操作被屏蔽 | fail | 50% | 全局搜索 BlockInput、LockInput、DisableInteraction、mIsReplacing 零命中。isAnimating 仅出现在第三 |

**附带发现**：

| 级别 | 问题 |
|------|------|
| [已确认] | PlaneEditorManager.cs L683 DestroyImmediate — 旧组件被同步销毁，无法附加任何视觉反馈 |
| [已确认] | PlaneEditorManager.cs L692 Effect_UI_SKinSuccess — 唯一特效出现在新组件位置，语义为「安装成功」而非「被替换标识」 |
| [已确认] | PlaneEditPanel_Portrait.cs L515-523 OnReplaceClick — 循环替换多个组件时使用 replaceCount 计数器等待全部完成后保存，但期间无 UI 锁定 |

---

### 11. 新手引导 > 替换完成后续流程 — ⚠️ uncertain (50%)

**规则**：替换完成后强引导退出组装界面；退出后立即推进到第二关引导（Tutorial_text_8：「改装完毕，前往下一关！」），两步顺序不可倒转

**边界提示**：顺序约束：先退出组装→再引导第二关；如果顺序颠倒会导致 UI 显示错乱

**判定**：uncertain | 置信度 50%

**子要求判定**：

| SR | 描述 | 判定 | 置信度 | 关键证据 |
|----|------|------|--------|---------|
| SR-1 | 替换完成后强引导退出组装界面 | uncertain | 50% | 全局搜索 ExitAssembly、退出.*组装、CloseAssembly、BackToMain、ExitPlaneEditor 均零命中。但代码中存在弱引导 |
| SR-2 | 退出后推进到第二关引导，显示 Tutorial_text_8 | pass | 50% | Tutorial_text_8 存在于 MainUITutorialPanel.cs L100，对应 SecondBattle 步骤。TutorialUtil. |

**附带发现**：

| 级别 | 问题 |
|------|------|
| [已确认] | PlaneUITutorialPanel.cs L403 SetAllPartColliderState(true) — SoftBack阶段解除所有组件限制，用户可自由操作，非强制退出 |
| [已确认] | PlaneUITutorialPanel.cs L414 mAssistantPanel.SetActive_Self(false) — 退出引导未显示文字提示，仅有按钮位置的点击特效 |
| [已确认] | TutorialUtil.cs L268 — CheckPlaneEditCurTutorial 中 stepEnum != CompletedSkillPartAssembly 时直接 +1 递进，ReplacePart 之后的下一步由外部（主界面）状态机接管 |

---

### 12. 新手引导 > 关卡绑定 — ✅ pass (80%)

**规则**：第二关在第一关未通关时保持锁定；两个教学关卡均通过 ChallengeBattleStage 标准接口发起战斗

**边界提示**：锁定机制可能通过 PreRequisiteLevel 配置或代码中的前置条件判断实现

**判定**：pass | 置信度 80%

**子要求判定**：

| SR | 描述 | 判定 | 置信度 | 关键证据 |
|----|------|------|--------|---------|
| SR-1 | 第二关在第一关未通关时保持锁定 | pass | 50% | StarMapPoint.cs L51-58: 关卡锁定逻辑完整——PreRequisiteLevel==0 则解锁，否则 mIsLocked = !Check |
| SR-2 | 教学关卡通过 ChallengeBattleStage 发起战斗 | pass | 50% | ChallengeBattleStage 在 AccessManager.Access.cs L415 定义。调用点包括：MainPanel_Portrait. |

**附带发现**：

| 级别 | 问题 |
|------|------|
| [info] | StarMapPoint.cs L51 — PreRequisiteLevel==0 时直接解锁，第一关通常配置为0（无前置），具体值取决于配置表数据 [待确认-需查配置表] |

---

### 13. 新手引导 > 激活组件放置 — ⚠️ uncertain (30%)

**规则**：Step 11/12 激活类组件（ID=230057）仅可放置于飞船组装区域预设的 4 个特定合法格位；已占用 N 格则高亮显示剩余 4-N 个可用格位

**边界提示**：「4 个特定合法格位」由配置或代码硬编码决定；占用判断基于当前飞船上已安装组件的位置

**判定**：uncertain | 置信度 30%

**子要求判定**：

| SR | 描述 | 判定 | 置信度 | 关键证据 |
|----|------|------|--------|---------|
| SR-1 | 激活类组件仅可放置于预设的 4 个特定合法格位 | uncertain | 50% | 代码中存在 tutorialValidArea(PlaneEditUI_Portrait.Tutorial.cs L17-18) 作为教程阶段的拖拽限制区域(R |
| SR-2 | 已占用 N 格时仅高亮显示剩余 4-N 个可用格位 | fail | 50% | 全局搜索 ShowValidSlot、ShowAvailable、FilterOccupied、RemoveOccupied、高亮.*可用、AvailableS |

**附带发现**：

| 级别 | 问题 |
|------|------|
| [已确认] | PlaneEditorManager.Operate.cs L193-206 OnEnterTutorialArea — 仅检测是否在 tutorialValidArea 矩形内，不区分离散格位 |
| [已确认] | PlaneUITutorialPanel.cs L22 TUTORIAL_PART_ID_2=230057 — 确认 230057 是教程3的激活类组件ID |
| [已确认] | PlaneEditorManager.Operate.cs L210-212 OnExitTutorialArea — 方法体为空，离开教程区域无任何处理 |
| [已确认] | 全局代码 — 不存在「4个特定合法格位」的离散定义或计数逻辑，放置限制仅为矩形区域约束 |

---

### 14. 新手引导 > 激活组件拖拽三状态 — ❌ fail (94%)

**规则**：拖拽激活组件过程中三种状态循环切换：(1) 组件不在合法格位→显示 Tutorial_text_14（「将组件拖拽到黄色范围内」）且该范围高亮；(2) 组件在合法格位但未激活相邻武器组件→显示 Tutorial_text_12 并高亮旋转/翻转按钮；(3) 成功激活武器组件→显示 Tutorial_text_13（「组件已就位，激活特殊效果」）

**边界提示**：三种状态随玩家拖拽/旋转操作实时切换；状态(1)→(2) 通过放置到合法格位触发；状态(2)→(3) 通过旋转使蓝圈位置对齐武器组件触发

**判定**：fail | 置信度 94%

**子要求判定**：

| SR | 描述 | 判定 | 置信度 | 关键证据 |
|----|------|------|--------|---------|
| SR-1 | 状态1：不在合法格位时显示 Tutorial_text_14 且黄色范围高亮 | fail | 95% | Tutorial_text_14 仅出现在 MoveTutorialPanel.cs L24（ShowTipUI("Tutorial_text_14", 3f) |
| SR-2 | 状态2：在合法格位但未激活时显示 Tutorial_text_12 且高亮旋转/翻转按钮 | uncertain | 90% | Tutorial_text_12 在 PlaneUITutorialPanel.cs L527 的 StartFindValidPositionTutorial |
| SR-3 | 状态3：成功激活时显示 Tutorial_text_13 | pass | 98% | PlaneUITutorialPanel.cs L559: content = "Tutorial_text_13"，在 StartCompletedSkill |

---

### 15. 新手引导 > 激活影响显示 — ⚠️ uncertain (93%)

**规则**：选中带有激活效果的组件时，Effect 影响范围内的格位常驻显示高亮标记（ValidSprite/AreaSprite），被影响位置的已安装组件增加被激活特效；取消选中后影响范围高亮和特效立即消失

**边界提示**：「常驻」指选中期间持续显示，不闪烁不消失；高亮标记可能通过 ShowEffGrid/HideEffGrid 管理

**判定**：uncertain | 置信度 93%

**子要求判定**：

| SR | 描述 | 判定 | 置信度 | 关键证据 |
|----|------|------|--------|---------|
| SR-1 | 选中时 Effect 影响范围格位常驻显示高亮标记 | pass | 95% | ShowEffGrid()（CPlanePartInfo.cs L427-434）遍历 EffGridList 调用 SetGrid(PlaneHitGrid. |
| SR-2 | 被影响位置的已安装组件增加被激活特效 | fail | 90% | UpdateEffect()（PlaneEditHitGrid.cs L60-68）仅判断格位是否有组件（HasOverlapPart）并切换 grid spr |
| SR-3 | 取消选中后影响范围高亮和特效立即消失 | pass | 95% | HideEffGrid()（CPlanePartInfo.cs L439-445）遍历 EffGridList 调用 HideGrid()，HideGrid() |

---

### 16. 新手引导 > 激活按钮半径配置 — ⚠️ uncertain (85%)

**规则**：激活类组件的旋转/翻转按钮的点击半径配置需覆盖组件 Effect 影响位置，或为按钮半径单独提供可配置参数

**边界提示**：按钮半径可能通过 mMinRadius、collider size 或 RectTransform 尺寸实现；需要按钮操作范围能触达 Effect 范围内的目标

**判定**：uncertain | 置信度 85%

**子要求判定**：

| SR | 描述 | 判定 | 置信度 | 关键证据 |
|----|------|------|--------|---------|
| SR-1 | 按钮点击半径配置可覆盖 Effect 影响位置或有独立可配置参数 | uncertain | 85% | PlaneOperatePanel_Portrait.cs L37: [SerializeField] private float mMinRadius — 通 |

---

### 17. 新手引导 > 引导步骤顺序与持久化 — ✅ pass (94%)

**规则**：新手引导按固定线性步骤顺序执行（Tutorial_text_0 至 Tutorial_text_14），任意前置步骤未完成时后续步骤不可进入；场景切换（战斗↔主界面↔组装界面）后引导进度不重置

**边界提示**：步骤顺序控制可能通过枚举值比较 + progress 递增实现；持久化可能使用 PlayerPrefs 或服务端存储

**判定**：pass | 置信度 94%

**子要求判定**：

| SR | 描述 | 判定 | 置信度 | 关键证据 |
|----|------|------|--------|---------|
| SR-1 | 步骤按线性顺序执行，前置步骤未完成时后续不可进入 | pass | 93% | 三重保障机制：(1) TutorialStepEnum 枚举值线性递增（None=0 到 CompletedSkillPartAssembly=15）；(2)  |
| SR-2 | 场景切换后引导进度不重置 | pass | 95% | FinishTutorial()（TutorialUtil.cs L330-361）实现三层持久化：(1) 内存：UserDataManager.Instanc |

---

### 18. 新手引导 > 固定组件ID容错 — ⚠️ uncertain (91%)

**规则**：教程硬编码引用的组件 ID 230068（TUTORIAL_PART_ID_1）和 230057（TUTORIAL_PART_ID_2）在教程触发时执行 null 检查；组件缺失时有明确回退路径不卡死

**边界提示**：容错路径可能包括跳过当前步骤、显示错误提示或使用替代组件；硬编码 ID 是项目设计决策，此处关注容错而非消除硬编码

**判定**：uncertain | 置信度 91%

**子要求判定**：

| SR | 描述 | 判定 | 置信度 | 关键证据 |
|----|------|------|--------|---------|
| SR-1 | 230068 使用时执行 null 检查 | pass | 95% | TutorialUtil.cs L213: var targetPart = UserDataManager.Instance.LocalUserData.CP |
| SR-2 | 230057 使用时执行 null 检查 | pass | 95% | TutorialUtil.cs L233: var targetPart = ...Find(item => item.PartId == PlaneUITut |
| SR-3 | 组件缺失时有明确回退路径不卡死 | uncertain | 85% | 回退路径存在但为隐式设计：TUTORIAL_PART_ID_1 查找失败 → 回退到 FirstBattle（L219）；TUTORIAL_PART_ID_2  |

---

### 19. 新手引导 > 弱引导退出 — ⚠️ uncertain (92%)

**规则**：激活类组件成功激活武器组件（达到 Tutorial_text_13 状态）后，触发弱引导退出步骤；玩家执行任意退出动作即结束新手引导教程态，教程进度推进到最终完成状态

**边界提示**：「弱引导」指不强制指定操作目标，玩家可自由选择退出方式；「最终完成状态」可能对应 CompletedSkillPartAssembly 或类似枚举值

**判定**：uncertain | 置信度 92%

**子要求判定**：

| SR | 描述 | 判定 | 置信度 | 关键证据 |
|----|------|------|--------|---------|
| SR-1 | 成功激活后触发弱引导退出步骤 | fail | 92% | StartSoftBackFromPlaneTutorial()（PlaneUITutorialPanel.cs L398-415）是专门的弱引导退出方法：解锁 |
| SR-2 | 玩家任意退出动作即结束教程态，进度推进到最终完成状态 | pass | 93% | CompletedSkillPartAssemblyTutorial 的 AssistantPanelData 构造（L565-566）将 FinishActi |

---

## ast-grep 结构性检测汇总

> ⚠️ ast-grep 本次因进程挂起已终止，未产出有效结果。以下为降级说明。

### 自定义规则命中

| 规则 ID | 命中数 | 严重级 | 代表性命中 |
|---------|--------|--------|-----------|
| empty-method-body | N/A | warning | 降级：sg 不可用 |
| event-subscribe-unpaired | N/A | info | 降级：sg 不可用 |
| event-unsubscribe | N/A | info | 降级：sg 不可用 |
| coroutine-no-yield | N/A | warning | 降级：sg 不可用 |
| getcomponent-no-null-check | N/A | info | 降级：sg 不可用 |

### 事件配对分析

ast-grep 降级，事件配对分析未执行。

**已知手工发现**：
- `TUTORIAL_SHOW_HOLE_MASK` / `TUTORIAL_CLOSE_HOLE_MASK` — ✅ 已配对（TutorialMask.cs）
- `TUTORIAL_IS_COMPLETED` — ⚠️ 仅见 Dispatch，未确认 Subscribe 位置

---

## 未走查契约

| moduleLabel | rule | verifyMethods | 跳过原因 |
|-------------|------|---------------|----------|
| （无） | — | — | 本次所有 19 条契约均为 code_review 类型，无跳过项 |
