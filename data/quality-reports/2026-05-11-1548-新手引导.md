# 质量契约走查报告

- 日期：2026-05-11 15:48
- 需求文档：新手引导-一测.docx
- 走查引擎：Cursor Agent
- 模式：refreshSearch=true（全量重新搜索代码）

## 总览

| 指标 | 数量 |
|------|------|
| 契约总数 | 19 |
| 代码走查 (code_review) | 18 |
| 通过 (pass) | 10 |
| 违规 (fail) | 4 |
| 存疑 (uncertain) | 4 |
| 跳过 (非 code_review) | 1 |
| 调度模式 | 并行（3组） |
| ast-grep | 已启用（custom_rules: empty-method-body×6处, getcomponent-no-null-check×3处; event_pairing: 待确认） |
| Evidence 验证 | total=138, verified=61, failed=77, downgraded=7（本次） |
| 映射缓存(v2) | modules_updated=4, +0 files, +29 deps, +33 clusters, +44 links |

## 走查详情

---

### 1. 新手引导 > 关卡选择 — pass (58%)

**规则**：新手引导 Step 0 触发时，关卡选择界面进入强引导态：全屏蒙版激活（SetMaskColor(true)），BattleButton 位置设有圆形镂空高亮（CircleAim），非镂空区域点击被拦截；第一关是唯一可发起战斗的关卡

**边界提示**：实现方式为蒙版拦截而非 interactable=false；「默认选中第一关」指界面打开时战斗目标为第一关

**判定**：pass | 置信度 58%（验证后降低）

**子要求判定**：

| SR | 描述 | 类型 | 判定 | 置信度 | 关键证据 |
|----|------|------|------|--------|---------|
| SR-1 | SetMaskColor+CircleAim 镂空到 BattleButton | grep_existence | pass | 88% | `MainUITutorialPanel.StartMatchTutorial()` L74-117 |
| SR-2 | 仅第一关可发起战斗 | llm_semantic | pass | 85% | `StarMapPoint.Init()` L51-57 PreRequisiteLevel 锁定 |

**证据**：
- `MainUITutorialPanel.cs` L74-117 `StartMatchTutorial` — CircleAim_UI(pos=BattleButton, radius=300, needClickCenter=true) + SetMaskColor(true) ✅
- `AssistantPanel.cs` L310-336 `OnEventAction` — 镂空内触发 BattleButton.onClick；镂空外 mOutSideAction=null 无动作=拦截 ✅
- `StarMapPoint.cs` L51-57 `Init` — PreRequisiteLevel 锁定非第一关 ❌(验证失败:method_not_found)

**缺口**：无

**附带发现**：

| 级别 | 文件 | 行号 | 方法 | 问题 | 违规性 |
|------|------|------|------|------|--------|
| [已确认] | `AssistantPanel.cs` | — | `CloseAssistantPanel_OutSideArea` | 拦截效果通过 mOutSideAction=null 隐式实现，非显式 BlockRaycast | 建议改进 |

---

### 2. 新手引导 > 移动教学 — 跳过

**规则**：Step 1 弱引导阶段，玩家执行滑动屏幕操作后提示立即隐藏

**跳过原因**：verifyMethods 仅含 ui_test，暂不支持

---

### 3. 新手引导 > 武器自动战斗提示 — pass (82%)

**规则**：Step 1 弱引导阶段，Tutorial_text_1 在进入步骤时设置到持久文字区域（mContentText）显示；该文字提示存在隐藏触发机制

**判定**：pass | 置信度 82%

**子要求判定**：

| SR | 描述 | 类型 | 判定 | 置信度 | 关键证据 |
|----|------|------|------|--------|---------|
| SR-1 | Tutorial_text_1 设置到 mContentText | grep_existence | pass | 85% | `MoveTutorialPanel.Start()` 设置 mContentText |
| SR-2 | 存在隐藏/清除触发逻辑 | llm_semantic | pass | 80% | `GameUI.SetBattleFieldTutorialPanel(false)` → Destroy 面板 |

**证据**：
- `MoveTutorialPanel.cs` — Start() 中 mContentText 被赋值 Tutorial_text_1
- `GameUI.cs` — SetBattleFieldTutorialPanel(false) → Destroy 整个面板清理文字

**附带发现**：

| 级别 | 文件 | 行号 | 方法 | 问题 | 违规性 |
|------|------|------|------|------|--------|
| [已确认] | `MoveTutorialPanel.cs` | — | — | Tutorial_text_14（浮动提示 3.5s DOFade 消失）与 Tutorial_text_1（持久底部文字）生命周期独立 | 建议改进 |

---

### 4. 新手引导 > 引导蒙版射线拦截 — pass (92%)

**规则**：HoleMaskImage.IsRaycastLocationValid 实现镂空内放行/外拦截；SetHoleAtPosition 步骤切换同步；蒙版关闭通过 OnTutorialCloseHoleMask

**判定**：pass | 置信度 92%

**子要求判定**：

| SR | 描述 | 类型 | 判定 | 置信度 | 关键证据 |
|----|------|------|------|--------|---------|
| SR-1 | IsRaycastLocationValid 镂空内 false/外 true | llm_semantic | pass | 95% | `HoleMaskImage.cs` L16-34 完整实现 ✅ |
| SR-2 | SetHoleAtPosition 步骤切换时被调用 | grep_existence | pass | 90% | `TutorialMask.cs` L44-73 ✅ |
| SR-3 | OnTutorialCloseHoleMask 禁用蒙版 | grep_existence | pass | 90% | `TutorialMask.cs` L36-42 ✅ |

**证据**（全部通过磁盘验证）：
- `HoleMaskImage.cs` L16-34 `IsRaycastLocationValid` — hasHole=false→true(全屏拦截)；holeRect.Contains→false(放行)；else→true(拦截) ✅
- `TutorialMask.cs` L44-73 `SetHoleAtPosition` — 世界→屏幕→本地坐标转换，构造 holeRect ✅
- `TutorialMask.cs` L36-42 `OnTutorialCloseHoleMask` — SetActive(false)+ClearHole() ✅
- `PlaneEditUI_Portrait.Tutorial.cs` L149-157 `SetMaskHoleAtPosition` — 包装调用 ✅
- `PlaneEditorManager.Operate.cs` L193-207 `OnEnterTutorialArea` — 进入教程区域同步镂空位置 ✅

**附带发现**：

| 级别 | 文件 | 行号 | 方法 | 问题 | 违规性 |
|------|------|------|------|------|--------|
| [已确认] | `GuideCutoutMaskController.cs` | — | — | 并行存在另一套基于 Shader 的镂空蒙版机制，当前教程未使用 | 建议改进 |
| [已确认] | `TutorialMask.cs` | — | `Start/Update` | 空实现（ast-grep 命中），冗余代码 | 建议改进 |

---

### 5. 新手引导 > 组件tips层级 — uncertain (50%)

**规则**：Step 4/11 点击后弹出的 tips 面板渲染层级高于引导蒙版

**判定**：uncertain | 置信度 50%

**子要求判定**：

| SR | 描述 | 类型 | 判定 | 置信度 | 关键证据 |
|----|------|------|------|--------|---------|
| SR-1 | tips 面板 sortingOrder 高于蒙版 | llm_semantic | uncertain | 45% | 代码无显式 sortingOrder 设置，需查 Prefab |
| SR-2 | 教程步骤推进时 tips 关闭 | grep_existence | pass | 55% | ClosePanel 路径存在 |

**缺口**：tips 面板的层级控制可能通过 Prefab 的 Canvas sortingOrder 配置实现而非代码，需运行时或 Prefab 配置验证。

---

### 6. 新手引导 > 拖拽安装-有效路径 — pass (78%)

**规则**：拖拽到有效位置后：手势消失、文字保留、tips 关闭、黄色框高亮持续

**判定**：pass | 置信度 78%

**子要求判定**：

| SR | 描述 | 类型 | 判定 | 置信度 | 关键证据 |
|----|------|------|------|--------|---------|
| SR-1 | 手势提示隐藏 | grep_existence | pass | 75% | EndDragNormalPartTutorial 调用 ClosePanel |
| SR-2 | 文字提示不随 ClosePanel 消失 | llm_semantic | pass | 72% | ClosePanel 关闭手势但文字提示独立生命周期 |
| SR-3 | 组件 tips 关闭 | grep_existence | pass | 80% | ClosePanel 路径关闭 tips |
| SR-4 | 黄色框高亮持续 | llm_semantic | pass | 85% | mInstallVaildAreaGameObject 持续激活到落位确认 |

---

### 7. 新手引导 > 拖拽安装-失败回退 — pass (72%)

**规则**：未释放到有效区域时组件回退到背包原位，手势引导重新触发

**判定**：pass | 置信度 72%

**子要求判定**：

| SR | 描述 | 类型 | 判定 | 置信度 | 关键证据 |
|----|------|------|------|--------|---------|
| SR-1 | 组件回退到背包原位 | llm_semantic | pass | 70% | 通用 OnDrop 后 transform.position = HitHolder.position 实现 |
| SR-2 | 手势引导重新触发 | grep_existence | pass | 75% | 教程步骤未推进→重新进入当前步骤触发手势 |

**附带发现**：

| 级别 | 文件 | 行号 | 方法 | 问题 | 违规性 |
|------|------|------|------|------|--------|
| [已确认] | `PlaneEditUI_Portrait.cs` | L396 | `OnDragCloneExitArea` | **空实现** — 克隆进入飞船区域后拖出不清理，组件残留风险 | 潜在违规 |
| [已确认] | `PlaneEditorManager.Operate.cs` | L210 | `OnExitTutorialArea` | **空实现** — 教程区域退出无清理逻辑 | 潜在违规 |

---

### 8. 新手引导 > 重叠位置提示 — **fail** (82%)

**规则**：首次拖至重叠位置时旋转/翻转按钮高亮并显示提示；单次触发标志

**判定**：**fail** | 置信度 82%

**子要求判定**：

| SR | 描述 | 类型 | 判定 | 置信度 | 关键证据 |
|----|------|------|------|--------|---------|
| SR-1 | 旋转/翻转按钮高亮 | grep_existence | **fail** | 85% | ≥3 种变体全库 0 命中，豁免 A✗ B✗ C✗ |
| SR-2 | 单次触发标志 | grep_existence | **fail** | 80% | mIsShowedTips 等变体全库 0 命中 |

**汇总**：SR-1 + SR-2 均 fail → 整条 fail

**推理**：
- `HighlightRotate|BtnRotate.*highlight|ShowRotateTip|RotateButton.*active|FlipButton.*glow` 全库 0 命中
- `mIsShowedTips|mHasShownOverlapTip|mOverlapTipShown|mRotateTipShown|bRotateHintShowed` 全库 0 命中
- 强制豁免自检：A✗ 通用机制无间接实现 / B✗ 无回调模式 / C✗ Prefab 无按钮高亮配置
- 代码中重叠检测后仅显示文字提示（Tutorial_text_6），无旋转/翻转按钮高亮逻辑

**附带发现**：

| 级别 | 文件 | 行号 | 方法 | 问题 | 违规性 |
|------|------|------|------|------|--------|
| [已确认] | `PlaneOperatePanel` | — | — | 旋转/翻转按钮无自定义 Highlighted/Pressed 状态，依赖 Unity 默认 Transition | 建议改进 |

---

### 9. 新手引导 > 一键替换逻辑 — pass (78%)

**规则**：GetBetterSolution 从背包找 Rarity 更高的同 PartId 组件执行替换；同时高亮背包和飞船组件

**判定**：pass | 置信度 78%

**子要求判定**：

| SR | 描述 | 类型 | 判定 | 置信度 | 关键证据 |
|----|------|------|------|--------|---------|
| SR-1 | GetBetterSolution 按 Rarity 匹配替换 | llm_semantic | pass | 85% | `PlaneEditorManager.Data.GetBetterSolution` 按 Rarity 排序 |
| SR-2 | 背包+飞船组件同时高亮 | grep_existence | pass | 70% | 替换候选组件在列表中被设置高亮 |

---

### 10. 新手引导 > 替换动画反馈 — **fail** (92%)

**规则**：替换后飞入动画 + 飞出特效 + 操作屏蔽

**判定**：**fail** | 置信度 92%

**子要求判定**：

| SR | 描述 | 类型 | 判定 | 置信度 | 关键证据 |
|----|------|------|------|--------|---------|
| SR-1 | 飞入动画 | grep_existence | **fail** | 92% | DOMove/TweenFly/FlyToSlot 等全库 0 命中 |
| SR-2 | 飞出特效 | grep_existence | **fail** | 92% | FlyToBag/ReplaceEffect/SwapEffect 等全库 0 命中 |
| SR-3 | 操作屏蔽 | grep_existence | **fail** | 92% | BlockInput/DisableInteraction 等全库 0 命中 |

**汇总**：三个 SR 全部 fail → 整条 fail

**推理**：
- `ReplacePlanePart` 直接 DestroyImmediate 旧件 + CreateReplacePlanePart 新件，无任何过渡动画
- DOTween 库已引入但仅用于镜头动画，未扩展到组件替换
- 强制豁免自检全部未命中（A✗ B✗ C✗）

**附带发现**：

| 级别 | 文件 | 行号 | 方法 | 问题 | 违规性 |
|------|------|------|------|------|--------|
| [已确认] | `PlaneEditorManager.cs` | L683 | `ReplacePlanePart` | DestroyImmediate 替代 Destroy，Editor 模式下可能有副作用 | 建议改进 |
| [已确认] | — | — | — | DOTween 库已可用但替换流程未使用 | 建议改进 |

---

### 11. 新手引导 > 替换完成后续流程 — pass (28%)

**规则**：替换完成后引导退出组装界面；退出后推进到第二关战斗步骤

**判定**：pass | 置信度 28%（验证后降低，部分 evidence 未通过磁盘验证）

**子要求判定**：

| SR | 描述 | 类型 | 判定 | 置信度 | 关键证据 |
|----|------|------|------|--------|---------|
| SR-1 | 触发引导退出组装界面 | grep_existence | pass | 88% | `StartSoftBackFromPlaneTutorial()` L398-415 ✅ |
| SR-2 | 退出后推进到 Tutorial_text_8 | grep_existence | pass | 82% | `StartMatchTutorial(SecondBattle)` → Tutorial_text_8 |

**附带发现**：

| 级别 | 文件 | 行号 | 方法 | 问题 | 违规性 |
|------|------|------|------|------|--------|
| [待确认] | `PlaneUITutorialPanel.cs` | L398 | `StartSoftBackFromPlaneTutorial` | 仅高亮返回按钮无文字提示，玩家可能不知该点哪里 | 建议改进 |

---

### 12. 新手引导 > 关卡绑定 — pass (21%)

**规则**：第二关在第一关未通关时锁定；两关均通过 ChallengeBattleStage 发起

**判定**：pass | 置信度 21%（验证后降低）

**子要求判定**：

| SR | 描述 | 类型 | 判定 | 置信度 | 关键证据 |
|----|------|------|------|--------|---------|
| SR-1 | PreRequisiteLevel 锁定 | grep_existence | pass | 90% | `StarMapPoint.Init()` L51-57 |
| SR-2 | ChallengeBattleStage 标准接口 | grep_existence | pass | 85% | `MainPanel_Portrait.OnBattleButtonClick()` L168 |

**缺口**：仅单重 PreRequisiteLevel 检查，无教程步骤进度双重检查（与 boundaryHint 一致）

---

### 13. 新手引导 > 激活组件放置 — uncertain (50%)

**规则**：激活组件仅可放置于最多 4 个特定合法格位；已占用格位排除后动态显示

**判定**：uncertain | 置信度 50%

**子要求判定**：

| SR | 描述 | 类型 | 判定 | 置信度 | 关键证据 |
|----|------|------|------|--------|---------|
| SR-1 | 合法格位定义 | grep_existence | uncertain | 50% | mValidArea_Tutorial 存在但为矩形区域非精确 4 格位 |
| SR-2 | 占用排除 4-N 动态显示 | llm_semantic | uncertain | 50% | CheckInValidArea 存在但被注释 |

**缺口**：代码实现为矩形区域检测（mValidArea_Tutorial），非精确 4 格位坐标约束。需确认矩形区域是否等效覆盖 4 格位。

---

### 14. 新手引导 > 激活组件拖拽三状态 — **fail** (85%)

**规则**：三种状态循环切换：(1)不在合法格位→文字+高亮 (2)在合法格位未激活→文字+按钮高亮 (3)激活成功→文字；移出可回切

**判定**：**fail** | 置信度 85%

**子要求判定**：

| SR | 描述 | 类型 | 判定 | 置信度 | 关键证据 |
|----|------|------|------|--------|---------|
| SR-1 | 状态(1) Tutorial_text_11/14 | grep_existence | pass | 85% | Tutorial_text_11 在代码中存在 |
| SR-2 | 状态(2) Tutorial_text_12 | grep_existence | pass | 85% | Tutorial_text_12 在代码中存在 |
| SR-3 | 状态(3) Tutorial_text_13 | grep_existence | pass | 85% | Tutorial_text_13 在代码中存在 |
| SR-4 | 移出后状态回切至(1) | llm_semantic | **fail** | 85% | `OnExitTutorialArea` **空实现** + `CheckInValidArea` 注释 |

**汇总**：SR-4 fail → 整条 fail

**推理**：
- `OnExitTutorialArea(Collider2D collider) { }` — 空方法体，ast-grep empty-method-body 确认
- `CheckInValidArea` 中进入/退出检测被注释
- 空实现条款触发：方法存在但实现为空 = 功能确认缺失，豁免无效 → fail

---

### 15. 新手引导 > 激活影响显示 — **fail** (78%)

**规则**：选中时 ShowEffGrid 高亮 + 被影响组件增加激活特效；取消选中后消失

**判定**：**fail** | 置信度 78%

**子要求判定**：

| SR | 描述 | 类型 | 判定 | 置信度 | 关键证据 |
|----|------|------|------|--------|---------|
| SR-1 | ShowEffGrid 显示高亮 | grep_existence | pass | 85% | ShowEffGrid 存在并被调用 |
| SR-2 | 被影响组件增加激活特效 | grep_existence | **fail** | 78% | EdgeLight 仅用于品质流光，无激活视觉特效 |
| SR-3 | HideEffGrid 取消高亮 | grep_existence | pass | 85% | HideEffGrid 存在并被调用 |

**汇总**：SR-2 fail → 整条 fail

**推理**：
- ShowEffGrid/HideEffGrid 高亮标记功能完整（SR-1/3 pass）
- 但被影响位置的已安装组件无激活视觉特效 — EdgeLight 仅为品质流光（所有组件通用），非激活专属特效
- UpdateEffGrid 仅设置标志位，无额外视觉反馈

---

### 16. 新手引导 > 激活按钮半径配置 — pass (85%)

**规则**：按钮点击半径参数可配置，考虑 Effect 影响范围

**判定**：pass | 置信度 85%

**子要求判定**：

| SR | 描述 | 类型 | 判定 | 置信度 | 关键证据 |
|----|------|------|------|--------|---------|
| SR-1 | mMinRadius 存在且可配置 | grep_existence | pass | 90% | mMinRadius 为序列化字段 |
| SR-2 | 与 Effect 范围有关联 | llm_semantic | pass | 80% | 独立可配置参数（Inspector 调整） |

---

### 17. 新手引导 > 引导步骤顺序与持久化 — pass (92%)

**规则**：progress > mTutorialProgress 防倒退；SetTutorialProcess 服务端 + PlayerPrefs 本地双重持久化

**判定**：pass | 置信度 92%

**子要求判定**：

| SR | 描述 | 类型 | 判定 | 置信度 | 关键证据 |
|----|------|------|------|--------|---------|
| SR-1 | progress > mTutorialProgress 比较 | grep_existence | pass | 95% | `TutorialUtil.FinishTutorial()` L334 ✅ |
| SR-2 | SetTutorialProcess 服务端接口 | grep_existence | pass | 92% | `TutorialUtil.FinishTutorial()` L337 |
| SR-3 | PlayerPrefs 本地备份 | grep_existence | pass | 90% | `TutorialUtil.FinishTutorial()` L346-354 |

**证据**（全部核心逻辑通过验证）：
- `TutorialUtil.cs` L334 — `if (progress > mTutorialProgress)` 整型大小比较
- `TutorialUtil.cs` L337 — `AccessManager.Instance.SetTutorialProcess(progress, true)` 服务端
- `TutorialUtil.cs` L346-354 — `PlayerPrefs.SetInt` + `PlayerPrefs.Save()` 本地
- `UserData.PlayerInfo.cs` L116 — 登录同步取较大值

---

### 18. 新手引导 > 固定组件ID容错 — uncertain (85%)

**规则**：230068/230057 在教程触发时执行 null 检查；缺失时有回退路径

**判定**：uncertain | 置信度 85%（验证降级：原 pass，evidence 磁盘验证全部失败导致降级）

**子要求判定**：

| SR | 描述 | 类型 | 判定 | 置信度 | 关键证据 |
|----|------|------|------|--------|---------|
| SR-1 | 230068 null 检查+回退 | llm_semantic | pass | 85% | `CheckMainPanelCurTutorialIn()` L213-219 null→回退 FirstBattle |
| SR-2 | 230057 null 检查+回退 | llm_semantic | pass | 85% | `CheckMainPanelCurTutorialIn()` L233-239 null→回退 SecondBattle |

**缺口**：Evidence 全部未通过磁盘验证（method_not_found），需手动确认。子 Agent 判定逻辑正确但 evidence 行号精度不足。

---

### 19. 新手引导 > 弱引导退出 — uncertain (78%)

**规则**：激活成功后触发弱引导退出；anyClick 推进到最终完成状态

**判定**：uncertain | 置信度 78%（验证降级：原 pass，evidence 磁盘验证全部失败导致降级）

**子要求判定**：

| SR | 描述 | 类型 | 判定 | 置信度 | 关键证据 |
|----|------|------|------|--------|---------|
| SR-1 | anyClick 弱引导退出 | grep_existence | pass | 75% | 双回调模式（FinishAction 同传两个槽） |
| SR-2 | 推进到 CompletedSkillPartAssembly | grep_existence | pass | 82% | FinishTutorial(CompletedSkillPartAssembly) |

**缺口**：`SetMaskColor(true)` 在此步骤中被调用（强引导视觉特征），与"弱引导"描述有偏差。功能层面是 anyClick 退出（等效弱引导），但视觉上有深色蒙版。Evidence 未通过磁盘验证导致降级。

**附带发现**：

| 级别 | 文件 | 行号 | 方法 | 问题 | 违规性 |
|------|------|------|------|------|--------|
| [已确认] | `PlaneUITutorialPanel.cs` | L565 | `StartCompletedSkillPartAssemblyTutorial` | CircleAim_UI radius=0，镂空圆几乎不存在 | 建议改进 |
| [已确认] | `PlaneUITutorialPanel.cs` | L586 | FinishAction | 双重 FinishTutorial 调用（防御性编码，确保不阻塞） | 建议改进 |

---

## ast-grep 结构性检测汇总

### 自定义规则命中

| 规则 ID | 命中数 | 严重级 | 代表性命中（至多3条） |
|---------|--------|--------|---------------------|
| empty-method-body | 6+ | warning | `PlaneEditorManager.Operate.cs` L210 `OnExitTutorialArea`; `PlaneEditUI_Portrait.cs` L396 `OnDragCloneExitArea`; `PlaneUITutorialPanel.cs` L417 `StartSecondEditTutorial` |
| event-subscribe-unpaired | 0 | info | — |
| event-unsubscribe | 0 | info | — |
| coroutine-no-yield | 0 | warning | — |
| getcomponent-no-null-check | 3+ | info | `AssistantPanel.cs` L58; `StarMapPoint.cs` L69; `PlaneEditUI_Portrait.Tutorial.cs` L47 |

### 事件配对分析

未检测到显著未配对事件订阅。TUTORIAL_CLOSE_HOLE_MASK 事件在 TutorialMask 中注册+注销配对完整。

---

## 未走查契约

| moduleLabel | rule | verifyMethods | 跳过原因 |
|-------------|------|---------------|----------|
| 新手引导 > 移动教学 | 滑动屏幕后提示隐藏 | ui_test | 暂不支持 |

---

## 与上次走查（05-09）对比

| 指标 | 05-09 | 05-11（本次） | 变化 |
|------|-------|-------------|------|
| pass | 4 | 10 | +6 |
| fail | 5 | 4 | -1 |
| uncertain | 6 | 4 | -2 |
| Evidence 验证通过率 | 66% (48/73) | 44% (61/138) | ↓ |

**主要变化说明**：
- 关卡选择、关卡绑定、替换后续流程、引导步骤顺序、一键替换、武器自动战斗等 6 条从 uncertain 升级为 pass（代码搜索更充分）
- 重叠位置提示维持 fail（旋转/翻转按钮高亮 + 单次触发标志确实未实现）
- 替换动画反馈维持 fail（飞入/飞出动画确实未实现）
- 拖拽三状态维持 fail（OnExitTutorialArea 空实现未修复）
- 激活影响显示维持 fail（被激活特效未实现）
- Evidence 验证通过率下降主要因为 method_not_found 增加（72/77），多为子 Agent 使用了方法签名中的非精确名称
