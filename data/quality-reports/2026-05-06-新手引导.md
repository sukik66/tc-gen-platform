# 质量契约走查报告

- 日期：2026-05-06 17:05
- 需求文档：新手引导-一测.docx
- 走查引擎：Cursor Agent

## 总览

| 指标 | 数量 |
|------|------|
| 契约总数 | 14 |
| 代码走查 (code_review) | 11 |
| 通过 (pass) | 5 |
| 违规 (fail) | 1 |
| 存疑 (uncertain) | 5 |
| 跳过 (非 code_review) | 3 |
| 调度模式 | 并行（3组） |
| ast-grep | 未启用（降级） |
| Evidence 验证 | total=54, verified=32, failed=22, downgraded=0（本次） |
| 映射缓存 | 命中 13 文件 ‖ 已更新 +0 文件 |

## 走查详情

### 1. [新手引导 > 关卡选择] — uncertain (55%)

**规则**：新手引导开始时，关卡选择界面第一关默认选中且为唯一可点击关卡，其余关卡按钮处于锁定不可交互状态

**边界提示**：玩家已通关后再次进入新手引导时，默认仍聚焦第一关；引导结束前其他关卡不解锁

**判定**：uncertain | 置信度 55%

**推理**：StarMapPoint 通过 PreRequisiteLevel 配置实现关卡锁定（PreRequisiteLevel==0 不锁即第一关），ChapterPanel_MainPanel.SelectLevel 中 mBattleButtonCanvasGroup 控制战斗按钮可用性。但新手引导期间是否有额外逻辑强制仅第一关可点击，代码中未找到专用引导态约束。StarMapPoint 的 Collider 始终 enabled，玩家可点击锁定关卡查看信息面板。

**证据**：
- `StarMapPoint.cs` L51-58 `StarMapPoint.Init` — PreRequisiteLevel==0 时不锁定；否则通过 CheckBattleStageFinish 判断 ✅
- `ChapterPanel_MainPanel.cs` L508-513 `SelectLevel` — 锁定关卡战斗按钮不可点击 ✅
- `StarMapPoint.cs` L78-117 `SetPointLockState` — 锁定材质更换但 Collider 不被禁用 ✅
- `MainPanel_Portrait.cs` L217-223 `CheckCurrentTutorial` — 引导仅调用 OpenMainUITutorial，不限制关卡交互 ✅

**缺口**：未找到新手引导专用的关卡交互限制代码；配置表实际数据无法从代码确认

**附带发现**：
- StarMapPoint 的 Collider 在锁定后仍 enabled，UI 层面锁定不够彻底
- ChapterPanel_MainPanel.OnPointerClick 的 catch(Exception ex) 为空，吞掉所有异常

---

### 2. [新手引导 > 引导蒙版] — pass (78%)

**规则**：强引导进行中，非当前步骤关注的 UI 区域被半透明蒙版遮盖且不可交互，仅高亮区域可操作；步骤切换时蒙版覆盖区域与高亮区域同步更新

**边界提示**：步骤切换时不得出现无响应空白区或短暂全屏遮挡

**判定**：pass | 置信度 78%

**推理**：完整的双层蒙版遮挡+高亮穿透机制。HoleMaskImage 重写 IsRaycastLocationValid，孔内穿透孔外拦截；AssistantPanel 使用 CircleGuide 材质实现圆形高亮。步骤切换时蒙版通过 SetMaskArea 同步更新。

**证据**：
- `HoleMaskImage.cs` L16-34 `IsRaycastLocationValid` — hasHole=true 时孔内穿透，孔外拦截 ✅
- `TutorialMask.cs` L44-81 `SetHoleAtPosition / ClearHole` — 世界坐标转蒙版本地坐标构造孔洞 ✅
- `AssistantPanel.cs` L109-148,244-247 `SetMaskColor / ShowAssistantPanel` — 半透明黑色背景遮罩 ✅
- `PlaneEditUI_Portrait.Tutorial.cs` L139-157 `SetMaskArea` — 步骤切换时蒙版同步更新 ✅
- `PlaneEditorManager.Operate.cs` L193-207 `OnEnterTutorialArea` — 进入教程区域时调用 SetMaskArea ✅

**缺口**：CircleGuide shader 层面细节未验证；帧间短暂遮挡需运行时确认

**附带发现**：
- OnExitTutorialArea 方法体为空，退出教程区域无响应
- TutorialMask 保留 IsTest 调试开关，生产环境误开会显示调试矩形

---

### 3. [新手引导 > 组装界面 > 拖拽安装] — pass (75%)

**规则**：组件拖拽教学中，玩家将组件从背包拖拽至飞船组装区域有效位置并释放则视为安装成功；若仅点击组件未拖拽或拖拽至非法区域释放，则组件回退至背包原位，手势引导重新出现

**边界提示**：拖拽过程中手指抬起区域非高亮有效位置均视为非法释放；回退后组件在背包中的位置与操作前一致

**判定**：pass | 置信度 75%

**推理**：超过拖拽阈值后创建 UI 克隆体、原物品 alpha=0；进入有效区域时创建游戏世界组件并完成教程。仅点击（未超阈值）直接清除状态。拖拽未进入有效区域：销毁克隆体、恢复 alpha=1。手势引导面板 canClosePanel=false 持续显示。

**证据**：
- `PlaneEditUI_Portrait.cs` L159-269 `HandleTouchDrag / HandleTouchUp / CreateDragClone` — 拖拽阈值检测→创建克隆→恢复 ✅
- `PlaneEditUI_Portrait.cs` L361-393 `OnDragCloneEnterArea` — 进入拖放区域时完成教程步骤 ✅
- `PlaneUITutorialPanel.cs` L239-286 `StartDragNormalPartTutorial` — canClosePanel=false 面板持续显示 ✅
- `PlaneEditorManager.Operate.cs` L193-207 `OnEnterTutorialArea` — TUTORIAL_IS_COMPLETED 事件 ✅
- `AssistantPanel.cs` L310-348 `OnEventAction` — 面板不关闭 ✅

**缺口**：进入有效区域即完成教程，不存在"进入后又拖出"的中间状态

**附带发现**：
- OnDragCloneExitArea 方法体完全为空
- HandleTouchDown 中 DragItem 时序异常可能导致拖拽失效

---

### 4. [新手引导 > 组装界面 > 重叠提示] — uncertain (55%)

**规则**：玩家首次将组件拖拽至与已安装组件重叠的位置时，旋转和翻转按钮变为高亮状态并显示对应中文提示文字（Tutorial_text_6），提示持续至组件被正确安装到非重叠有效位置

**边界提示**：若组件初次即放置到非重叠位置则此提示不出现；同一教程周期内该提示仅显示一次（mIsShowedTips 控制）

**判定**：uncertain | 置信度 55%

**推理**：代码中存在重叠提示触发逻辑和 mIsShowedTips 一次性控制，Tutorial_text_6 文字显示 + 初始非重叠不触发均成立。但「旋转和翻转按钮变为高亮状态」在代码中缺乏证据——PlaneOperatePanel_Portrait 中按钮在整个流程中没有 highlight/color/interactable 状态变更。

**证据**：
- `PlaneEditorManager.Data.cs` L391-395 `CheckValidInstall` — 重叠时调用 ShowTutorialTips ✅
- `PlaneUITutorialPanel.cs` L289-325 `StartCompletePartAssemblyTutorial` — mIsShowedTips 门控 + Tutorial_text_6 ✅
- `PlaneUITutorialPanel.cs` L128-133 `ClosePanel` — 重置 mIsShowedTips ✅
- `PlaneOperatePanel_Portrait.cs` L293-297 `TryInstall` — 安装成功时触发完成分支 ✅

**缺口**：旋转/翻转按钮高亮在 cs 代码中未找到，可能通过 Animator 或 Prefab 实现

**附带发现**：
- hitInstallEffectGO 特效可能因 mIsShowedTips 重置导致泄漏

---

### 5. [新手引导 > 一键替换] — uncertain (50%)

**规则**：点击一键替换按钮后，系统将背包中比当前装备 Rarity 更高的同 PartId 组件替换到飞船上，被替换组件移回背包；替换过程中播放新组件飞入与被替换组件飞出的过渡动画

**边界提示**：教程中替换目标为 210010 组件；若同时存在多个可替换候选，引导仅替换 GetBetterSolution 返回的第一个匹配项

**判定**：uncertain | 置信度 50%

**推理**：GetBetterSolution 的 Rarity 比对逻辑正确，但多处与契约不符：(1) OnReplaceClick 替换全部可升级项而非仅首项；(2) 无飞入/飞出过渡动画代码，仅 Effect_UI_SKinSuccess 闪光特效；(3) 210010 在代码中未硬编码；(4) 旧组件 DestroyImmediate 直接销毁无视觉回退。

**证据**：
- `PlaneEditorManager.Data.cs` L1363-1428 `GetBetterSolution` — 按 PartId 分组 Rarity 贪心匹配 ✅
- `PlaneEditPanel_Portrait.cs` L506-526 `OnReplaceClick` — 遍历全部条目调用 ReplacePlanePart ✅
- `PlaneEditorManager.cs` L670-709 `ReplacePlanePart` — DestroyImmediate + 闪光特效，无飞入/飞出动画 ✅
- `PlaneUITutorialPanel.cs` L347-396 `StartReplacePartTutorial` — 教程引导触发 OnReplaceClick ✅

**缺口**：飞入/飞出动画在 cs 代码中缺失；210010 未在代码中找到；替换全部而非仅首项

**附带发现**：
- OnReplaceClick 中 replaceCount 未达 count 时永远不自动保存
- ReplacePlanePart 无教程版本存在 null 访问风险

---

### 6. [新手引导 > 激活类组件放置] — uncertain (55%)

**规则**：激活类组件（ID 230057）在教程拖拽阶段仅可放置于预设的 4 个特定合法格位；拖拽过程中界面仅高亮显示当前可用的合法格位

**边界提示**：若上一步安装的普通组件占用了其中 2 格，则仅显示剩余 2 格为合法位置；4 格全满时拖拽操作被禁止

**判定**：uncertain | 置信度 55%

**推理**：tutorialValidArea（RectTransform）限制拖拽区域，但「4 个特定合法格位」在代码中未找到离散定义——tutorialValidArea 是连续矩形区域而非 4 个离散格位。已占用格位排除逻辑未确认。

**证据**：
- `PlaneEditorManager.Operate.cs` L136-163 `CheckInValidArea` — 教程阶段启用 tutorialValidArea 检测 ✅
- `PlaneEditorManager.Operate.cs` L380-510 `OnDrag` — Clamp 到 tutorialValidArea 范围内 ✅
- `PlaneEditorManager.Operate.cs` L193-207 `OnEnterTutorialArea` — 进入时 dispatch 完成事件 ✅
- `PlaneUITutorialPanel.cs` L319-323 `StartCompletePartAssemblyTutorial` — 仅普通组件阶段有合法格位提示 ✅

**缺口**：4 格合法位置的离散定义和已占用格位排除逻辑缺失

**附带发现**：
- mInstallVaildAreaGameObject 拼写错误（Vaild→Valid）
- OnEnterTutorialArea 在组件从列表拖入时也被调用

---

### 7. [新手引导 > 激活组件拖拽状态] — ⚠️ fail (78%)

**规则**：拖拽激活组件过程中，系统实时检测位置并在三种状态间循环切换显示：(1) 未拖至合法格位时显示 Tutorial_text_14 黄色范围提示；(2) 已放入合法格位但未成功激活武器组件时显示 Tutorial_text_12 蓝圈位置提示并高亮旋转/翻转按钮；(3) 成功激活武器组件时显示 Tutorial_text_13 激活成功提示

**边界提示**：组件从合法格位移出后状态回切到第一种；三种状态随玩家操作循环检测变化，不是单向线性推进

**判定**：fail | 置信度 78%

**推理**：三状态循环在代码中不成立：
1. **Tutorial_text_14 用途错误**：仅存在于 `MoveTutorialPanel.cs`（战斗移动教程），与改装编辑器完全无关
2. **无先置显示**：`StartFindValidPositionTutorial()` 直接显示 Tutorial_text_12，无 text_14 分支
3. **单向推进**：`CheckPlaneEditCurTutorial()` 通过 stepEnum+1 递增，无循环/回退
4. **退出无响应**：`OnExitTutorialArea()` 方法体为空，组件移出后无状态回切
5. **架构冲突**：需求的三状态循环与现有单向步骤推进架构根本性矛盾

**证据**：
- `MoveTutorialPanel.cs` L21-24 `Start` — Tutorial_text_14 仅在战斗移动教程中使用 ✅
- `PlaneUITutorialPanel.cs` L514-540 `StartFindValidPositionTutorial` — 直接显示 text_12，无 text_14 ✅
- `PlaneUITutorialPanel.cs` L543-593 `StartCompletedSkillPartAssemblyTutorial` — text_13 在独立步骤中 ✅
- `TutorialUtil.cs` L255-277 `CheckPlaneEditCurTutorial` — stepEnum+1 单向递增 ✅
- `PlaneOperatePanel_Portrait.cs` L299-304 `TryInstall` — 直接 FinishTutorial 推进无回退 ✅

**缺口**：无

**附带发现**：
- OnExitTutorialArea() 方法体为空，组件移出合法区域后无任何处理
- CheckInValidArea 中的自动检测已被注释掉
- 三状态循环需求与单向步骤架构存在根本性冲突——需要架构层面的改造才能实现

---

### 8. [新手引导 > 激活影响显示] — pass (80%)

**规则**：选中带有激活影响效果的组件时，其影响范围（Effect 类型格子）常驻显示，被影响位置的组件增加特效提示（ValidSprite / AreaSprite），且该显示不受蒙版或其他 UI 遮挡

**边界提示**：组件取消选中后影响范围和特效立即消失；拖拽过程中影响范围实时随位置更新（UpdateEffGrid）

**判定**：pass | 置信度 80%

**推理**：ShowEffGrid 显示 Effect 类型格子，UpdateEffGrid 实时检测重叠并调用 UpdateEffect 设置 ValidSprite/AreaSprite。拖拽时持续调用 UpdateEffGrid。EffGrid 使用 SpriteRenderer 世界空间渲染，不受 UI Canvas 蒙版遮挡。HideEffGrid 在取消选中时调用。

**证据**：
- `PlaneOperatePanel_Portrait.cs` L80 `Init` — 选中时调用 ShowEffGrid ✅
- `CPlanePartInfo.cs` L479-491 `UpdateEffGrid` — 遍历 EffGridList 调用 UpdateEffect ✅
- `PlaneEditHitGrid.cs` L60-67 `UpdateEffect` — 检测重叠设置 ValidSprite/AreaSprite ✅
- `PlaneEditorManager.Operate.cs` L516-519 `OnDrag` — 拖拽时实时 UpdateEffGrid ✅
- `CPlanePartInfo.cs` L439-445 `HideEffGrid` — 取消选中时隐藏 ✅
- `PlaneEditHitGrid.cs` L20 `class PlaneEditHitGrid` — SpriteRenderer 世界空间不受 UI 蒙版遮挡 ✅

**缺口**：SpriteRenderer 的 sortingOrder 具体配置需通过 Prefab 确认

---

### 9. [新手引导 > 引导步骤顺序] — pass (80%)

**规则**：新手引导必须按 TutorialStepEnum 枚举定义的线性顺序执行（FirstBattle→MoveControl→...→CompletedSkillPartAssembly），任意前置步骤未完成时后续步骤不可进入；场景切换后引导进度保持不重置

**边界提示**：进度由 mTutorialProgress 持久化到服务端（AccessManager.SetTutorialProcess）和本地（PlayerPrefs）双重存储

**判定**：pass | 置信度 80%

**推理**：TutorialStepEnum 严格线性枚举序列。FinishTutorial 仅当 progress > mTutorialProgress 时更新（只进不退）。双重存储确认：服务端 SetTutorialProcess + 本地 PlayerPrefs。mTutorialProgress 作为 UserData 持久化字段跨会话/场景保留。

**证据**：
- `TutorialUtil.cs` L17-83 `TutorialStepEnum` — 完整线性枚举 ✅
- `TutorialUtil.cs` L330-361 `FinishTutorial` — progress > mTutorialProgress 才更新 ✅
- `TutorialUtil.cs` L152-253 `CheckMainPanelCurTutorialIn` — 基于进度判定当前步骤 ✅
- `UserData.PlayerInfo.cs` L65 `UserData` — mTutorialProgress 持久化字段 ✅
- `AccessManager.Query.cs` L14-22 `SetTutorialProcess` — 服务端同步 ✅
- `TutorialProgressResponse.cs` L30 — 服务端恢复进度 ✅

**缺口**：stepEnum+1 兜底逻辑依赖枚举连续性（当前确实连续）

**附带发现**：
- FinishTutorial 中 catch(QueryException ex) 为空体，进度写入失败时无感知
- TutorialManager.EnableTutorial 硬编码返回 false，可能存在代码死角

---

### 10. [新手引导 > 固定组件 ID] — uncertain (60%)

**规则**：新手引导中引用的组件 ID（230068 硬编码为 TUTORIAL_PART_ID_1、230057 硬编码为 TUTORIAL_PART_ID_2）在教程期间必须存在于玩家数据中，组件缺失时有容错处理而非流程卡死

**边界提示**：210010 在教程代码中未硬编码，替换目标由 GetBetterSolution 按 Rarity 数据驱动；组件缺失时当前容错为静默回退到战斗步骤，无用户可见提示，存在无限循环风险

**判定**：uncertain | 置信度 60%

**推理**：硬编码确认（TUTORIAL_PART_ID_1=230068, TUTORIAL_PART_ID_2=230057）。容错存在但不完善：缺失时回退到战斗步骤（静默无提示），但若 Stage 已通关则 SecondBattle→SecondEdit 可能形成无限循环。210010 在代码中未硬编码，替换由 Rarity 数据驱动。

**证据**：
- `PlaneUITutorialPanel.cs` L14-22 — TUTORIAL_PART_ID_1=230068, TUTORIAL_PART_ID_2=230057 ✅
- `TutorialUtil.cs` L213-219 `CheckMainPanelCurTutorialIn` — PART_ID_1 缺失回退 FirstBattle ✅
- `TutorialUtil.cs` L233-239 `CheckMainPanelCurTutorialIn` — PART_ID_2 缺失回退 SecondBattle ✅
- `TutorialUtil.cs` L180-189 `CheckMainPanelCurTutorialIn` — SecondBattle 已通关时转 SecondEdit 形成循环 ✅
- `PlaneEditorManager.Data.cs` L1363-1408 `GetBetterSolution` — 无硬编码 210010 ✅

**缺口**：无法确认运行时是否有其他路径保证教程组件必然存在

**附带发现**：
- ⚠️ **P0 风险**：TUTORIAL_PART_ID_2 缺失时容错回退到 SecondBattle 可能形成无限循环
- 容错为静默回退，无用户可见错误提示或日志警告

---

### 11. [新手引导 > 关卡绑定] — pass (75%)

**规则**：新手引导的两个关卡通过 PreRequisiteLevel 和教程步骤顺序双重绑定：第一关未通关前第二关不可进入且按钮被禁用；引导关卡使用标准战斗接口（ChallengeBattleStage），战利品与正常关卡一致

**边界提示**：引导关卡战斗失败时是否重新挑战或直接视为通关，需求未明示

**判定**：pass | 置信度 75%

**推理**：双重绑定确认：(1) PreRequisiteLevel 配置锁定；(2) 教程步骤顺序约束。ChallengeBattleStage 标准接口统一使用。战利品通过统一配置无特殊排除。

**证据**：
- `BattleLevelConfig.cs` L57 — PreRequisiteLevel 字段定义 ✅
- `StarMapPoint.cs` L51-58 `Init` — CheckBattleStageFinish 判定锁定 ✅
- `TutorialUtil.cs` L180-189 `CheckMainPanelCurTutorialIn` — SecondBattle 步骤约束 ✅
- `ChapterPanel_MainPanel.cs` L260 `StartGame` — 标准 ChallengeBattleStage 接口 ✅
- `ChapterPanel_MainPanel.cs` L508-512 `SelectLevel` — 锁定关卡按钮禁用 ✅
- `BattleLevelConfig.cs` L69-85 — 战利品配置统一适用 ✅

**缺口**：配置表中第二关的 PreRequisiteLevel 实际值无法从代码确认；战斗失败行为未找到处理

**附带发现**：
- ⚠️ TutorialManager.EnterTutorialBattleByLevel 走本地模拟战斗（ServerName='Tutorial', 密码硬编码12345678），不通过 ChallengeBattleStage 服务端接口——教程战斗可能与正式关卡战利品不一致
- GameManager.Instance.mPassword = 12345678 硬编码存在安全风险

---

## 未走查契约

| moduleLabel | rule | verifyMethods | 跳过原因 |
|-------------|------|---------------|----------|
| 新手引导 > 移动教学 | 移动教学阶段为弱引导，玩家执行滑动屏幕操作后教学提示立即消失 | ui_test | 暂不支持 |
| 新手引导 > 组装界面 > 组件 tips 层级 | 组装界面中点击组件后弹出的 tips 面板层级高于蒙版层 | ui_test | 暂不支持 |
| 新手引导 > 语音同步 | 每个引导步骤的对应语音文件在文本提示出现时同步开始播放 | ui_test | 暂不支持 |
