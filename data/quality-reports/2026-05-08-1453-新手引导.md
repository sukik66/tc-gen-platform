# 质量契约走查报告

- 日期：2026-05-08 14:53
- 需求文档：新手引导-一测.docx
- 走查引擎：Cursor Agent（3 组并行）

---

## 总览

| 指标 | 数量 |
|------|------|
| 契约总数 | 19 |
| 代码走查 (code_review) | 18 |
| 通过 (pass) | 6 |
| 违规 (fail) | 9 |
| 存疑 (uncertain) | 3 |
| 跳过 (非 code_review) | 1（新手引导 > 移动教学，ui_test only） |
| 调度模式 | 并行（3 组 × 6 条，同时执行） |
| ast-grep | 已启用（组A/B/C 均执行成功，空方法体命中有效提示） |
| Evidence 验证 | total≈101, verified≈81, failed≈20（method_not_found 格式差异） |
| 映射缓存(v2) | modules_updated=1, clusters=12 |

---

## ⚡ 走查重点汇总

### 🔴 确认违规 — fail（9 条）

| # | 契约 | 核心违反点 | 置信度 | 优先级 |
|---|------|-----------|--------|--------|
| 1 | 武器自动战斗提示 | `MoveTutorialPanel.mContentText` 仅在 Start() 赋值，无任何隐藏/清除调用，Tutorial_text_1 文字**永久残留** | 88% | P2 |
| 2 | 拖拽安装-失败回退 | `ReturnToBag`/`ShowHandGuide` 等全库 **0 命中**，拖拽失败后无组件回退+手势重启逻辑（功能完全缺失） | 83% | **P0** |
| 3 | 重叠位置提示 | `HighlightRotate`/`BtnRotate.*highlight`/`ShowRotateTip` 全库 **0 命中**，重叠时旋转/翻转按钮无高亮实现 | 90% | P1 |
| 4 | 一键替换逻辑 | `HighlightBagItem`/`HighlightPlanePart` 全库 **0 命中**，背包候选组件与飞船待替换组件**双侧高亮完全缺失** | 88% | **P0** |
| 5 | 替换动画反馈 | `DOMove`/`TweenFly`/`FlyToSlot`/`BlockInput` 全库 **0 命中**；`ReplacePlanePart` 为即时替换（DestroyImmediate），飞入/飞出动画与输入屏蔽**三项全缺** | 92% | P1 |
| 6 | 激活组件放置 | `tutorialValidArea` 为单个 `RectTransform` 矩形，非 4 格位坐标列表；`CheckOccupied`/`FilterOccupied` 全库 **0 命中**，动态 4-N 占用排除逻辑**缺失** | 86% | **P0** |
| 7 | 激活组件拖拽三状态 | SR1-SR3 已实现（三态文字存在），但 `OnExitTutorialArea` **方法体完全为空**，组件移出后无状态回切，"循环检测"需求未实现 | 72% | **P0** |
| 8 | 激活按钮半径配置 | `mMinRadius` 无 `[SerializeField]`，运行时恒为 0（不可配置）；与 Effect 影响槽位数据**无任何关联** | 68% | P2 |
| 9 | 弱引导退出 | `anyClick` 全库 **0 命中**；Tutorial_text_13 完成后使用 `SetMaskColor(true)`（强引导特征），而非弱引导 anyClick 机制 | 65% | P2 |

### 🟡 存疑 — uncertain（3 条）

| # | 契约 | 存疑原因 | 置信度 |
|---|------|---------|--------|
| 1 | 组件tips层级 | sortingOrder/SetSiblingIndex 代码层无直接设置，可能依赖 Prefab 配置；Prefab 搜索未定位到具体属性 | 42% |
| 2 | 拖拽安装-有效路径 | SR-1/SR-3（手势/tips关闭）有部分证据；SR-2（文字提示保留）EndDragNormalPartTutorial 调用 ClosePanel 的具体作用域仍存争议 | 65% |
| 3 | 激活影响显示 | SR-1/SR-3（ShowEffGrid/HideEffGrid）通过；SR-2（被影响组件激活特效）— `UpdateEffect()` 仅切换 EffGrid Sprite，`ShowActivateEffect`/`EdgeLight` 全库 0 命中，组件本身无独立激活特效 | 75% |

### ✅ 通过 — pass（6 条）

| # | 契约 | 支撑证据摘要 | 置信度 |
|---|------|------------|--------|
| 1 | 关卡选择 | `SetMaskColor(true)` + `CircleAim` 镂空定位到 BattleButton，MainUITutorialPanel.StartMatchTutorial 完整实现 | 82% |
| 2 | 引导蒙版射线拦截 | `HoleMaskImage.IsRaycastLocationValid`：镂空内返回 false（放行），镂空外返回 true（拦截）；`SetHoleAtPosition` 步骤切换时同步更新 | 92% |
| 3 | 替换完成后续流程 | `StartSoftBackFromPlaneTutorial` (L385) 弱引导退出；`Tutorial_text_8` 对应 SecondBattle 步骤，链路完整 | 83% |
| 4 | 关卡绑定 | `PreRequisiteLevel` (StarMapPoint.cs:51-57) 实现关卡锁定；`ChallengeBattleStage` 为通用战斗接口 | 80% |
| 5 | 引导步骤顺序与持久化 | `progress > mTutorialProgress` 防倒退；`SetTutorialProcess`（服务端）+ `PlayerPrefs.SetInt`（本地双写）完整 | 95% |
| 6 | 固定组件ID容错 | 230068/230057 均有 null 检查容错路径（缺失→回退到 FirstBattle/SecondBattle，不卡死）；210010 非硬编码 | 92% |

---

## 走查详情

### 1. 新手引导 > 关卡选择 — pass (82%)

**规则**：新手引导 Step 0 触发时，关卡选择界面进入强引导态：全屏蒙版激活（SetMaskColor(true)），BattleButton 位置设有圆形镂空高亮（CircleAim），非镂空区域点击被拦截；第一关是唯一可发起战斗的关卡

**边界提示**：实现方式为蒙版拦截而非 interactable=false

**判定**：pass | 置信度 82%

**子要求判定**：

| SR | 描述 | 类型 | 判定 | 置信度 | 关键证据 |
|----|------|------|------|--------|---------|
| SR-1 | SetMaskColor(true) + CircleAim 镂空定位到 BattleButton | grep_existence | pass | 85% | MainUITutorialPanel.cs StartMatchTutorial |
| SR-2 | 强引导态下仅第一关可发起战斗 | llm_semantic | pass | 78% | 蒙版全屏拦截功能上等效 |

**证据**：
- `C:\Demo\client\Assets\Scripts\ExternalClient\UI\SubUIPanel\TutorialUI\MainUITutorialPanel.cs` L74-123 `StartMatchTutorial` — SetMaskColor(true) 激活蒙版，CircleAim 镂空定位到 BattleButton

---

### 2. 新手引导 > 移动教学 — 跳过（ui_test only）

---

### 3. 新手引导 > 武器自动战斗提示 — fail (88%)

**规则**：Step 1 弱引导阶段，Tutorial_text_1 在进入步骤时设置到持久文字区域（mContentText）显示；该文字提示存在隐藏触发机制，不永久残留到教程结束

**判定**：fail | 置信度 88%

**子要求判定**：

| SR | 描述 | 类型 | 判定 | 置信度 | 关键证据 |
|----|------|------|------|--------|---------|
| SR-1 | Tutorial_text_1 在 StartMoveTutorial 时设置到 mContentText | grep_existence | pass | 90% | MoveTutorialPanel.cs L23 |
| SR-2 | 存在隐藏 Tutorial_text_1 的触发机制 | llm_semantic | **fail** | 88% | mContentText 设置后无任何清除/隐藏调用 |

**汇总**：SR-2 fail → 整条 fail

**推理**：`MoveTutorialPanel.cs L23` 确认 Tutorial_text_1 设置到 mContentText；但代码中 mContentText 设置后无任何清除调用，Tutorial_text_1 文字将永久残留直到场景销毁。

**证据**：
- `C:\Demo\client\Assets\Scripts\Client\BattleUI\TutorialPanel\MoveTutorialPanel.cs` L23 `StartMoveTutorial` — Tutorial_text_1 设置到 mContentText（持久显示）

**缺口**：已验证：搜索 `mContentText.*hide|ClearMoveTutorial|HideContentText` 均无结果；mContentText 设置后无任何清除/隐藏调用，文字永久残留属于功能缺陷。

---

### 4. 新手引导 > 引导蒙版射线拦截 — pass (92%)

**规则**：HoleMaskImage.IsRaycastLocationValid 实现「镂空内放行（返回 false）、镂空外拦截（返回 true）」；步骤切换时 SetHoleAtPosition 同步更新镂空位置；蒙版关闭时通过 OnTutorialCloseHoleMask 或等效方法禁用蒙版

**判定**：pass | 置信度 92%

**子要求判定**：

| SR | 描述 | 类型 | 判定 | 置信度 | 关键证据 |
|----|------|------|------|--------|---------|
| SR-1 | IsRaycastLocationValid 实现镂空内/外逻辑 | llm_semantic | pass | 95% | HoleMaskImage.cs IsRaycastLocationValid |
| SR-2 | 步骤切换时 SetHoleAtPosition 被调用 | grep_existence | pass | 90% | TutorialMask 步骤切换调用链 |
| SR-3 | 蒙版关闭时调用禁用方法 | grep_existence | pass | 90% | OnTutorialCloseHoleMask 已实现 |

**证据**：
- HoleMaskImage.cs `IsRaycastLocationValid` — `hasHole=false` 全屏拦截；`holeRect.Contains(localPoint)` 为 true 时返回 false（放行）；否则 true（拦截）

---

### 5. 新手引导 > 组件tips层级 — uncertain (42%)

**规则**：Step 4/11 点击组件后弹出的 tips 面板渲染层级高于引导蒙版，tips 内容完整可见不被遮挡

**判定**：uncertain | 置信度 42%

**子要求判定**：

| SR | 描述 | 类型 | 判定 | 置信度 | 关键证据 |
|----|------|------|------|--------|---------|
| SR-1 | tips 面板 sortingOrder/SetSiblingIndex 高于蒙版层 | llm_semantic | uncertain | 40% | 代码层无直接设置，依赖 Prefab 配置 |
| SR-2 | 教程步骤推进时 tips 被关闭 | grep_existence | uncertain | 45% | ClosePartTip 存在但调用时机需确认 |

**缺口**：已验证：`sortingOrder.*tips|SetSiblingIndex.*tip` 代码层无命中；需检查 Prefab 中 Canvas sortingOrder 属性；视觉层级只能通过运行时确认。

---

### 6. 新手引导 > 拖拽安装-有效路径 — uncertain (65%)

**规则**：拖至黄色框有效位置释放后：手势提示消失，文字提示保留，组件tips关闭，黄色框高亮持续直至落位确认

**判定**：uncertain | 置信度 65%

**子要求判定**：

| SR | 描述 | 类型 | 判定 | 置信度 | 关键证据 |
|----|------|------|------|--------|---------|
| SR-1 | 拖拽成功后手势提示被隐藏 | grep_existence | uncertain | 60% | 部分手势隐藏逻辑存在但调用链不完整 |
| SR-2 | 文字提示不随 ClosePanel 消失 | llm_semantic | uncertain | 65% | EndDragNormalPartTutorial 调用 ClosePanel，文字层是否独立存争议 |
| SR-3 | 拖拽完成后组件 tips 被关闭 | grep_existence | uncertain | 70% | ClosePartTip 存在但调用时机待确认 |
| SR-4 | 黄色框高亮持续到落位确认 | llm_semantic | uncertain | 65% | tutorialValidArea 高亮状态管理逻辑部分存在 |

**缺口**：EndDragNormalPartTutorial 调用 ClosePanel 的具体范围需 Read 确认；文字提示层是否独立于 ClosePanel 需运行时验证。

---

### 7. 新手引导 > 拖拽安装-失败回退 — fail (83%)

**规则**：仅点击未拖拽或拖拽后未释放到有效区域时，组件回退到背包原位，手势引导重新触发

**判定**：fail | 置信度 83%

**子要求判定**：

| SR | 描述 | 类型 | 判定 | 置信度 | 关键证据 |
|----|------|------|------|--------|---------|
| SR-1 | 拖拽失败时组件回退到背包 | llm_semantic | **fail** | 85% | ReturnToBag/BackToBag 全库 0 命中，功能缺失 |
| SR-2 | 回退后手势引导重新触发 | grep_existence | **fail** | 80% | ShowHandGuide/RestartGuide 全库 0 命中 |

**汇总**：SR-1 和 SR-2 均 fail → 整条 fail

**推理**：`ReturnToBag`、`BackToBag`、`ShowHandGuide`、`RestartGuide`、`ShowDragGuide` 等关键词在全库均 0 命中，属于**功能完全缺失**，而非实现偏差。需新增拖拽失败回退 + 手势重启完整逻辑。

---

### 8. 新手引导 > 重叠位置提示 — fail (90%)

**规则**：首次将组件拖至重叠位置时旋转/翻转按钮高亮并显示中文提示；提示持续至正确安装；同周期最多触发一次

**判定**：fail | 置信度 90%

**子要求判定**：

| SR | 描述 | 类型 | 判定 | 置信度 | 关键证据 |
|----|------|------|------|--------|---------|
| SR-1 | 重叠后旋转/翻转按钮高亮 | grep_existence | **fail** | 95% | HighlightRotate/BtnRotate.*highlight 全库 0 命中 |
| SR-2 | 单次触发标志（mIsShowedTips）存在 | grep_existence | pass | 85% | mIsShowedTips L46 声明，L295 置 true |

**汇总**：SR-1 fail → 整条 fail

**推理**：重叠检测后仅弹出 Tutorial_text_6 文字提示，旋转/翻转按钮本身无任何高亮/SetActive 操作。SR-2 单次触发标志完整，但高亮视觉反馈缺失。

---

### 9. 新手引导 > 一键替换逻辑 — fail (88%)

**规则**：「一键替换」时 GetBetterSolution 执行数据替换；同时高亮背包候选组件（210010）和飞船待替换组件

**判定**：fail | 置信度 88%

**子要求判定**：

| SR | 描述 | 类型 | 判定 | 置信度 | 关键证据 |
|----|------|------|------|--------|---------|
| SR-1 | GetBetterSolution 按 Rarity 匹配并替换 | llm_semantic | pass | 92% | Data.cs:1363 Rarity 降序匹配，ReplacePlanePart 被调用 |
| SR-2 | 背包候选组件被高亮显示 | grep_existence | **fail** | 88% | HighlightBagItem/mHighlightPartId 全库 0 命中 |
| SR-3 | 飞船待替换组件被高亮显示 | grep_existence | **fail** | 88% | HighlightPlanePart/ShowReplaceHighlight 全库 0 命中 |

**汇总**：SR-2 和 SR-3 均 fail → 整条 fail

**推理**：GetBetterSolution 数据逻辑完整；SetReplacePart 仅将候选对象平移至教程面板层（blocksRaycasts=false），**不构成视觉高亮**；飞船侧待替换槽位无任何高亮代码。

---

### 10. 新手引导 > 替换动画反馈 — fail (92%)

**规则**：点击替换后飞入/飞出动画存在并触发；被替换组件有明显特效；动画期间操作被屏蔽

**判定**：fail | 置信度 92%

**子要求判定**：

| SR | 描述 | 类型 | 判定 | 置信度 | 关键证据 |
|----|------|------|------|--------|---------|
| SR-1 | 飞入动画存在并触发 | grep_existence | **fail** | 95% | DOMove/TweenFly/FlyToSlot 全库 0 命中 |
| SR-2 | 被替换组件有飞出/特效 | grep_existence | **fail** | 92% | FlyToBag/SwapEffect 全库 0 命中；旧组件被 DestroyImmediate |
| SR-3 | 动画期间操作被屏蔽 | grep_existence | **fail** | 90% | BlockInput/LockInput 全库 0 命中；替换为即时操作 |

**汇总**：三项 SR 均 fail → 整条 fail（三项全缺）

**证据（反向）**：
- `ReplacePlanePart` 调用 `DestroyImmediate(旧部件)` + `CreateReplacePlanePart`，为即时替换无动画
- `Effect_UI_SKinSuccess` 为安装成功特效，不等于「被替换组件飞出特效」

---

### 11. 新手引导 > 替换完成后续流程 — pass (83%)

**规则**：替换完成后引导退出组装界面，退出后推进到第二关引导步骤

**判定**：pass | 置信度 83%

**子要求判定**：

| SR | 描述 | 类型 | 判定 | 置信度 | 关键证据 |
|----|------|------|------|--------|---------|
| SR-1 | 替换完成后触发引导退出 | grep_existence | pass | 85% | StartSoftBackFromPlaneTutorial L385/398 |
| SR-2 | 退出后推进到第二关引导 | grep_existence | pass | 82% | Tutorial_text_8 对应 SecondBattle 步骤 |

**附带发现**：
- [已确认] `CheckNextTutorial()` 在 `onClick.Invoke()` 之前调用（L374 vs L382），存在步骤超前推进的时序风险
- [已确认] 使用弱引导（StartSoftBackFromPlaneTutorial）而非需求描述的「强引导退出」，存在玩家不操作导致流程卡住的风险

---

### 12. 新手引导 > 关卡绑定 — pass (80%)

**规则**：第二关未通关前锁定；两个教学关卡通过 ChallengeBattleStage 接口发起战斗

**判定**：pass | 置信度 80%

**子要求判定**：

| SR | 描述 | 类型 | 判定 | 置信度 | 关键证据 |
|----|------|------|------|--------|---------|
| SR-1 | 第二关有解锁前置条件检查 | grep_existence | pass | 82% | PreRequisiteLevel StarMapPoint.cs:51-57 |
| SR-2 | ChallengeBattleStage 接口被调用 | grep_existence | pass | 80% | 多处调用确认 |

**附带发现**：
- [已确认] 契约描述「双重绑定」（PreRequisiteLevel + 步骤进度），代码**仅有单重** PreRequisiteLevel 检查，无步骤进度叠加保护

---

### 13. 新手引导 > 激活组件放置 — fail (86%)

**规则**：激活类组件拖拽阶段仅可放置于最多 4 个特定合法格位；已占用 N 格时仅显示 4-N 个格位

**判定**：fail | 置信度 86%

**子要求判定**：

| SR | 描述 | 类型 | 判定 | 置信度 | 关键证据 |
|----|------|------|------|--------|---------|
| SR-1 | 存在 4 个特定合法格位定义 | grep_existence | uncertain | 65% | tutorialValidArea 命中（Operate.cs:65）但为单个矩形，非 4 格位列表 |
| SR-2 | 格位占用检测动态 4-N 排除 | llm_semantic | **fail** | 90% | CheckOccupied/FilterOccupied 全库 0 命中；tutorialValidArea 为静态矩形 |

**汇总**：SR-2 fail → 整条 fail

**推理**：`tutorialValidArea` 为单个 `RectTransform` 矩形区域，矩形是否恰好对应 4 个格位需 Prefab 确认；动态占用排除（4-N 显示）完全不存在，无论占用多少格，合法区域固定不变。

---

### 14. 新手引导 > 激活组件拖拽三状态 — fail (72%)

**规则**：三种状态随玩家操作循环切换：(1)不在合法格位→显示 Tutorial_text_11/14 并高亮黄色范围；(2)在合法格位但未激活→显示 Tutorial_text_12；(3)激活成功→显示 Tutorial_text_13；组件移出后状态回切至(1)

**判定**：fail | 置信度 72%

**子要求判定**：

| SR | 描述 | 类型 | 判定 | 置信度 | 关键证据 |
|----|------|------|------|--------|---------|
| SR-1 | 状态(1)显示 Tutorial_text_11/14 | grep_existence | pass | 85% | StartDragSkillPartTutorial L481 显示 Tutorial_text_11 |
| SR-2 | 状态(2)显示 Tutorial_text_12 | grep_existence | pass | 88% | StartFindValidPositionTutorial L527 显示 Tutorial_text_12 |
| SR-3 | 状态(3)显示 Tutorial_text_13 | grep_existence | pass | 90% | StartCompletedSkillPartAssemblyTutorial L559 显示 Tutorial_text_13 |
| SR-4 | 组件移出后状态回切至(1)（循环检测） | llm_semantic | **fail** | 88% | OnExitTutorialArea 方法体完全为空 |

**汇总**：SR-4 fail → 整条 fail

**证据（反向）**：
- `PlaneEditorManager.Operate.cs:210-213` `OnExitTutorialArea` 方法体**完全为空**，组件离开合法区域后不触发任何状态回切
- 状态机为单向线性：DragSkillPart→FindValidPosition→CompletedSkillPartAssembly，无回退路径

**附带发现**：
- [已确认] `Tutorial_text_14` 仅用于战斗移动教程（MoveTutorialPanel.cs:24），与拖拽三状态无关，是历史遗留命名混淆点

---

### 15. 新手引导 > 激活影响显示 — uncertain (75%)

**规则**：选中激活效果组件时，Effect 影响范围格位常驻高亮（ShowEffGrid）；被影响组件增加激活特效（ShowActivateEffect）；取消选中后高亮和特效立即消失（HideEffGrid）

**判定**：uncertain | 置信度 75%

**子要求判定**：

| SR | 描述 | 类型 | 判定 | 置信度 | 关键证据 |
|----|------|------|------|--------|---------|
| SR-1 | 选中时调用 ShowEffGrid | grep_existence | pass | 85% | PlaneOperatePanel_Portrait.cs:80 Init() 调用 ShowEffGrid |
| SR-2 | 被影响组件增加激活特效 | grep_existence | uncertain | 55% | UpdateEffect() 仅切换 EffGrid Sprite；ShowActivateEffect/EdgeLight 全库 0 命中 |
| SR-3 | 取消选中后调用 HideEffGrid | grep_existence | pass | 85% | PlaneEditorManager.Operate.cs:269,286 取消选中调用 HideEffGrid |

**汇总**：SR-2 uncertain → 整条 uncertain

**缺口**：
- 已验证：`ShowActivateEffect|EdgeLight.*active|ActiveEffect` 全库 0 命中
- `PlanePartEdgeLight` 仅用于稀有度颜色，未被激活影响机制复用
- 需设计确认：「被影响组件增加特效」是否要求组件本身有独立视觉效果，还是格子 Sprite 切换已足够

---

### 16. 新手引导 > 激活按钮半径配置 — fail (68%)

**规则**：旋转/翻转按钮点击半径参数（mMinRadius）考虑 Effect 影响位置，或为独立可配置参数

**判定**：fail | 置信度 68%

**子要求判定**：

| SR | 描述 | 类型 | 判定 | 置信度 | 关键证据 |
|----|------|------|------|--------|---------|
| SR-1 | mMinRadius 存在且可配置 | grep_existence | uncertain | 65% | mMinRadius 声明存在但无 [SerializeField]，运行时恒为 0 |
| SR-2 | mMinRadius 与 Effect 数据关联或独立可配置 | llm_semantic | **fail** | 72% | 半径由形状格子计算，与 Effect 影响槽位无任何关联 |

**汇总**：SR-2 fail → 整条 fail

**推理**：mMinRadius 在 PlaneOperatePanel_Portrait.cs、PlaneOperatePanel.cs、WeaponOperatePanel.cs 三处声明均无 `[SerializeField]`，运行时恒为默认值 0，完全不起「独立可配置」的作用。按钮半径由 `GetMaxVertexDistance(GetAllGridPos(), GetCoreGrid().GridPos)` 基于组件形状计算，与 Effect 影响槽位数据无关联。

---

### 17. 新手引导 > 引导步骤顺序与持久化 — pass (95%)

**规则**：步骤严格线性执行，progress > mTutorialProgress 防倒退；服务端+本地双重持久化

**判定**：pass | 置信度 95%

**子要求判定**：

| SR | 描述 | 类型 | 判定 | 置信度 | 关键证据 |
|----|------|------|------|--------|---------|
| SR-1 | progress > mTutorialProgress 防倒退 | grep_existence | pass | 98% | TutorialUtil.cs:334 `if (progress > ... mTutorialProgress)` |
| SR-2 | SetTutorialProcess 服务端接口被调用 | grep_existence | pass | 95% | TutorialUtil.cs:338 `AccessManager.Instance.SetTutorialProcess(progress, true)` |
| SR-3 | PlayerPrefs 本地备份被写入 | grep_existence | pass | 92% | TutorialUtil.cs:344 `PlayerPrefs.SetInt(...)` 双写（单步+总进度） |

**证据**：
- `C:\Demo\client\Assets\Scripts\ExternalClient\Utils\TutorialUtil.cs` L334-351 — 防倒退+服务端同步+本地双写完整逻辑

---

### 18. 新手引导 > 固定组件ID容错 — pass (92%)

**规则**：230068/230057 使用处有 null 检查；缺失时有容错回退路径

**判定**：pass | 置信度 92%

**子要求判定**：

| SR | 描述 | 类型 | 判定 | 置信度 | 关键证据 |
|----|------|------|------|--------|---------|
| SR-1 | 230068 有 null 检查 + 容错路径 | llm_semantic | pass | 92% | TutorialUtil.cs:213-239 `targetPart != null` → FirstEdit，else → FirstBattle |
| SR-2 | 230057 有 null 检查 + 容错路径 | llm_semantic | pass | 92% | TutorialUtil.cs:213-239 `targetPart != null` → SecondEdit，else → SecondBattle |

**证据**：
- `C:\Demo\client\Assets\Scripts\ExternalClient\Utils\TutorialUtil.cs` L213-239 — 两个 ID 的对称 null 检查与回退路径

---

### 19. 新手引导 > 弱引导退出 — fail (65%)

**规则**：激活成功（Tutorial_text_13）后触发弱引导退出；anyClick/任意操作响应退出；进度推进至最终完成状态

**判定**：fail | 置信度 65%

**子要求判定**：

| SR | 描述 | 类型 | 判定 | 置信度 | 关键证据 |
|----|------|------|------|--------|---------|
| SR-1 | 激活成功后存在弱引导退出（anyClick） | grep_existence | **fail** | 70% | anyClick 全库 0 命中；Tutorial_text_13 完成后使用 SetMaskColor(true) 强引导而非弱引导 |
| SR-2 | 退出后进度推进至最终完成状态 | grep_existence | pass | 75% | CompletedSkillPartAssembly 在 FinishAction 中被推进（L587） |

**汇总**：SR-1 fail → 整条 fail

**推理**：`anyClick` 全库 0 命中；`StartSoftBackFromPlaneTutorial` 存在但只在 Tutorial_text_7（一键替换完成）步骤触发，Tutorial_text_13 之后无弱引导机制。该步骤实际使用 SetMaskColor(true)（强引导特征），与需求「弱引导退出」不符。

---

## 未走查契约

| moduleLabel | rule | verifyMethods | 跳过原因 |
|-------------|------|---------------|----------|
| 新手引导 > 移动教学 | 玩家执行滑动后 Tutorial_text_1 提示立即隐藏 | ui_test | 运行时交互行为，需真机验证 |
